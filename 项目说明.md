# Quant MAS 项目说明

> **文档用途**：帮助初次接触本项目的同学理解「这个系统和量化交易有什么关系」，以及如何在控制平台上完成第一次回测。  
> **适用对象**：SRTP 组员、本地开发、服务器实验  
> **最后更新**：2026-06-09

---

## 1. 一句话定位

**Quant MAS 是一个「量化策略研究、验证与审计」平台，不是实盘自动交易系统。**

你在浏览器里看到的 Web 控制平台，是套在 **Quant Engine（量化引擎）** 外面的一层 **研究工作台**：用来提交回测任务、查看结果、做人工审查、导出论文产物。它解决的是「策略怎么从想法变成可复现、可审计的研究结论」，而不是「怎么连券商自动下单赚钱」。

---

## 2. 和「量化交易」的关系：两件事要分开

在业界，「量化交易」往往包含两条线。本系统 **只做第一条**。

| 类型 | 做什么 | Quant MAS 有没有 |
|------|--------|------------------|
| **量化研究** | 历史数据 → 策略/模型 → 回测 → 样本外验证 → 风控 → 论文/报告 | **有，这是核心** |
| **实盘交易** | 连券商 API → 实时行情 → 自动下单 → 持仓管理 | **没有，刻意禁用** |

README 与平台顶栏均明确：

- **Live trading disabled** — 实盘交易已禁用  
- **LLM agents do not place live orders** — LLM 智能体不下实盘订单  

因此：它不是「自动炒股软件」，而是 **量化研究员/策略开发** 常见工作流的工程化实现，且 **停在历史数据模拟**，不连接真实资金账户。

---

## 3. 系统里真正的「量化」在哪里

底层 **Quant Engine** 位于 `src/quant_mas/`，实现标准量化研究流水线：

```text
行情数据 (OHLCV parquet)
    ↓
特征工程 (features.parquet)
    ↓
策略 / 模型（如均线交叉 MovingAverageCross、ML 信号策略 ml_signal）
    ↓
回测引擎 BacktestEngine（模拟买卖、佣金/滑点、权益曲线）
    ↓
Walk-forward OOS（滚动样本外验证，论文级指标 oos.*）
    ↓
风控审查 + 审计日志 + 实验注册表 (experiments.json)
```

### 3.1 回测引擎在算什么

`BacktestEngine`（`src/quant_mas/backtest/engine.py`）对历史 OHLCV 数据：

1. 由策略生成买卖信号（如快/慢均线交叉）  
2. 按下一根 K 线开盘价模拟成交  
3. 计入佣金（`commission_bps`）与滑点（`slippage_bps`）  
4. 逐日更新现金、持仓、组合权益  
5. 输出权益曲线、成交记录、Sharpe/回撤等指标  

你在 **Backtests** 页点击「运行回测」，本质上就是在服务器上调用 `BacktestTool`，读取 `data/raw/market_data.parquet`，跑上述流程，产物写入 `outputs/reports/backtest_latest/`。

### 3.2 关键数据文件

| 路径 | 用途 |
|------|------|
| `data/raw/market_data.parquet` | 回测输入（OHLCV） |
| `data/features/features.parquet` | Walk-forward OOS 输入 |
| `outputs/reports/backtest_latest/` | 最新回测报告（metrics.json、equity_curve.csv 等） |
| `outputs/reports/walk_forward_latest/` | 最新 OOS 报告 |
| `outputs/reports/experiments.json` | 实验注册表 |

---

## 4. MAS、Agent 与 Web 控制台各自是什么

**Quant MAS = Quant（量化引擎）+ MAS（Multi-Agent System，多智能体系统）**

```text
┌─────────────────────────────────────────┐
│  Web 控制平台（frontend/）               │
│  提交 Job、看回测曲线、审查、导出论文      │
└──────────────────┬──────────────────────┘
                   │ HTTP /api/*
┌──────────────────▼──────────────────────┐
│  后端 Job 服务（src/backend/）           │
│  队列、进度、调用 Quant Engine 工具      │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│  Agent 层（src/quant_mas/agents/）       │
│  理解任务 → 路由到受控工具 → 汇总结果     │
│  不直接下单、不改动回测数学逻辑           │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│  Quant Engine（src/quant_mas/）        │
│  数据、回测、OOS、指标 — 确定性、可复现   │
└─────────────────────────────────────────┘
```

| 层次 | 角色 | 是否「量化交易」 |
|------|------|------------------|
| Web UI | 人机界面：填表单、看 Job、Refresh 摘要 | 研究管理，非交易 |
| Backend Job | 把 UI 请求转成 backtest / OOS / export 任务 | 任务调度，非交易 |
| Agent | 研究助理：选工具、查记忆、解释结果 | 编排，非计算核心 |
| Quant Engine | 真正算收益曲线、OOS 指标的地方 | **量化研究核心** |

**设计原则**：LLM 负责编排与解释，Python 代码负责量化计算，人负责最终审查。

### 4.1 工具注册与调用（ToolRegistry）

Agent **不能**直接 `import BacktestEngine`。标准路径：

```text
SupervisorAgent.route(task)
    → ToolRegistry.get("backtest").run(...)
        → BacktestTool 内部调用 Quant Engine
```

- **注册**：`ToolRegistry.register(tool)`，每个工具唯一 `name`
- **白名单**：`data_summary` · `backtest` · `train_model` · `ml_backtest` · `pipeline` · `risk_check` · `report`
- **MCP 网关**：`MCPToolCall` 先经 `ToolPolicy` 鉴权（拒绝 shell/broker/secrets），再执行

### 4.2 两种编排模式

| 模式 | 场景 |
|------|------|
| **SupervisorAgent** | 用户一句话 → 关键词匹配 → **单个工具** |
| **ResearchWorkflow** | 固定 6 节点 DAG：data_check → feature_build → train_model → ml_backtest → risk_check → report |
| **MCPScheduler** | YAML recipe，拓扑排序，写 `audit.jsonl` |

### 4.3 Help 页面与汇报材料

- Web 控制台 **Help** 页（`frontend/src/pages/Help.tsx`）：分步说明 Data / Backtest / OOS / Job 等流程
- SRTP 汇报：[Quant_MAS_ZJU_CS_Premium_PPT.html](../Quant_MAS_ZJU_CS_Premium_PPT.html)（31 页）· [Quant_MAS_汇报讲解稿.md](../Quant_MAS_汇报讲解稿.md)
- 实验数据：[docs/ppt_data/DATA_MANIFEST.md](docs/ppt_data/DATA_MANIFEST.md)

---

## 5. 与量化交易概念的对照表

| 量化交易/研究概念 | 在本系统中的体现 |
|-------------------|------------------|
| 行情数据 | `scripts/download_data.py` → `data/raw/market_data.parquet` |
| 特征 | `scripts/build_features.py` → `data/features/features.parquet` |
| 策略 | `MovingAverageCrossStrategy`、配置于 `configs/backtest.yaml` |
| 回测 | `BacktestEngine` + UI/CLI 提交 `backtest` Job |
| 交易成本 | `configs/backtest.yaml` 中 `commission_bps`、`slippage_bps` |
| 绩效指标 | Sharpe、回撤等；回测属 `simulation.*` / `backtest.summary` |
| 样本外验证 | Walk-forward OOS Job；指标属 **`oos.*`（论文级）** |
| 风控 | **Risk Review** 页人工 Approve / Reject |
| 实验管理 | **Experiments** 页 + `experiments.json` |
| 实盘下单 | **无** |

---

## 6. 为什么要做成这样

本项目面向 **AI Agent + 量化研究** 的科研与实习展示（SRTP、简历、论文），需要同时体现：

1. **会量化**：数据管道、回测、Walk-forward OOS、指标族区分  
2. **会 Agent 工程**：工具注册、Supervisor 路由、Memory/RAG  
3. **有合规边界**：审计日志、人工审查、不把 in-sample 回测当论文结论  

Web 控制台因此是 **量化研究实验的管理界面**（类似实验室的电子实验记录），而不是券商交易终端。

### 6.1 指标族提醒（写论文必读）

| 指标族 | 含义 | 能否用于论文结论 |
|--------|------|------------------|
| `simulation.*` / `backtest.summary` | 全样本或 in-sample 回测 | **不能**单独作为论文级结论 |
| `oos.*` | Walk-forward 样本外 | **可以**（经审计、人工审查后） |
| `training.*` | 训练集表现 | 不能替代 OOS |
| `population.*` | 种群/竞争学习 | 不能与 `oos.*` 混用 |

平台 Context Panel 与 Help 页均强调：**Paper-grade = OOS only**。

---

## 7. 场景一：我只想做一次完整回测，该点哪里？

下面是从零到看到结果的最短路径（**UI 方式** + **CLI 备选**）。

### 7.1 前置条件（服务器上，首次必做）

回测需要服务器上存在行情文件。SSH 登录后在项目根目录执行：

```bash
cd /mnt/localDisk3/weizian/Quant-MAS   # 按你的实际路径调整
mkdir -p data/raw

PYTHONPATH=src python scripts/download_data.py \
  --symbols AAPL MSFT SPY \
  --start 2018-01-01 \
  --end 2025-12-31 \
  --source auto

# 确认文件存在
ls -lh data/raw/market_data.parquet
```

后端需已启动（另见《量化交易试验系统控制平台使用说明.md》第 4、5 节）：

```bash
export QUANT_MAS_ARTIFACT_ROOT=/mnt/localDisk3/weizian/Quant-MAS
export QUANT_MAS_AUTH_MODE=open
PYTHONPATH=src python -m uvicorn backend.app:app --host 0.0.0.0 --port 8000
```

本地 Windows 需开 SSH 隧道并启动前端：

```powershell
ssh -p 9961 -L 8000:127.0.0.1:8000 weizian@10.98.36.128
cd frontend
npm run dev
```

浏览器打开 `http://localhost:5173`，顶栏应显示 **Server connected**。

### 7.2 UI 操作步骤（推荐）

| 步骤 | 操作 |
|------|------|
| 1 | 侧边栏 → **Backtests（回测）** |
| 2 | 滚动到 **Run Backtest Job（运行回测任务）** |
| 3 | **Experiment name**：填实验名，如 `my_first_backtest` |
| 4 | **Fast window / Slow window**：默认 `5` / `20`（均线交叉参数，可不改） |
| 5 | 点击 **Run backtest / 运行回测** |
| 6 | 在下方 **Job Console** 观察：`queued` → `started` → `progress` → `completed` |
| 7 | 若 `failed`：阅读红色错误（常见：缺少 `market_data.parquet`） |
| 8 | `completed` 后，点击顶栏 **Refresh** |
| 9 | 同一页查看权益曲线小图、metric_family；侧边栏 **Experiments** 应出现新实验 |

也可在 **Experiments（实验）** 页底部提交同样的回测 Job，效果相同。

### 7.3 回测背后发生了什么

1. 前端 `POST /api/jobs`，`type: "backtest"`  
2. 后端 `job_tasks.py` 调用 `BacktestTool` 或带窗口覆盖的回测逻辑  
3. 读取 `configs/backtest.yaml` + `configs/storage.yaml`  
4. 加载 parquet → 跑 `MovingAverageCrossStrategy` → 写报告  
5. 更新 `outputs/reports/experiments.json`（若配置了 `QUANT_MAS_EXPERIMENT_MEMORY_PATH`）

### 7.4 产物在哪里

```text
outputs/reports/backtest_latest/
├── metrics.json          # Sharpe、回撤等
├── equity_curve.csv      # 权益曲线（UI 图表数据来源）
└── …                     # 其他报告文件
```

### 7.5 CLI 等价命令（不经过 UI）

在服务器项目根目录：

```bash
PYTHONPATH=src python scripts/run_backtest.py --config configs/backtest.yaml
```

完成后同样 Refresh UI，或在 `outputs/reports/backtest_latest/` 直接查看文件。

### 7.6 第一次回测 Checklist

- [ ] 服务器有 `data/raw/market_data.parquet`  
- [ ] uvicorn 8000 运行中  
- [ ] 本地 SSH 隧道 + `npm run dev`  
- [ ] 顶栏 **Server connected**  
- [ ] Backtests 页 Job 状态 **completed**  
- [ ] Refresh 后能看到权益曲线  

---

## 8. 场景二：Agent 和回测各自扮演什么角色？

这是本项目最容易混淆的一点：**回测是「算」，Agent 是「派活和解释」**。

### 8.1 回测（Backtest / Quant Engine）

| 维度 | 说明 |
|------|------|
| **本质** | 确定性数值计算 |
| **输入** | 历史 OHLCV、策略参数、成本模型 |
| **输出** | 权益曲线、成交、Sharpe、回撤等 |
| **谁执行** | `BacktestEngine`、Python、pandas，**不经过 LLM** |
| **可复现性** | 同数据同配置 → 同结果 |
| **在 UI 中** | Backtests / Experiments 页的 **Run backtest** Job |

**类比**：回测引擎是 **「计算器 + 历史模拟器」**，负责回答：「如果过去按这个规则买卖，账户曲线长什么样？」

### 8.2 Agent（多智能体层）

| 维度 | 说明 |
|------|------|
| **本质** | 任务理解与工具编排 |
| **输入** | 自然语言或结构化任务（如「跑一次回测」「查 OOS 基线」） |
| **输出** | 调用工具后的文字摘要、元数据、审计事件 |
| **谁执行** | `SupervisorAgent` 等，按规则或 LLM 路由到 **ToolRegistry** 中的工具 |
| **可复现性** | 路由到同一工具且参数相同 → 量化结果可复现；LLM 措辞可能不同 |
| **在 UI 中** | **Agents** 页的 Mock run；完整流水线可用 CLI `scripts/run_agent.py` |

**允许调用的工具示例**（Tools 页可见）：

- `run_backtest` — 触发回测  
- `read_report` — 读报告  
- `risk_check` — 风控检查  
- `query_memory` — 查实验记忆  

**类比**：Agent 是 **「研究助理」**，负责理解你要做什么、去调用正确的程序、帮你读结果——**自己不算 Sharpe**。

### 8.3 二者如何协作

```text
用户：「帮我用 5/20 均线回测 AAPL 数据」
         │
         ▼
    SupervisorAgent 解析任务
         │
         ▼
    路由 → backtest 工具（BacktestTool）
         │
         ▼
    BacktestEngine 确定性计算  ← 真正的量化在这里
         │
         ▼
    返回 metrics + 产物路径
         │
         ▼
    Agent 整理成可读摘要（可选 LLM）
         │
         ▼
    写入 experiments.json / 审计日志
```

### 8.4 当前 UI 里你实际用到的是哪一层？

| 你在 UI 做的操作 | 主要涉及 |
|------------------|----------|
| Backtests → Run backtest | **直接 Job → Quant Engine**（可不经 Agent） |
| Agents → Mock run | **Agent 层演示**（连通性/编排，非完整实盘流水线） |
| Memory / RAG 搜索 | Agent 可用的 **记忆工具** 的只读展示 |
| Risk Review 批准 | **人工**关卡，非 Agent 自动放行 |

**结论**：做 SRTP 实验时，**回测 Job 是量化核心**；Agent 层展示「多智能体如何安全地调用量化工具」，二者分工明确，不要期待 Agent 本身「预测股价」。

### 8.5 安全边界（Agent 不能做什么）

- 不连接券商、不提交真实订单  
- 不能绕过 ToolPolicy 调用未授权工具  
- 不能把 `simulation.*` 指标自动包装成论文级 `oos.*` 结论  
- 候选策略仍需 **Risk Review 人工审查**  

---

## 9. 场景三：OOS 和 Backtest 在量化研究里分别算什么？

### 9.1 Backtest（回测）是什么

**定义**：在一段历史数据上，用固定规则模拟交易，看策略表现。

本系统默认 UI 回测为 **Moving Average Cross（均线交叉）**：

- 快均线上穿慢均线 → 买入信号  
- 快均线下穿慢均线 → 卖出信号  
- 在全段历史上跑一遍 → 得到权益曲线和指标  

**特点**：

- 计算快、直观，适合 **探索想法**  
- 若参数在同一段数据上「看过再调」，容易 **过拟合**  
- 指标族：`backtest.summary`、`simulation.*`  
- **仅供研究参考，不是论文级最终结论**  

**在本项目**：Backtests 页 + `backtest` Job + `outputs/reports/backtest_latest/`。

### 9.2 OOS（Out-of-Sample，样本外）是什么

**定义**：在模型/策略 **从未「偷看过」的未来时间段** 上验证表现，检验是否只是拟合历史噪声。

本系统采用 **Walk-forward OOS**（滚动前进验证），配置见 `configs/walk_forward.yaml`：

```text
时间轴 ─────────────────────────────────────────────►

窗口1: [====训练====][验证][测试][OOS]
窗口2:      [====训练====][验证][测试][OOS]
窗口3:           [====训练====][验证][测试][OOS]
       … 按 step 滚动 …
```

每个窗口：

1. 在训练段 fit 模型（如 LightGBM 方向预测）  
2. 在验证/测试段调参与 sanity check  
3. 在 **OOS 段** 记录真正的样本外指标  
4. 汇总多窗口 → 得到 **`oos.*` 指标族**（如 OOS Sharpe 0.586）  

**特点**：

- 更接近真实「用过去预测未来」  
- 计算量更大，需要 `data/features/features.parquet`  
- 指标族：**`oos.*`** → 平台标为 **Paper-grade（论文级）**  
- 写 SRTP 论文/报告时应 **优先引用 OOS**，而非单次全样本回测  

**在本项目**：Walk-forward OOS 页 + `walk_forward_oos` Job + `outputs/reports/walk_forward_latest/`。

### 9.3 对比一览

| 维度 | Backtest（回测） | Walk-forward OOS |
|------|------------------|------------------|
| 目的 | 快速验证策略逻辑 | 严格检验泛化能力 |
| 数据 | 通常整段历史一次跑完 | 多窗口滚动，OOS 段未参与训练 |
| 本系统策略示例 | 均线交叉 | ML 信号 + LightGBM（见 walk_forward.yaml） |
| 输入文件 | `market_data.parquet` | `features.parquet` |
| 指标族 | `simulation.*` / `backtest.summary` | **`oos.*`** |
| 能否写进论文结论 | 一般 **不能** 单独作为最终结论 | **可以**（经审查后） |
| UI 入口 | Backtests | Walk-forward OOS |
| Job type | `backtest` | `walk_forward_oos` |

### 9.4 推荐研究顺序

```text
1. 下载数据 (download_data.py)
2. 构建特征 (build_features.py)        ← OOS 需要
3. 回测 (Backtest Job)                 ← 快速看策略是否合理
4. Walk-forward OOS (OOS Job)          ← 论文级验证
5. Risk Review 人工审查
6. Paper Artifacts 导出
7. 论文中只强调 oos.* 指标
```

### 9.5 运行 OOS 的简要步骤（UI）

| 步骤 | 操作 |
|------|------|
| 1 | 服务器确保已有 `data/features/features.parquet`（`build_features.py`） |
| 2 | 侧边栏 → **Walk-forward OOS** |
| 3 | 填写实验名 → **Run walk-forward OOS** |
| 4 | Job Console 等待 `completed` → 顶栏 **Refresh** |
| 5 | 查看 OOS Sharpe、窗口数等；Context Panel 基线可对比 `oos.*` |

---

## 10. 整体理解：一个比喻

> **Quant MAS = 量化研究的「工厂 + 实验室 + 档案室」**

| 比喻 | 对应模块 |
|------|----------|
| **工厂** | Quant Engine：回测、OOS、算指标 |
| **实验室** | Agent + Job：规划实验、调用工具 |
| **档案室** | Experiments、Audit、Paper：登记、审计、导出 |
| **没有「营业部」** | 无券商接口、无实盘下单 |

---

## 11. 相关文档

| 文档 | 内容 |
|------|------|
| [README.md](README.md) | 项目总览（中英） |
| [量化交易试验系统控制平台使用说明.md](量化交易试验系统控制平台使用说明.md) | SSH、后端、前端、Job 操作、故障排查 |
| [项目指导.md](项目指导.md) | 完整开发与实验指导 |
| 控制平台 **Help** 页 | 前端内置分章节操作指南（中英切换） |
| [docs/research_protocol.md](docs/research_protocol.md) | 研究协议与指标规范 |

---

## 12. 免责声明

1. 本平台 **不提供投资建议**，不承诺收益。  
2. **禁止**将本平台用于未经审计的实盘下单。  
3. 论文与报告请仅引用经 Walk-forward 验证的 **`oos.*`** 指标。  
4. 回测表现不等于未来真实交易结果。

---

*如有疑问，可先阅读 Help 页「故障排查」章节，或对照《量化交易试验系统控制平台使用说明.md》第 12 节。*

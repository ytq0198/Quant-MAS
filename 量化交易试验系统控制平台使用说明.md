# Quant MAS 量化研究试验系统控制平台 — 使用说明

> **文档版本**：v5 Enterprise Research Console  
> **适用对象**：本地开发机（Windows）+ 远程 GPU/算力服务器（a6000）  
> **最后更新**：2026-06

---

## 1. 平台定位（请先读）

Quant MAS 控制平台是 **多智能体量化研究试验系统** 的 Web 工作台，用于：

- 管理实验、回测、Walk-forward OOS、风险审查、审计与论文产物  
- 在浏览器中提交研究任务（Job）并查看进度  
- 展示 Agent / Tool / Memory / 可观测性等企业级模块  

**它不是实盘交易系统。**

| 原则 | 说明 |
|------|------|
| Live trading disabled | 实盘交易已禁用 |
| LLM agents do not place live orders | LLM 智能体不下实盘订单 |
| Research-only | 所有回测/OOS 仅用于科研与论文 |
| Paper-grade = OOS only | 论文级结论只能使用经审计的 `oos.*` 指标 |
| Human review required | 候选策略推进需人工审查 |

---

## 2. 系统架构

典型开发/实验架构：**前端在本地 Windows，后端在服务器**。

```text
┌─────────────────────────────────────────────────────────────┐
│  本地 Windows                                                │
│  浏览器  http://localhost:5173                               │
│       ↓                                                      │
│  Vite 开发服务器 (npm run dev)                               │
│       ↓  代理 /api → http://127.0.0.1:8000                   │
│  SSH 本地端口 8000                                           │
└───────────────────────────┬─────────────────────────────────┘
                            │  SSH 隧道 (-L 8000:127.0.0.1:8000)
┌───────────────────────────▼─────────────────────────────────┐
│  远程服务器 (a6000)                                          │
│  uvicorn backend.app:app  →  127.0.0.1:8000                 │
│       ↓                                                      │
│  Quant Engine (src/quant_mas/) + 产物目录 outputs/           │
└─────────────────────────────────────────────────────────────┘
```

**数据流**：浏览器只访问本地 5173；所有 `/api/*` 经 Vite 转发到本机 8000，再经 SSH 隧道到达服务器后端。

---

## 3. 环境准备

### 3.1 本地（Windows）

| 依赖 | 版本建议 |
|------|----------|
| Node.js | 18+ |
| npm | 随 Node 安装 |
| Git | 用于同步代码 |
| OpenSSH 客户端 | Windows 10/11 自带，用于 SSH 隧道 |

### 3.2 服务器（a6000）

| 项目 | 说明 |
|------|------|
| SSH | `ssh -p 9961 weizian@10.98.36.128` |
| 项目路径 | `/mnt/localDisk3/weizian/Quant-MAS` |
| Conda 环境 | `/mnt/localDisk3/weizian/conda_envs/quant-mas` |
| Python | 3.11+ |
| 后端端口 | `8000`（绑定 `0.0.0.0` 或 `127.0.0.1` 均可；隧道用 127.0.0.1） |

> 注意：主机名 `a6000-9961` 在部分本地网络无法解析，请使用 IP `10.98.36.128`。

---

## 4. 服务器后端部署与启动

### 4.1 登录服务器

```bash
ssh -p 9961 weizian@10.98.36.128
```

### 4.2 进入项目并激活环境

```bash
cd /mnt/localDisk3/weizian/Quant-MAS
source /mnt/localDisk3/weizian/conda_envs/quant-mas/bin/activate
# 若使用 conda activate：
# conda activate /mnt/localDisk3/weizian/conda_envs/quant-mas
```

### 4.3 安装/更新依赖（首次或依赖变更后）

```bash
python -m pip install -e ".[api]"
```

### 4.4 配置环境变量（读取真实实验产物）

在启动 uvicorn **之前**执行：

```bash
export QUANT_MAS_ARTIFACT_ROOT=/mnt/localDisk3/weizian/Quant-MAS
export QUANT_MAS_EXPERIMENT_MEMORY_PATH=/mnt/localDisk3/weizian/Quant-MAS/outputs/reports/experiments.json
export QUANT_MAS_PAPER_DIR=/mnt/localDisk3/weizian/Quant-MAS/outputs/paper
export QUANT_MAS_AUDIT_DIR=/mnt/localDisk3/weizian/Quant-MAS/outputs/pipelines
```

可选：

```bash
export QUANT_MAS_LOG_ROOT=/mnt/localDisk3/weizian/Quant-MAS/logs
export QUANT_MAS_AUDIT_WRITE_PATH=/mnt/localDisk3/weizian/Quant-MAS/logs/backend_audit.jsonl
```

### 4.5 认证模式（二选一）

**开发/内网（推荐起步）— open 模式，无需 API Key：**

```bash
export QUANT_MAS_AUTH_MODE=open
```

**服务器对外/多人使用 — api_key 模式：**

```bash
export QUANT_MAS_AUTH_MODE=api_key
export QUANT_MAS_API_KEYS="viewer-secret:viewer,research-secret:researcher,reviewer-secret:reviewer,admin-secret:admin"
```

角色说明：

| 角色 | 权限 |
|------|------|
| viewer | 只读大部分 GET 接口 |
| researcher | 提交 Job、运行 Agent、导出论文 |
| reviewer | 查看审计日志、批准/拒绝审查 |
| admin | 最高权限 |

### 4.6 启动后端

```bash
cd /mnt/localDisk3/weizian/Quant-MAS
PYTHONPATH=src python -m uvicorn backend.app:app --host 0.0.0.0 --port 8000
```

保持该终端运行。在服务器上自检：

```bash
curl http://127.0.0.1:8000/api/status
curl http://127.0.0.1:8000/api/health
```

应返回 JSON，且 `live_trading_enabled` 为 `false`。

---

## 5. SSH 隧道（本地 Windows → 服务器后端）

在 **本地 PowerShell** 新开一个终端，执行（保持连接不断开）：

```powershell
ssh -p 9961 -L 8000:127.0.0.1:8000 weizian@10.98.36.128
```

含义：把本机 `localhost:8000` 转发到服务器上的 `127.0.0.1:8000`。

隧道建立后，在本地验证：

```powershell
curl http://127.0.0.1:8000/api/status
```

若返回 JSON，说明隧道正常。

**常见问题：**

| 现象 | 处理 |
|------|------|
| `Connection refused` | 服务器 uvicorn 未启动，或端口不是 8000 |
| 隧道断开 | 重新执行 ssh -L 命令 |
| 本地 8000 被占用 | 改用 `-L 8001:127.0.0.1:8000`，并修改 `frontend/vite.config.ts` 中 proxy target 为 `8001` |

---

## 6. 本地前端启动

在 **另一个本地 PowerShell** 终端：

```powershell
Set-Location "d:\scientific reasearch and work\SRTP\Quant MAS\frontend"
npm install          # 首次需要
npm run dev
```

浏览器访问：

```text
http://localhost:5173
```

Vite 会将 `/api` 代理到 `http://127.0.0.1:8000`（即 SSH 隧道入口）。

### 6.1 连接状态判断

顶栏 Badge 含义：

| 显示 | 含义 |
|------|------|
| **Server connected** | 已成功连接服务器后端 |
| **Local fallback** | 后端不可达，使用本地夹具数据（功能受限） |

请点击 **Refresh** 或在 **Settings** 保存 API Key 后刷新。

### 6.2 语言切换

- 顶栏或页面右上角 **🌐** 按钮：中文 ↔ English  
- 侧边栏底部也可切换  
- 选择会保存在浏览器 `localStorage`

---

## 7. 实验数据准备（首次跑 Job 前）

UI 提交的回测/OOS 任务会调用服务器上的 Quant Engine，需要数据文件存在。

在 **服务器** 项目根目录执行：

```bash
# 1. 下载行情数据
python scripts/download_data.py

# 2. 构建特征（Walk-forward OOS 需要）
python scripts/build_features.py
```

关键路径（相对项目根）：

| 文件 | 用途 |
|------|------|
| `data/raw/market_data.parquet` | 回测输入 |
| `data/features/features.parquet` | OOS 输入 |
| `outputs/reports/experiments.json` | 实验注册表 |
| `outputs/reports/backtest_latest/` | 最新回测产物 |
| `outputs/paper/` | 论文导出目录 |

若缺少数据，Job 会 **failed**，Job Console 会显示具体错误（如文件不存在）。

---

## 8. 控制平台界面说明

### 8.1 整体布局

```text
┌──────────┬────────────────────────────────────┬─────────────┐
│ Sidebar  │  Header（标题、连接状态、Refresh）   │ Context     │
│ 导航     ├────────────────────────────────────┤ Panel       │
│          │  主内容区（当前页面）                 │ 安全边界等  │
└──────────┴────────────────────────────────────┴─────────────┘
```

- **Settings** 页面无右侧 Context Panel，宽度更宽。  
- **API Key** 仅在 **Settings** 中配置，不在首页。

### 8.2 导航菜单

| 菜单 | 功能 |
|------|------|
| **Overview** | 总览：基线 KPI、工作流、安全边界、模块快捷入口 |
| **Experiments** | 实验注册表 + 提交回测 Job |
| **Backtests** | 回测摘要 + 提交回测 Job |
| **Walk-forward OOS** | 论文级 OOS 指标 + 提交 OOS Job |
| **Risk Review** | 风险关卡、审查队列、批准/拒绝 |
| **Agents** | 智能体卡片、Mock Run 控制台 |
| **Tools** | 受控工具与 ToolPolicy |
| **Memory / RAG** | 记忆检索、RAG 文档 |
| **Audit Logs** | 审计事件表 |
| **Paper Artifacts** | 论文产物列表 + 导出 Job |
| **Database** | 数据库/向量库/图数据库状态 |
| **Observability** | 系统健康、Job 列表、日志、配置 |
| **Settings** | API Key、后端连接、环境变量、免责声明 |

---

## 9. 核心操作：提交研究任务（Job）

平台已从「只读展示」升级为 **可执行研究控制台**。以下操作会真正调用服务器 Quant Engine。

### 9.1 运行回测

1. 进入 **Backtests** 或 **Experiments**  
2. 填写 **Experiment name**（如 `ui_ma_backtest_001`）  
3. 可选调整 **Fast window / Slow window**（默认 5 / 20）  
4. 点击 **Run backtest** / **运行回测**  
5. 下方 **Job Console** 显示进度与事件  
6. 状态变为 `completed` 后，点击顶栏 **Refresh** 查看更新后的回测曲线与实验注册表  

后端等价操作：`POST /api/jobs`，`type: "backtest"`。

产物位置：`outputs/reports/backtest_latest/`。

### 9.2 运行 Walk-forward OOS

1. 进入 **Walk-forward OOS**  
2. 填写实验名 → **Run walk-forward OOS**  
3. 等待 Job Console 完成  
4. Refresh 后查看 OOS Sharpe、窗口数等  

需已有 `data/features/features.parquet`。

产物位置：`outputs/reports/walk_forward_latest/`。

### 9.3 导出论文产物

1. 进入 **Paper Artifacts**  
2. 点击 **Export paper artifacts**  
3. Job 完成后 Refresh，表格中应出现 CSV/MD/JSON 等文件  

依赖：`outputs/reports/experiments.json` 中有实验记录。

### 9.4 人工审查

1. 进入 **Risk Review**  
2. 在 **Review Queue** 中找到 `pending` 项  
3. 点击 **Approve** 或 **Reject**  

`api_key` 模式下需 **reviewer** 角色 Key（在 Settings 中配置）。

---

## 10. Settings 与 API Key

1. 左侧进入 **Settings**  
2. 在 **API Access** 输入 Key（Header 名：`X-Quant-MAS-Key`）  
3. 点击 **Save**，再点顶栏 **Refresh**  

Key 存储在浏览器 `localStorage`，不会写入仓库。

**open 模式**下可不填 Key；**api_key 模式**下：

| 操作 | 最低角色 |
|------|----------|
| 浏览 Dashboard | viewer |
| 提交 Job / 导出论文 | researcher |
| 批准审查 / 读审计 | reviewer |

---

## 11. 各页面只读功能摘要

### Overview
- 测试基线（如 361 passed）、OOS Sharpe 0.586  
- 研究工作流：Data → … → Paper Export  
- 模块快捷卡片，点击跳转对应页  

### Experiments
- 表格：experiment_id、metric_family、oos_available、status  
- 点击行查看右侧摘要  

### Backtests / OOS
- 非 OOS 回测仅供研究参考；OOS 为 paper-grade 指标族  
- 图表来自真实产物或 fallback  

### Agents
- **Mock run**：Phase 2 联调用，返回说明文本，非完整 Agent 流水线  

### Memory / RAG
- 输入关键词搜索实验/策略上下文  

### Observability
- System Health、Job 列表、Metrics、Server Logs、Effective Config  
- 若此页空白，见 [12. 故障排查](#12-故障排查)  

### Database
- 后端模式、向量库、Neo4j 关系等元数据  

---

## 12. 故障排查

### 12.1 顶栏显示 Local fallback

| 检查项 | 命令/操作 |
|--------|-----------|
| SSH 隧道是否开启 | 本地 `curl http://127.0.0.1:8000/api/status` |
| 服务器 uvicorn 是否运行 | 服务器 `curl http://127.0.0.1:8000/api/status` |
| 前端是否 `npm run dev` | 访问 `http://localhost:5173` |

### 12.2 Observability / Settings 页面空白

多因 `/api/config/effective` 字段与前端不一致导致。**请确保代码为最新版本**（含 `env` 字段修复与前端 `api/config.ts` 归一化），并 **Ctrl+F5** 强刷浏览器。

本地验证：

```powershell
curl http://127.0.0.1:8000/api/config/effective
```

响应应包含 `"env": { ... }` 或 `"values": { ... }`。

### 12.3 Job 状态 failed

打开 Job Console 查看错误信息。常见原因：

- 缺少 `data/raw/market_data.parquet` → 运行 `scripts/download_data.py`  
- 缺少 features → 运行 `scripts/build_features.py`  
- 权限不足 → Settings 中使用 `researcher` Key  

### 12.4 POST 接口 403

`api_key` 模式下 Key 角色不够。换用 researcher/reviewer Key 或临时 `QUANT_MAS_AUTH_MODE=open`。

### 12.5 实验注册表为空

配置 `QUANT_MAS_EXPERIMENT_MEMORY_PATH` 并至少成功运行一次回测或 CLI：

```bash
python scripts/run_backtest.py --config configs/backtest.yaml
```

---

## 13. 命令行替代方式（CLI）

UI 与 CLI 共用同一 Quant Engine，可互为补充。

```bash
# 回测
python scripts/run_backtest.py --config configs/backtest.yaml

# Walk-forward OOS
python scripts/run_walk_forward.py --config configs/walk_forward.yaml

# 论文导出
python scripts/export_paper_artifacts.py \
  --memory-path outputs/reports/experiments.json \
  --output-dir outputs/paper

# Agent 编排
python scripts/run_agent.py --config configs/agent.yaml
```

**推荐工作流**：CLI/服务器跑长任务 → UI 查看产物、审查、展示。

---

## 14. 生产构建（可选）

本地构建静态前端：

```powershell
cd frontend
npm run build
```

产物在 `frontend/dist/`，可由 Nginx 托管；`/api` 需反向代理到后端。

服务器 Docker 骨架见仓库根目录 `docker-compose.yml`、`Dockerfile.backend`、`Dockerfile.frontend`。

---

## 15. 关键 API 速查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/status` | 项目状态与基线 |
| GET | `/api/experiments` | 实验列表 |
| POST | `/api/jobs` | 提交 backtest / walk_forward_oos / paper_export |
| GET | `/api/jobs/{id}` | Job 详情 |
| GET | `/api/jobs/{id}/events` | Job 事件 |
| POST | `/api/artifacts/export` | 论文导出（入队） |
| POST | `/api/review/{id}/approve` | 批准审查 |
| GET | `/api/health/deep` | 深度健康检查 |
| GET | `/api/config/effective` | 脱敏配置 |

完整 API 见 `docs/api_reference.md`。

---

## 16. 安全与合规提醒

1. 本平台 **不提供投资建议**，不承诺收益。  
2. **禁止**将本平台用于未经审计的实盘下单。  
3. 勿将 `.env`、API Key、服务器密码提交到 Git。  
4. 论文结论请仅引用 **walk-forward OOS（`oos.*`）** 指标，勿与 `simulation.*` / `training.*` 混用。  
5. 所有候选策略须经过：回测 → 风控 → 审计 → **人工审查**。

---

## 17. 推荐阅读

| 文档 | 内容 |
|------|------|
| [README.md](README.md) | 项目总览 |
| [docs/fullstack_quickstart.md](docs/fullstack_quickstart.md) | 全栈快速开始 |
| [docs/ui_design.md](docs/ui_design.md) | UI 与研究控制台设计 |
| [项目v5企业级设计.md](项目v5企业级设计.md) | v5 企业级架构 |
| [docs/server_deployment.md](docs/server_deployment.md) | 服务器部署 |

---

## 18. 快速启动 Checklist

**服务器：**

- [ ] SSH 登录 a6000  
- [ ] `conda activate` / `source` 环境  
- [ ] 设置 `QUANT_MAS_*` 环境变量  
- [ ] 启动 `uvicorn` 8000  
- [ ] `curl /api/status` 正常  

**本地 Windows：**

- [ ] `ssh -L 8000:127.0.0.1:8000 ...` 隧道已开  
- [ ] `curl http://127.0.0.1:8000/api/status` 正常  
- [ ] `cd frontend && npm run dev`  
- [ ] 浏览器打开 `http://localhost:5173`  
- [ ] 顶栏 **Server connected**  
- [ ] （可选）Settings 填入 API Key  

**首次实验：**

- [ ] 服务器已有 `market_data.parquet`  
- [ ] UI 提交回测 Job → `completed`  
- [ ] Experiments 页出现新记录  

---

如有问题，请检查：SSH 隧道 → 后端进程 → 环境变量 → 数据文件 → API Key 角色 → 浏览器 Console（F12）报错。

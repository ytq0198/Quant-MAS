# Quant MAS 项目指导

> 基于 MAS 的量化研究与交易智能体系统  
> 第一阶段目标：**可运行、可回测、可训练、可记录进度** 的工程项目，而非一开始就追求复杂全自动实盘。

---

## 目录

1. [项目定位与工作模式](#1-项目定位与工作模式)
2. [系统架构](#2-系统架构)
3. [框架选型策略](#3-框架选型策略)
4. [项目目录结构](#4-项目目录结构)
5. [本地与服务器分工](#5-本地与服务器分工)
6. [开发工作流](#6-开发工作流)
7. [Git 分支策略](#7-git-分支策略)
8. [第一阶段：最小量化闭环](#8-第一阶段最小量化闭环)
9. [阶段规划（Phase 0–6）](#9-阶段规划phase-06)
10. [Codex 使用指南](#10-codex-使用指南)
11. [Cursor 文档维护](#11-cursor-文档维护)
12. [服务器部署与运行](#12-服务器部署与运行)
13. [同步原则与注意事项](#13-同步原则与注意事项)
14. [立即行动：下一阶段](#14-立即行动下一阶段)
15. [核心原则总结](#15-核心原则总结)
16. [当前进度快照](#16-当前进度快照)
17. [下一阶段行动计划（Prompt 11–20）](#17-下一阶段行动计划prompt-1120)
18. [数据获取策略与数据清单](#18-数据获取策略与数据清单)

---

## 1. 项目定位与工作模式

### 1.1 项目定位

**Quant MAS** — 基于多智能体系统（MAS）的量化研究与交易智能体平台。

核心原则：

- **Quant Engine** 负责确定性计算（数据、特征、模型、策略、回测、风控、执行）
- **Agent Layer** 负责研究、规划、解释、报告和工具编排
- **LLM Agent 不允许直接实盘下单**
- 所有交易信号必须经过回测、风控、审计和人工确认

### 1.2 工作环境

| 角色 | 路径 / 工具 | 职责 |
|------|-------------|------|
| 本地项目 | `D:\scientific reasearch and work\SRTP\Quant MAS` | 代码开发、文档、小数据测试 |
| Codex | 打开本地项目 | 主写代码 |
| Cursor | 本地 + SSH 服务器 | 记录进度、整理文档、服务器训练/回测 |
| ChatGPT | 对话 | 设计架构、拆任务、审查方案、分析结果 |
| GitHub | 远程仓库 | 代码版本同步 |
| 服务器 | Linux 远程 | 大数据、模型训练、批量回测、长期任务 |

> **注意**：本地路径含空格，命令行操作需加引号：
> ```powershell
> cd "D:\scientific reasearch and work\SRTP\Quant MAS"
> ```

### 1.3 整体工作闭环

```
设计 → Codex 编码 → 本地测试 → GitHub 同步 → 服务器训练 → 结果分析 → 再设计
```

**每日标准流程：**

1. 与 ChatGPT 确定今日任务
2. ChatGPT 拆成 Codex 子任务
3. 在 Codex 中打开本地项目，提交任务提示词
4. Codex 编写代码
5. 本地运行 `pytest` 或基础脚本
6. Cursor 更新 `docs/progress.md`
7. `git commit` → `push` 到 GitHub
8. Cursor SSH 连接服务器 → `git pull`
9. 服务器运行训练 / 回测
10. 将 metrics、日志、错误贴给 ChatGPT 分析
11. ChatGPT 给出下一轮修改建议

---

## 2. 系统架构

### 2.1 设计思路

**不要**设计成「一个 LLM 直接自动交易」。

**应该**设计成：

```
量化核心引擎 + 多智能体 MAS 编排 + 工具系统 + 记忆/RAG + 风控审计
```

### 2.2 架构分层

```
Quant MAS
├── Quant Engine Layer          # 确定性计算核心
│   ├── 数据层
│   ├── 特征层
│   ├── 模型训练层
│   ├── 策略层
│   ├── 回测层
│   ├── 风控层
│   └── 模拟交易层
│
├── Tool Layer                  # Agent 可调用的工具
│   ├── 数据 / 特征 / 回测 / 训练 / 风控 / 报告工具
│
├── MAS Agent Layer             # 研究与编排
│   ├── SupervisorAgent
│   ├── ResearchAgent / DataAgent / FeatureAgent
│   ├── ModelAgent / StrategyAgent / BacktestAgent
│   └── RiskAgent / ReportAgent / AuditAgent
│
├── Memory / RAG Layer          # 实验与知识检索
│   ├── 实验记忆 / 交易记忆
│   └── 策略文档库 / 回测报告库 / 研究资料检索
│
└── Orchestration Layer         # 流程编排（后期）
    ├── LangGraph / AgentScope（可选）
    ├── 内部任务流
    ├── MCP-style 工具适配
    └── 后期 A2A / ANP 扩展
```

**一句话**：Agent 负责研究、规划、解释和调度；真正的计算、回测、训练、风控必须由确定性的 Quant Engine 完成。

---

## 3. 框架选型策略

### 3.1 三阶段路线

| 阶段 | 方案 | 原因 |
|------|------|------|
| **第一阶段** | 自建轻量 Agent Core | 看得懂、改得动、跑得通、能写进论文/报告 |
| **第二阶段** | 引入 LangGraph | 量化研究天然是有状态工作流，适合流程编排 |
| **第三阶段** | 可选 AgentScope | 多主体市场模拟、多投资风格 Agent 协作 |

### 3.2 第一阶段：轻量 Agent Core

自建以下组件，**不急着上 LangGraph / AgentScope**：

- `Message`
- `Agent`
- `Tool` / `ToolRegistry`
- `InternalAgentBus`

### 3.3 第二阶段：LangGraph 编排节点

完成数据 → 特征 → 策略 → 回测 → 模型训练 → 报告后，引入：

`ResearchNode` → `DataCheckNode` → `FeatureBuildNode` → `TrainModelNode` → `BacktestNode` → `RiskCheckNode` → `ReportNode`

### 3.4 第三阶段：AgentScope（可选）

适合后期做多 Agent 对话协作与市场模拟，例如：

- 价值投资者 Agent / 动量交易者 Agent
- 风控 Agent / 市场模拟 Agent

---

## 4. 项目目录结构

```
Quant MAS/
├── README.md
├── pyproject.toml
├── requirements.txt
├── .env.example
├── .gitignore
│
├── configs/
│   ├── data.yaml
│   ├── features.yaml
│   ├── train.yaml
│   ├── backtest.yaml
│   ├── agent.yaml
│   ├── risk.yaml
│   └── storage.yaml
│
├── src/quant_mas/
│   ├── core/                   # message, config, llm, agent, events, exceptions
│   ├── agents/                 # supervisor, research, data, feature, model, ...
│   ├── tools/                  # base, registry, builtin/, quant/
│   ├── data/                   # fetchers, storage, catalog, validators, dataset
│   ├── features/               # technical, labels, pipelines
│   ├── models/                 # base, lightgbm_model, registry
│   ├── strategies/             # base, ma_cross, ml_signal, ensemble
│   ├── backtest/               # engine, portfolio, costs, metrics, report
│   ├── risk/                   # limits, position_sizing, drawdown_guard, kill_switch
│   ├── execution/              # broker_base, paper_broker, order_manager, trade_logger
│   ├── memory/                 # experiment_memory, trade_memory
│   ├── rag/                    # document_loader, chunker, retriever, vector_store
│   ├── context/                # context_builder, market_context, prompt_templates
│   └── utils/
│
├── scripts/
│   ├── download_data.py
│   ├── build_features.py
│   ├── train_model.py
│   ├── run_backtest.py
│   ├── run_pipeline.py          # 统一端到端 pipeline（Prompt 11）
│   ├── run_ml_backtest.py       # ML 信号回测（Prompt 16）
│   ├── run_walk_forward.py      # Walk-forward 评估（Prompt 17）
│   ├── run_agent.py
│   ├── paper_trade.py
│   └── generate_report.py
│
├── tests/
├── docs/
│   ├── progress.md
│   ├── architecture.md
│   ├── experiment_log.md
│   ├── meeting_notes.md
│   └── research_notes.md
│
├── notebooks/
├── outputs/                    # reports, models, logs, figures（不提交 git）
└── server/                     # setup_server.sh, train_remote.sh, ...
```

---

## 5. 本地与服务器分工

### 5.1 本地 Windows

**负责：** 代码开发、项目文档、小数据测试、单元测试、架构设计

**可存放：**
- 少量 sample / synthetic data
- 测试用 csv/parquet
- README、docs、configs

**不要放：** 大规模历史数据

### 5.2 服务器

**负责：** 大规模历史数据、特征数据、模型训练、批量回测、GPU 实验、日志和报告归档

**推荐目录结构：**

```
/opt/quant-mas/                          # 或 ~/quant-mas/
├── repo/Quant-MAS/
├── datasets/
│   ├── raw/
│   ├── processed/
│   └── features/
├── experiments/
│   ├── exp_001_ma_cross/
│   ├── exp_002_lightgbm/
│   └── exp_003_ml_signal/
├── models/
├── reports/
└── logs/
```

**服务器主要承担的任务类型：**

- 大规模历史数据存储与处理
- 长时间运行的训练 / 回测任务
- LightGBM / XGBoost 模型训练（含 GPU 实验）
- 批量回测、Walk-forward 多窗口实验
- 实验日志与报告归档

> **一句话**：本地负责开发和测试，服务器负责真实数据、训练、批量回测和实验归档。

### 5.3 Prompt 11–20：本地 vs 服务器分工

| Prompt | 任务 | 本地 | 服务器 | 说明 |
|--------|------|:----:|:------:|------|
| **11** | `run_pipeline.py` | ✅ | ✅ | 本地用小数据 / synthetic；服务器用真实行情 |
| **12** | 端到端测试 | ✅ | 可选 | 主要本地 `pytest tmp_path`，不依赖服务器 |
| **13** | 文档更新 | ✅ | ❌ | Cursor 本地维护 `docs/` |
| **14** | 服务器部署脚本 | ❌ | ✅ | **必须在服务器验证** |
| **15** | ML 训练输出 | 小数据可跑 | ✅ | 真实多标的 / 多年数据训练放服务器 |
| **16** | ML 信号回测 | 小数据可跑 | ✅ | 真实模型 + 多标的回测放服务器 |
| **17** | Walk-forward | 小数据可跑 | ✅ | 多窗口反复训练，**强烈建议服务器** |
| **18** | 风控层 | ✅ | 可选 | 本地开发测试；大规模组合回测时服务器验证 |
| **19** | SupervisorAgent | ✅ | 可选 | Agent 路由逻辑，主要本地测试 |
| **20** | Memory / RAG 雏形 | ✅ | 可选 | JSONL + 关键词检索本地即可；向量库 / 大量文档后再迁服务器 |

#### 必须在或强烈建议在服务器上跑

**1. Prompt 14：服务器部署脚本**

在服务器验证以下文件：

- `server/setup_server.sh`
- `server/run_server_tests.sh`
- `server/run_small_pipeline.sh`
- `configs/storage.server.yaml.example`

```bash
conda create -n quant-mas python=3.11 -y
conda activate quant-mas
pip install -r requirements.txt
pip install -e .
pytest
```

目的：确认服务器环境能正常运行项目。

**2. Prompt 15：真实 ML 训练**

多标的、多年数据、LightGBM / XGBoost 训练建议在服务器执行：

```bash
python scripts/train_model.py --config configs/train.yaml
```

原因：数据量大、训练耗时长、后续可能用 GPU；模型和特征重要性文件适合留在服务器。

**3. Prompt 16：MLSignalStrategy + ML 回测**

synthetic 数据可本地测；真实特征 + 真实模型 + 多标的 + 长区间回测放服务器：

```bash
python scripts/run_ml_backtest.py --config configs/backtest_ml.yaml
```

**4. Prompt 17：Walk-forward / 样本外报告**

Walk-forward 会反复执行切分 → 训练 → 预测 → 回测 → 汇总，多标的 / 多窗口 / 多参数组合时本地会很慢：

```bash
python scripts/run_walk_forward.py --config configs/walk_forward.yaml
```

**5. 真实端到端 Pipeline（Prompt 11）**

| 环境 | 用途 |
|------|------|
| **本地** | synthetic / sample parquet；验证流程逻辑 |
| **服务器** | 真实行情、真实特征、真实回测、完整实验记录 |

```bash
python scripts/run_pipeline.py \
  --symbols AAPL MSFT SPY QQQ NVDA \
  --start 2018-01-01 \
  --end 2025-12-31 \
  --storage-config configs/storage.server.yaml
```

#### 主要在本地跑的任务

| Prompt | 说明 | 本地命令 |
|--------|------|----------|
| **11** 初版 | synthetic / sample 小数据验证 | `python scripts/run_pipeline.py --help`；`pytest` |
| **12** | 单元 / 集成测试，`tmp_path` + synthetic | `pytest tests/test_end_to_end_pipeline.py` |
| **13** | Cursor 维护文档 | `docs/progress.md`、`architecture.md`、`experiment_log.md` |
| **18** | 风控计算量小，本地开发测试 | `pytest tests/test_risk.py` |
| **19** | Agent 路由逻辑 | `pytest tests/test_supervisor_agent.py` |
| **20** | JSONL、Markdown、关键词检索 | `pytest tests/test_memory_rag.py` |

---

## 6. 开发工作流

详见 [1.3 整体工作闭环](#13-整体工作闭环)。

**关键原则：**

- 每次只实现一个小模块
- 每次修改后保证 `pytest` 可运行
- 不要生成大数据文件
- 测试不得依赖真实网络请求和真实 LLM API

---

## 7. Git 分支策略

| 分支 | 用途 |
|------|------|
| `main` | 稳定版本 |
| `dev` | 日常开发 |
| `feature/project-skeleton` | 项目骨架 |
| `feature/data-layer` | 数据层 |
| `feature/backtest-engine` | 回测层 |
| `feature/model-training` | 模型训练 |
| `feature/agent-core` | Agent 框架 |
| `feature/rag-memory` | RAG 和记忆 |
| `feature/langgraph-orchestration` | LangGraph 编排（后期） |

**常用命令：**

```powershell
cd "D:\scientific reasearch and work\SRTP\Quant MAS"
git checkout -b feature/project-skeleton
git add .
git commit -m "Initialize quant mas project skeleton"
git push origin feature/project-skeleton
```

---

## 8. 第一阶段：最小量化闭环

第一阶段**不是完整 MAS**，而是跑通最小量化闭环：

```
数据读取 → 特征构建 → 均线策略 → 回测 → 指标输出 → 实验记录 → 报告生成
```

**第一阶段完成后应能运行：**

```powershell
python scripts/download_data.py --symbols AAPL MSFT SPY --start 2018-01-01 --end 2025-12-31
python scripts/build_features.py --config configs/features.yaml
python scripts/run_backtest.py --config configs/backtest.yaml
python scripts/generate_report.py --latest
pytest
```

---

## 9. 阶段规划（Phase 0–6）

### Phase 0：项目骨架 ✅ 已完成

- [x] 项目可安装（`pip install -e .`）
- [x] `pytest` 可跑
- [x] 目录结构清晰，README 清楚
- [x] `docs/progress.md` 可记录进度

### Phase 1：量化核心 MVP ✅ 基本完成，待收口

- [x] Parquet 数据存储与校验
- [x] 技术指标特征
- [x] 均线策略 + 简单回测
- [x] 绩效指标 + 报告输出 + 实验记录
- [ ] 统一 pipeline 脚本（`run_pipeline.py`）
- [ ] 端到端 pipeline 测试（`test_end_to_end_pipeline.py`）

### Phase 2：机器学习模型 🔄 进行中

- [x] LightGBM / XGBoost 基础训练
- [x] 时间序列切分（禁止随机切分）
- [x] 未来收益标签 + 特征重要性
- [ ] 完善训练输出（完整 metrics、artifact 路径）
- [ ] MLSignalStrategy + ML 回测闭环
- [ ] Walk-forward / 样本外评估报告
- [ ] 服务器真实训练实验

### Phase 3：Agent Core ✅ 基础完成，待增强

- [x] Message / LLMClient / BaseAgent
- [x] Tool / ToolRegistry
- [x] SupervisorAgent / ReportAgent
- [ ] 支持 ML 回测、风控检查、pipeline 路由
- [ ] RiskAgent 接入 RiskTool

### Phase 4：Memory + RAG 📋 待启动

- [x] ExperimentMemory 基础版
- [ ] ExperimentMemory 增强（搜索、排序、artifact）
- [ ] TradeMemory 空壳
- [ ] SimpleRetriever 关键词检索雏形

### Phase 5：LangGraph 编排 ⏸ 暂缓

- [ ] DataCheckNode → FeatureBuildNode → TrainModelNode → BacktestNode → AuditNode → RiskNode → ReportNode
- **前提**：Phase 2–4 稳定后再做

### Phase 6：Paper Trading ⏸ 暂缓

- [ ] PaperBroker / OrderManager / TradeLogger / RiskGate / HumanApprovalGate
- [ ] **仍然不要直接实盘**

---

## 10. Codex 使用指南

### 10.1 总背景提示词

将以下内容作为 Codex 项目总背景：

```
你正在帮助我开发一个科研项目，项目路径是：
D:\scientific reasearch and work\SRTP\Quant MAS

项目名称：Quant MAS

项目目标：
构建一个"基于多智能体系统 MAS 的量化研究与交易智能体平台"。

注意：
这不是一个让 LLM 直接实盘交易的系统。
系统的核心原则是：
1. Quant Engine 负责确定性计算，包括数据、特征、模型、策略、回测、风控、执行。
2. Agent Layer 负责研究、规划、解释、报告和工具编排。
3. LLM Agent 不允许直接实盘下单。
4. 所有交易信号必须经过回测、风控、审计和人工确认。
5. 第一阶段只实现最小量化闭环，不要一开始引入复杂 LangGraph / AgentScope。
6. 后期可以扩展 LangGraph、MCP、RAG、Memory 和多智能体协作。

技术要求：
1. Python 3.11+
2. 使用 src/quant_mas 作为主包目录
3. 使用 pytest
4. 使用 YAML 配置
5. 使用 Parquet 存储数据
6. 使用 pandas / numpy / scikit-learn
7. 机器学习模型优先支持 LightGBM 或 XGBoost
8. 所有测试不得依赖真实网络请求和真实 LLM API，必须使用 mock 或 synthetic data

开发方式：
请每次只实现一个小模块。
每次修改后保证 pytest 可以运行。
不要生成大数据文件。
不要把 data、outputs、models、logs 目录中的大文件加入 git。
```

### 10.2 分阶段任务提示词（Prompt 1–10）✅ 已完成

| 序号 | 任务 | 验收标准 | 状态 |
|------|------|----------|------|
| **Prompt 1** | 初始化项目骨架 | `pip install -e .` 成功；`pytest` 通过；`import quant_mas` 不报错 | ✅ |
| **Prompt 2** | 数据存储与目录管理 | `pytest tests/test_storage.py` 通过 | ✅ |
| **Prompt 3** | 数据下载和校验 | `pytest tests/test_data_validation.py` 通过；`download_data.py --help` 正常 | ✅ |
| **Prompt 4** | 特征工程 | `pytest tests/test_features.py` 通过；严禁未来函数 | ✅ |
| **Prompt 5** | 均线策略和回测 | `pytest tests/test_backtest.py` 通过；`run_backtest.py --help` 正常 | ✅ |
| **Prompt 6** | 实验记忆和报告 | `pytest tests/test_experiment_memory.py` 通过 | ✅ |
| **Prompt 7** | 模型训练 | `pytest tests/test_train_model.py` 通过；时间序列切分，禁止 label 泄露 | ✅ |
| **Prompt 8** | 轻量 Agent Core | `pytest tests/test_agent_core.py` 通过；不用 LangGraph/AgentScope | ✅ |
| **Prompt 9** | 量化 Tools 封装 | `pytest tests/test_quant_tools.py` 通过 | ✅ |
| **Prompt 10** | SupervisorAgent 和任务流 | `pytest tests/test_supervisor_agent.py` 通过；`run_agent.py --help` 正常 | ✅ |

**当前测试状态：42 passed**

### 10.3 下一阶段任务提示词（Prompt 11–20）

**不要**一次性全部交给 Codex，按顺序每次只提交一个 Prompt。

| 序号 | 阶段 | 目标 | 交给 Codex 的任务 | 验收标准 |
|------|------|------|-------------------|----------|
| **Prompt 11** | Phase 1 收口 | 本地跑通完整 CLI 链路 | 写 `scripts/run_pipeline.py` | 一条命令完成特征、回测、报告、实验记录 |
| **Prompt 12** | Phase 1 收口 | 做端到端测试 | 写 `tests/test_end_to_end_pipeline.py` | 不联网、不调 LLM，`pytest` 通过 |
| **Prompt 13** | Phase 1 收口 | 完善文档 | 更新 `docs/progress.md`、`architecture.md`、`experiment_log.md` | 文档反映当前真实进度 |
| **Prompt 14** | 服务器准备 | 让服务器可复现实验 | 写 `server/setup_server.sh` 和 `configs/storage.server.yaml.example` | 服务器能安装依赖并跑 `pytest` |
| **Prompt 15** | Phase 2 | 跑真实 LightGBM 训练 | 优化 `train_model.py` 输出和实验记录 | 生成模型、指标、特征重要性 |
| **Prompt 16** | Phase 2 | ML 信号接入回测 | 实现 `MLSignalStrategy` 和 ML 回测配置 | 模型预测能变成仓位信号并回测 |
| **Prompt 17** | Phase 2 | 样本外评估 | 增加 walk-forward / out-of-sample 报告 | 报告区分 train/val/test 表现 |
| **Prompt 18** | Phase 2.5 | 风控层补齐 | 实现 `RiskLimits`、`RiskDecision`、`RiskTool` | 风控可以裁剪或拒绝仓位 |
| **Prompt 19** | Phase 3 | Agent 能力增强 | 扩展 SupervisorAgent 路由 | 支持训练、ML 回测、风险检查、报告 |
| **Prompt 20** | Phase 4 准备 | Memory / RAG 基础 | 扩展 ExperimentMemory，新增文档索引雏形 | Agent 能检索历史实验和报告摘要 |

> Prompt 11–20 的完整提示词见 [附录 B. 下一阶段 Codex Prompt 详情（Prompt 11–20）](#b-下一阶段-codex-prompt-详情prompt-1120)。

### 10.4 正确开发顺序

**已完成（Prompt 1–10）：**

```
项目骨架 → 数据存储 → 数据下载与校验 → 特征工程 → 均线策略 → 回测引擎
  → 实验记录和报告 → 机器学习模型 → 轻量 Agent Core → Quant Tools → SupervisorAgent
```

**下一步（Prompt 11–20）：**

```
Phase 1 收口
  ↓ run_pipeline.py + 端到端测试 + 文档更新
服务器准备
  ↓ setup_server.sh + storage.server.yaml.example
Phase 2 机器学习
  ↓ 完善训练输出 → MLSignalStrategy → Walk-forward 报告
Phase 2.5 风控层
  ↓ RiskLimits / RiskTool
Phase 3 Agent 增强
  ↓ SupervisorAgent 扩展路由
Phase 4 Memory/RAG 雏形
  ↓ ExperimentMemory 增强 + SimpleRetriever
（暂缓）Phase 5 LangGraph / Phase 6 Paper Trading
```

> Prompt 1–10 详情见 [附录 A](#a-codex-分阶段-prompt-详情)；Prompt 11–20 详情见 [附录 B](#b-下一阶段-codex-prompt-详情prompt-1120)。

---

## 11. Cursor 文档维护

Cursor 负责维护以下文档：

### docs/progress.md

```markdown
# Quant MAS 开发进度

## 当前阶段
Phase 1 收口 + Phase 2 机器学习实验

## 已完成（Prompt 1–10）
- [x] 项目骨架、数据层、特征层、回测层
- [x] 实验记忆、模型训练、Agent Core、量化 Tools、SupervisorAgent
- [x] pytest：42 passed

## 进行中
- [ ] run_pipeline.py 统一 pipeline
- [ ] 端到端 pipeline 测试
- [ ] 服务器部署脚本

## 下一步
1. Prompt 11：run_pipeline.py
2. Prompt 12：test_end_to_end_pipeline.py
3. Prompt 14：服务器部署

## 最近实验
| 日期 | 实验 | 结果 | 问题 |
|------|------|------|------|
| 2026-06-01 | Prompt 1–10 全模块测试 | 42 passed | 无 |
```

### docs/experiment_log.md

记录每次实验：日期、数据、策略、参数、结果、问题、下一步。

### docs/architecture.md

记录架构变化与核心原则（LLM 不直接下单、策略必须回测和风控、Agent 只负责研究编排）。

---

## 12. 服务器部署与运行

### 12.1 初始部署

```bash
mkdir -p ~/quant-mas && cd ~/quant-mas
git clone <GitHub仓库地址> repo && cd repo

conda create -n quant-mas python=3.11 -y
conda activate quant-mas
pip install -r requirements.txt
pip install -e .
pytest
```

### 12.2 服务器配置

创建 `configs/storage.server.yaml`：

```yaml
project_root: /home/你的用户名/quant-mas
raw_data_dir: /home/你的用户名/quant-mas/datasets/raw
processed_data_dir: /home/你的用户名/quant-mas/datasets/processed
features_dir: /home/你的用户名/quant-mas/datasets/features
models_dir: /home/你的用户名/quant-mas/models
reports_dir: /home/你的用户名/quant-mas/reports
logs_dir: /home/你的用户名/quant-mas/logs
```

### 12.3 服务器运行示例

```bash
python scripts/download_data.py \
  --symbols AAPL MSFT SPY \
  --start 2018-01-01 --end 2025-12-31 \
  --output-dir ~/quant-mas/datasets/raw

python scripts/build_features.py \
  --input-dir ~/quant-mas/datasets/raw \
  --output-dir ~/quant-mas/datasets/features

python scripts/run_backtest.py --config configs/backtest.yaml
python scripts/train_model.py --config configs/train.yaml

# 统一 pipeline（真实数据）
python scripts/run_pipeline.py \
  --symbols AAPL MSFT SPY \
  --start 2018-01-01 --end 2025-12-31 \
  --storage-config configs/storage.server.yaml

# ML 信号回测
python scripts/run_ml_backtest.py --config configs/backtest_ml.yaml

# Walk-forward 样本外评估
python scripts/run_walk_forward.py --config configs/walk_forward.yaml
```

---

## 13. 同步原则与注意事项

| 内容 | 同步方式 |
|------|----------|
| 代码 | GitHub |
| 小配置 | GitHub |
| 大数据 | 留在服务器 |
| 模型文件 | 留在服务器；必要时只下载指标和报告 |
| 报告 | 可同步回本地 |

**`.gitignore` 必须忽略：**

```
data/
outputs/
*.parquet
*.pkl
*.joblib
*.pt
*.pth
*.ckpt
*.log
.env
__pycache__/
.pytest_cache/
```

**不要用 U 盘或手动复制大量文件。**

---

## 14. 立即行动：下一阶段

**Prompt 1–10 已完成，不应再重复搭骨架。**

当前最优先任务：**Prompt 11 — 新增统一 Pipeline 脚本**

原因：各模块已独立可用，但还缺稳定可复现的端到端流程。只有 pipeline 跑通后，服务器训练、ML 回测、Agent 编排才有意义。

**不要先做**：MLSignalStrategy、RAG、LangGraph。

### 交给 Codex 的第一个任务

执行 **Prompt 11**（完整提示词见 [附录 B](#prompt-11新增统一-pipeline-脚本)）。

### 本地验证

```powershell
cd "D:\scientific reasearch and work\SRTP\Quant MAS"
pytest
python scripts/run_pipeline.py --help
git status
```

### 每完成一个 Prompt 后的检查清单

1. 本地运行 `pytest`，确认全部通过
2. 运行相关 CLI 的 `--help`
3. 让 Cursor 更新 `docs/progress.md`、`docs/experiment_log.md`、`docs/architecture.md`
4. `git commit` → `push` 到 GitHub
5. 若涉及服务器：SSH 登录 → `git pull` → `conda activate quant-mas` → `pytest`

---

## 15. 核心原则总结

| 维度 | 策略 |
|------|------|
| 本地 | 代码开发、文档、轻量测试 |
| Codex | 按任务逐步实现模块 |
| Cursor | 整理文档 + SSH 服务器跑训练/回测 |
| GitHub | 代码同步和版本管理 |
| 服务器 | 大数据、训练、批量回测 |

**架构路线：**

1. **第一阶段**：量化核心 MVP（数据 → 特征 → 策略 → 回测 → 报告）
2. **第二阶段**：轻量 Agent Core + Tools（Agent 调用数据、训练、回测、报告）
3. **第三阶段**：RAG、Memory、LangGraph / AgentScope → 真正的 MAS

> **最重要的一句话：先把「量化系统」做可靠，再把「智能体系统」接上去。Agent 是大脑和调度器，不是替代回测、风控和训练的魔法。**

---

## 16. 当前进度快照

### 16.1 已完成模块（Prompt 1–10）

| 模块 | 内容 | 测试 |
|------|------|------|
| 数据层 | ParquetStorage、DataCatalog、YFinanceFetcher、validate_ohlcv | test_storage、test_data_validation |
| 特征层 | technical、labels、pipelines | test_features |
| 策略 / 回测 | MovingAverageCrossStrategy、BacktestEngine、metrics、report | test_backtest |
| 实验记忆 | ExperimentMemory、generate_report | test_experiment_memory |
| 模型训练 | LightGBMDirectionModel、train_model.py（70/15/15 时间切分） | test_train_model |
| Agent Core | Message、MockLLMClient、BaseAgent、ToolRegistry | test_agent_core |
| 量化 Tools | DataSummaryTool、BacktestTool、TrainModelTool、ReportTool | test_quant_tools |
| Supervisor | 规则路由（回测 / 训练 / 报告 / 数据） | test_supervisor_agent |

**pytest 状态：42 passed**

### 16.2 当前缺口

- 缺少 **统一 pipeline 入口**（`run_pipeline.py`）
- 缺少 **端到端闭环测试**（`test_end_to_end_pipeline.py`）
- ML 模型尚未接入 **策略回测闭环**（`MLSignalStrategy`）
- **风控层**尚未实现
- **Memory / RAG** 仅有基础版，未增强
- **服务器部署**尚未脚本化

### 16.3 阶段定位

```
✅ Phase 0  项目骨架
✅ Phase 1  量化核心 MVP（待收口：pipeline + e2e 测试）
🔄 Phase 2  机器学习模型与服务器训练
🔄 Phase 3  Agent Core（基础完成，待增强路由）
📋 Phase 4  Memory + RAG（待启动）
⏸ Phase 5  LangGraph 编排（暂缓）
⏸ Phase 6  Paper Trading（暂缓）
```

---

## 17. 下一阶段行动计划（Prompt 11–20）

### 17.1 阶段目标

进入 **Phase 2 机器学习模型与服务器训练实验**，同时为 **Phase 3 Agent Core 增强** 和 **Phase 4 Memory + RAG** 做准备。

核心路线不变：**量化核心可靠 → Agent 调度 → Memory/RAG → 后期流程编排**

### 17.2 行动步骤总览

| Step | 名称 | 目标 | 工具分工 | 对应 Prompt |
|------|------|------|----------|-------------|
| 1 | 小数据端到端跑通 | 验证 build_features → backtest → report 完整可用 | Codex 调试；Cursor 更新进度 | 11、12 |
| 2 | 统一 pipeline 脚本 | 一条命令串联特征、回测、报告 | Codex 编写 `run_pipeline.py` | 11 |
| 3 | 服务器部署测试 | 服务器跑小规模 pipeline | Codex 写部署脚本；Cursor SSH 验证 | 14 |
| 4 | 完善 ML 模块 | 完整训练输出 + 样本外回测 | Codex 编写；Cursor 整理 | 15、16、17 |
| 5 | Agent Core 扩展 | Agent 调用 ML 模块和量化工具 | Codex 扩展 SupervisorAgent | 19 |
| 6 | 风控层补齐 | 仓位裁剪 / 拒绝机制 | Codex 实现 RiskTool | 18 |
| 7 | Memory + RAG 雏形 | 实验检索 + 文档索引 | Codex 实现；Cursor 更新 architecture | 20 |
| 8 | 文档与进度更新 | 保持 docs 与代码同步 | Cursor 维护 | 13 |

### 17.3 推荐执行顺序

**本地 Codex（开发与测试）：**

```
Prompt 11 → Prompt 12 → Prompt 13 → Prompt 18 → Prompt 19 → Prompt 20
```

**服务器 Cursor SSH（部署与验证）：**

```
Prompt 14 → pytest → 小规模 pipeline
```

**服务器真实实验（大数据 / 训练 / 批量回测）：**

```
Prompt 15 → Prompt 16 → Prompt 17
```

**按 Prompt 编号的完整顺序：**

```
Prompt 11  run_pipeline.py              ← 现在最该先做（本地小数据）
Prompt 12  test_end_to_end_pipeline.py
Prompt 13  文档更新
Prompt 14  服务器部署脚本               ← 服务器验证
Prompt 15  完善 ML 训练输出             ← 服务器真实训练
Prompt 16  MLSignalStrategy             ← 服务器真实回测
Prompt 17  Walk-forward / 样本外报告    ← 服务器多窗口实验
Prompt 18  基础风控层                   ← 本地开发
Prompt 19  增强 SupervisorAgent         ← 本地开发
Prompt 20  Memory / RAG 雏形            ← 本地开发
```

### 17.4 Step 详情

#### Step 1–2：Phase 1 收口（本地）

**目标**：用少量 synthetic / sample parquet 验证完整 pipeline。

```powershell
python scripts/build_features.py --config configs/features.yaml
python scripts/run_backtest.py --config configs/backtest.yaml
python scripts/generate_report.py --latest
pytest
```

**验收**：报告完整生成；ExperimentMemory 记录实验元数据；`pytest` 全部通过。

#### Step 3：服务器环境部署

```bash
conda create -n quant-mas python=3.11 -y
conda activate quant-mas
pip install -r requirements.txt && pip install -e .
pytest
python scripts/run_pipeline.py --symbols AAPL MSFT SPY --start 2018-01-01 --end 2025-12-31
```

**验收**：服务器跑通小规模 pipeline，无报错，实验结果可记录。

#### Step 4：Phase 2 ML 模块

- 确认 `train_model.py` 输出 metrics.json、feature_importance.csv、model.pkl
- 实现 `MLSignalStrategy`，模型预测 → 仓位信号 → 回测
- 增加 walk-forward / 样本外评估，报告区分 train/val/test

#### Step 5–6：Agent + 风控

- SupervisorAgent 支持：ML 回测、风控检查、pipeline 全流程
- RiskLimits / RiskDecision / RiskTool：策略只生成 target_weight，风控有权裁剪或拒绝

#### Step 7：Memory + RAG 初步设计

- ExperimentMemory 增强：模型路径、特征表路径、回测报告路径
- TradeMemory 空壳（JSONL）
- RAG 文档路径：`docs/research_notes.md`、`docs/strategy_notes.md`、`outputs/reports/`

#### Step 8：后续 Phase 5 / 6（暂缓）

- **Phase 5**：LangGraph 编排（DataCheck → FeatureBuild → Train → Backtest → Audit → Risk → Report）
- **Phase 6**：Paper Trading（PaperBroker / RiskGate / HumanApprovalGate）

**前提**：Phase 2–4 稳定后再做。

---

## 18. 数据获取策略与数据清单

### 18.1 自动下载 vs 手动获取

**结论先行：**

- **Phase 1–2 科研实验**：用 **自动下载** 即可（yfinance 等），足够验证 pipeline、特征、回测、ML、Agent 调用
- **Phase 3–6 深度研究 / 实盘级回测**：需要 **手动 / 付费** 获取高质量数据，放到服务器 `datasets/raw/`

| 方式 | 优势 | 劣势 | 适合阶段 |
|------|------|------|----------|
| **自动下载** | 快速可用；与 `download_data.py` / `MarketDataFetcher` 直接兼容 | 免费 API 有限制；缺 Tick、深度财务、研报原文 | Phase 1–2 |
| **手动 / 付费** | 数据完整、精度高；适合论文和实盘级回测 | 需订阅或手动下载；需清洗格式化 | Phase 3–6 |

#### 自动下载（Phase 1–2）

集成到项目的方式：

```bash
python scripts/download_data.py \
  --symbols AAPL MSFT SPY \
  --start 2018-01-01 --end 2025-12-31 \
  --output-dir data/raw                    # 本地小数据

python scripts/download_data.py \
  --symbols AAPL MSFT SPY \
  --start 2018-01-01 --end 2025-12-31 \
  --output-dir ~/quant-mas/datasets/raw    # 服务器
```

常用 Python 数据源：

| 工具 | 内容 | 备注 |
|------|------|------|
| **yfinance** | 股票 OHLCV + 简单财务指标 | 项目已集成 `YFinanceFetcher` |
| **investpy** | 部分股票、ETF、指数 | 备选 |
| **Alpha Vantage** | OHLCV（需 API Key） | 免费版有频率限制 |

限制：Tick 级、分钟级、深度财务、机构研报通常无法通过免费 API 获取。

#### 手动 / 付费获取（Phase 3–6）

| 数据源 | 内容 |
|--------|------|
| Bloomberg / Wind / CSMAR | 高质量历史 OHLCV、中国市场数据 |
| Quandl | 美股、期货、ETF、宏观数据 |
| Interactive Brokers | 历史 Tick、Paper / 实盘 |
| EDGAR / SEC | 美国公司财报原文 |
| FactSet / News API | 研报、新闻、公告 |

手动数据放置路径：

```
~/quant-mas/datasets/raw/manual/
```

在 `configs/storage.server.yaml` 中指向该路径。

### 18.2 按 Phase 划分的数据清单

| Phase | 数据类别 | 数据源 / 下载方式 | 使用场景 | 格式 / 备注 |
|-------|----------|-------------------|----------|-------------|
| **Phase 1** | 股票历史 OHLCV | yfinance、investpy | 小规模 pipeline、特征、均线回测 | Parquet，日线 |
| **Phase 1** | Synthetic 数据 | 自建 Pandas DataFrame | 本地单元测试 / pytest | Parquet |
| **Phase 2** | 多标的历史 OHLCV | yfinance / Alpha Vantage | ML 训练、MLSignalStrategy、回测 | Parquet，注意缓存 |
| **Phase 2** | 股票财务指标 | yfinance / financialmodelingprep | ML 因子、特征增强 | JSON / Parquet |
| **Phase 2** | ETF / 指数 | yfinance / investpy | 组合构建、风险评估 | Parquet |
| **Phase 2** | 简单新闻 / 公告 | RSS / Finviz / Yahoo Finance news | 文本因子测试 | TXT / JSON |
| **Phase 3** | 多年历史 OHLCV（大规模） | Quandl / CSMAR / Bloomberg / Wind | 大规模 ML 训练、组合优化 | Parquet / HDF5 |
| **Phase 3** | 财报原文 | EDGAR / Wind / CSMAR | TextSignalAgent、财务因子 | TXT / PDF / JSON |
| **Phase 3** | 研报、新闻、公告 | FactSet / Bloomberg / Wind / News API | 文本因子、RAG 查询 | PDF / TXT / JSON |
| **Phase 4** | 高频 / Tick | IBKR / Bloomberg / Wind / Quandl | 事件驱动、高级策略 | Parquet / HDF5 |
| **Phase 5** | 多市场 / 跨品种 | Bloomberg / Wind / Quandl | 多市场 MAS 仿真 | CSV / Parquet |
| **Phase 5** | 宏观指标 | FRED / OECD / IMF / Wind | 因子研究 | CSV / JSON |
| **Phase 6** | Paper / 实盘 | Broker API（IBKR、Alpaca 等） | 模拟或实盘验证 | JSON / REST / Database |

### 18.3 各 Phase 数据优先顺序

| Phase | 优先使用的数据 |
|-------|----------------|
| Phase 1 | Synthetic + yfinance 小数据（本地） |
| Phase 2 | yfinance 多标的 + 简单财报指标（本地小数据 → 服务器真实数据） |
| Phase 3 | 手动下载历史 OHLCV + 财报原文（服务器） |
| Phase 4 | 高频 / Tick 数据（服务器） |
| Phase 5 | 多市场 / 跨品种 + 宏观指标（服务器） |
| Phase 6 | Broker 实盘 / Paper Trading 数据 |

### 18.4 服务器数据目录规范

```
/home/<USER>/quant-mas/
├── datasets/
│   ├── raw/              # 自动下载或手动放置的原始 OHLCV
│   │   └── manual/       # 手动 / 付费数据
│   ├── processed/
│   └── features/         # 特征表
├── models/               # 训练产物
├── reports/              # 回测报告（或 outputs/reports/）
└── logs/                 # 实验日志
```

实验记录：`docs/experiment_log.md`（代码通过 GitHub 同步；实验 metrics / 报告留在服务器）

### 18.5 项目数据策略总结

```
Phase 1–2（现在）
  ├── 本地：synthetic 数据 + pytest（不联网）
  ├── 本地/服务器：yfinance 自动下载少量标的验证 pipeline
  └── 服务器：多标的、多年数据的真实训练与回测

Phase 3–6（后续）
  ├── 手动 / 付费数据 → datasets/raw/manual/
  ├── 高频、跨市场、宏观数据 → 服务器
  └── RAG 大量文档 / 向量库 → 视规模决定是否迁服务器
```

> **不需要一开始就手动找数据。** Phase 1–2 用项目自带的 `download_data.py` + yfinance 自动下载即可；等你进入 Phase 3 再做高质量数据对接。

---

## 附录 A. Codex 分阶段 Prompt 详情

### Prompt 1：初始化项目骨架

创建完整目录结构、`pyproject.toml`、`requirements.txt`、`.env.example`、`.gitignore`、基础 configs 和 docs、`tests/test_import.py`。

验收：`pip install -e .` 成功；`pytest` 通过；`import quant_mas` 不报错。

---

### Prompt 2：数据存储与目录管理

实现 `ParquetStorage`（save/load/exists）、`DataCatalog`（读取 `configs/storage.yaml`、自动创建目录、跨平台路径）、`tests/test_storage.py`。

---

### Prompt 3：数据下载和校验

实现 `MarketDataFetcher` 抽象类、`YFinanceFetcher`、`validate_ohlcv()`、`scripts/download_data.py` CLI、`tests/test_data_validation.py`（synthetic data，不依赖网络）。

输出字段：`date, symbol, open, high, low, close, volume`。

---

### Prompt 4：特征工程

实现 `technical.py`（returns、MA、MA distance、volatility、volume、RSI）、`labels.py`（future return/direction label）、`pipelines.py`（`build_feature_table`，按 symbol 分组）、`scripts/build_features.py`。

要求：严禁未来函数；rolling 按时间顺序；多 symbol 不互相污染。

---

### Prompt 5：均线策略和回测

实现 `Strategy` 抽象类、`MovingAverageCrossStrategy`、`CommissionModel`/`SlippageModel`、绩效指标（total_return、sharpe、max_drawdown 等）、`BacktestEngine`（下一根 bar 成交，避免同根信号同根成交）、`scripts/run_backtest.py`。

---

### Prompt 6：实验记忆和报告

实现 `ExperimentMemory`（JSON 存储实验记录）、`save_backtest_report()`（metrics.json、equity_curve.csv、trades.csv、summary.md）、`scripts/generate_report.py`。

---

### Prompt 7：模型训练

实现 `BasePredictiveModel`、`LightGBMDirectionModel`、`scripts/train_model.py`（时间序列切分 70/15/15，禁止随机切分，禁止 label 进入特征）、结果写入 ExperimentMemory。

---

### Prompt 8：轻量 Agent Core

实现 `Message`、`LLMClient`（MockLLMClient）、`BaseAgent`、`BaseTool`/`ToolRegistry`、`ReportAgent`、`tests/test_agent_core.py`。不使用 LangGraph/AgentScope；所有循环有 max_steps 限制。

---

### Prompt 9：量化 Tools

封装 `DataSummaryTool`、`BacktestTool`、`TrainModelTool`、`ReportTool`，均继承 `BaseTool`，返回 `ToolResult`（摘要和路径，不返回完整 DataFrame）。

---

### Prompt 10：SupervisorAgent 和内部任务流

实现规则路由（关键词 → 对应 Tool）、`AgentEvent`/`ToolCallEvent`/`AgentFinishEvent`、`scripts/run_agent.py`。不使用 LangGraph；不调用真实 LLM。

---

## 附录 B. 下一阶段 Codex Prompt 详情（Prompt 11–20）

> 每次只交给 Codex 一个 Prompt，完成后再进行下一个。

### Prompt 11：新增统一 Pipeline 脚本

```
你正在开发 Quant MAS 项目。当前项目已经完成数据层、特征层、均线策略、回测、实验记忆、报告、模型训练、轻量 Agent Core、量化 Tools 和 SupervisorAgent。

现在请实现一个统一端到端 pipeline 脚本：

文件：
- scripts/run_pipeline.py

目标：
用一条命令串联以下流程：
1. 可选：下载数据
2. 构建特征
3. 运行回测
4. 保存报告
5. 写入 ExperimentMemory
6. 打印本次实验摘要

CLI 参数要求：
- --symbols AAPL MSFT SPY
- --start 2018-01-01
- --end 2025-12-31
- --raw-dir
- --features-dir
- --output-dir
- --storage-config configs/storage.yaml
- --features-config configs/features.yaml
- --backtest-config configs/backtest.yaml
- --skip-download
- --skip-features
- --strategy ma_cross
- --experiment-name

设计要求：
1. 不要把核心业务逻辑写死在 CLI 中，应尽量复用已有模块。
2. 所有路径使用 pathlib.Path。
3. 支持 Windows 和 Linux。
4. 默认不强制联网；如果传入 --skip-download，就只使用本地已有 parquet。
5. pipeline 每一步都要有清晰日志输出。
6. 失败时返回非零退出码，并打印清晰错误。
7. 不要生成大数据文件进入 git。
8. 不调用真实 LLM API。

验收标准：
1. python scripts/run_pipeline.py --help 可以正常显示。
2. 使用本地 synthetic/sample parquet 时，可以完成 build_features → backtest → report。
3. outputs/reports/ 下生成 metrics.json、equity_curve.csv、trades.csv、summary.md。
4. ExperimentMemory 中能记录本次实验。
5. pytest 仍然全部通过。
```

---

### Prompt 12：增加端到端 Pipeline 测试

```
请为 Quant MAS 项目新增一个最小端到端 pipeline 测试。

文件：
- tests/test_end_to_end_pipeline.py

测试目标：
在不依赖真实网络、不调用真实 LLM、不需要真实 LightGBM 的情况下，跑通最小量化闭环：

1. 生成 synthetic OHLCV 数据
2. 保存为 parquet
3. 构建特征
4. 运行 MovingAverageCrossStrategy
5. 运行 BacktestEngine
6. 保存 backtest report
7. 写入 ExperimentMemory
8. 确认报告文件存在
9. 确认 metrics 中包含 total_return、sharpe、max_drawdown、final_equity

要求：
1. 使用 pytest tmp_path，不写入真实 data/ 或 outputs/ 目录。
2. synthetic 数据必须稳定可复现。
3. 不允许访问网络。
4. 不允许调用真实 LLM API。
5. 测试应尽量复用已有模块，而不是复制业务逻辑。
6. 测试运行时间要短。
7. 如果 run_pipeline.py 支持测试模式，可以直接调用 pipeline 内部函数；如果不支持，请为 pipeline 抽出可测试函数。

验收标准：
1. pytest tests/test_end_to_end_pipeline.py 通过。
2. pytest 全量测试通过。
3. 该测试能证明当前项目已经具备最小量化闭环。
```

---

### Prompt 13：更新项目文档

```
请根据当前 Quant MAS 项目的实际代码状态，更新项目文档。

需要更新：
1. docs/progress.md
2. docs/architecture.md
3. docs/experiment_log.md

docs/progress.md：
- 写明当前已经完成 Prompt 1–10。
- 写明当前 pytest 状态。
- 写明当前可用 CLI。
- 写明下一阶段目标：Phase 1 收口、Phase 2 机器学习真实实验、Phase 3 Agent 增强。
- 用 checklist 表示任务状态。

docs/architecture.md：
- 画出当前架构：Quant Engine Layer / Tool Layer / Agent Layer / Memory Layer
- 明确 LLM Agent 不允许直接实盘下单；Agent 只负责编排、报告、解释和工具调用
- 描述当前已经实现的模块和后续计划

docs/experiment_log.md：
- 增加模板：实验编号、日期、数据、策略/模型、参数、指标、产物路径、问题、下一步

要求：
1. 不要虚构不存在的实验结果。
2. 如果某个功能尚未真实跑过，请标记为"待验证"。
3. 保持文档简洁清晰，适合科研项目记录。
```

---

### Prompt 14：服务器部署脚本

```
请为 Quant MAS 项目新增服务器部署辅助文件。

需要新增：
1. server/setup_server.sh
2. server/run_server_tests.sh
3. server/run_small_pipeline.sh
4. configs/storage.server.yaml.example

server/setup_server.sh 要求：
- 检查 conda 是否存在。
- 创建或提示创建 quant-mas 环境，Python 3.11。
- 安装 requirements.txt。
- pip install -e .
- 打印 Python 版本、pip 版本。
- 不自动下载大数据。
- 不写死用户名路径。

server/run_server_tests.sh 要求：
- 激活环境的提示写在注释里。
- 运行 pytest。
- 输出日志到 logs/server_pytest.log。

server/run_small_pipeline.sh 要求：
- 运行一个小规模 pipeline 示例。
- 默认使用 AAPL、MSFT、SPY。
- start/end 参数可配置。
- 输出到服务器 reports/logs 目录。

configs/storage.server.yaml.example 要求：
- 使用占位符，例如 /home/<USER>/quant-mas。
- 包含 raw_data_dir、processed_data_dir、features_dir、models_dir、reports_dir、logs_dir。
- 不包含真实隐私路径。

要求：
1. shell 脚本尽量使用 set -euo pipefail。
2. 不能提交真实数据路径、API key、个人密码。
3. README 或 docs 中简单说明服务器运行方式。
```

---

### Prompt 15：完善真实 ML 训练输出

```
请完善 Quant MAS 的机器学习训练模块，使其适合真实服务器实验。

涉及文件：
- scripts/train_model.py
- src/quant_mas/models/lightgbm_model.py
- src/quant_mas/models/base.py
- src/quant_mas/memory/experiment_memory.py
- configs/train.yaml
- tests/test_train_model.py

目标：训练完成后输出完整实验产物：

1. metrics.json（train/val/test accuracy、auc、时间范围、样本数、label_column、feature_count）
2. feature_importance.csv（feature、importance）
3. model 文件（model.pkl 或 model.joblib）
4. experiment record（config、metrics、artifacts、created_at）

设计要求：
1. 禁止随机切分，必须按时间顺序切分。
2. 禁止 label 列、date、symbol、future_return_*、future_direction_* 进入特征。
3. 如果缺少 lightgbm，要给出清晰错误提示，不要让测试失败。
4. 测试中使用 synthetic feature data。
5. 输出路径必须可配置。
6. 所有路径使用 pathlib.Path。

验收标准：
1. pytest tests/test_train_model.py 通过。
2. python scripts/train_model.py --help 正常。
3. 使用 synthetic data 可以生成 metrics.json、feature_importance.csv、model 文件。
```

---

### Prompt 16：实现 MLSignalStrategy

```
请为 Quant MAS 实现机器学习信号策略 MLSignalStrategy，并接入回测流程。

需要实现：
1. src/quant_mas/strategies/ml_signal.py
   - MLSignalStrategy（buy_threshold、sell_threshold、max_weight）
   - 输出：date, symbol, signal, target_weight

2. scripts/run_ml_backtest.py
   - 读取特征数据 → 加载模型 → 生成 pred_proba → MLSignalStrategy → BacktestEngine → 保存报告

3. configs/backtest_ml.yaml
4. tests/test_ml_signal_strategy.py

要求：
1. 不能在回测中使用未来 label。
2. pred_proba 必须来自模型对特征的预测。
3. 回测必须仍然遵守下一根 bar 成交规则。
4. 测试不依赖真实 LightGBM，可以用 mock model。
5. 不调用真实网络或真实 LLM。

验收标准：
1. pytest tests/test_ml_signal_strategy.py 通过。
2. python scripts/run_ml_backtest.py --help 正常。
3. 使用 synthetic data + mock/simple model 可以跑通 ML 信号回测。
```

---

### Prompt 17：实现样本外 / Walk-forward 报告

```
请为 Quant MAS 增加样本外评估和 walk-forward 报告能力。

需要实现：
1. src/quant_mas/backtest/walk_forward.py
   - WalkForwardSplit、generate_splits(df, train_window, test_window, step)

2. src/quant_mas/backtest/report.py 增强
   - 支持 train_metrics / val_metrics / test_metrics / oos_metrics
   - summary.md 中显示样本内/样本外区别

3. scripts/run_walk_forward.py
4. configs/walk_forward.yaml
5. tests/test_walk_forward.py

要求：
1. 严格避免未来数据泄露。
2. 不使用随机切分。
3. 测试不依赖真实金融数据。
4. 第一版可以只实现 split 和报告汇总，不必训练复杂模型。

验收标准：
1. pytest tests/test_walk_forward.py 通过。
2. python scripts/run_walk_forward.py --help 正常。
```

---

### Prompt 18：实现基础风控层

```
请为 Quant MAS 实现第一版基础风控层。

需要实现：
1. src/quant_mas/risk/limits.py — RiskLimits dataclass
2. src/quant_mas/risk/decision.py — RiskDecision dataclass
3. src/quant_mas/risk/exposure.py — calculate_total_exposure、check_position_limits
4. src/quant_mas/risk/drawdown_guard.py — check_drawdown
5. src/quant_mas/tools/quant/risk_tool.py — RiskTool
6. configs/risk.yaml
7. tests/test_risk.py

设计要求：
1. 风控层独立于策略层。
2. 策略只生成 target_weight，风控有权裁剪或拒绝。
3. 第一版不接真实 broker。
4. 不调用真实 LLM。
5. 所有输出必须可审计。

验收标准：
1. pytest tests/test_risk.py 通过。
2. RiskTool 可以注册进 ToolRegistry。
```

---

### Prompt 19：增强 SupervisorAgent 路由

```
请增强 Quant MAS 的 SupervisorAgent 路由能力。

当前已支持：backtest / train / report / data

新增支持：
1. ml_backtest / 机器学习回测 / ML回测 → MLBacktestTool 或 BacktestTool ML 模式
2. risk / 风控 / 风险检查 → RiskTool
3. pipeline / 全流程 / 端到端 → PipelineTool

需要修改：
- src/quant_mas/agents/supervisor_agent.py
- src/quant_mas/tools/registry.py 或相关注册入口
- tests/test_supervisor_agent.py

要求：
1. 继续使用规则路由，不引入 LangGraph。
2. 不调用真实 LLM。
3. 所有工具调用记录 ToolCallEvent。
4. 未知任务要返回清晰提示。
5. 保持原有测试通过。

验收标准：
1. pytest tests/test_supervisor_agent.py 通过。
2. SupervisorAgent 能根据中文和英文关键词路由到对应工具。
```

---

### Prompt 20：Memory / RAG 雏形

```
请为 Quant MAS 实现 Memory / RAG 的最小雏形，为后续 ResearchAgent 做准备。

需要实现：
1. src/quant_mas/memory/experiment_memory.py 增强
   - 按 experiment_id 查询、按 name keyword 搜索、按 metric 排序、列出 artifact 路径

2. src/quant_mas/memory/trade_memory.py — TradeMemory 空壳（JSONL）

3. src/quant_mas/rag/document_loader.py — 加载 md/txt/json，返回 Document 对象

4. src/quant_mas/rag/simple_retriever.py — 关键词检索（不用向量库），从 docs/ 和 outputs/reports/ 检索

5. tests/test_memory_rag.py

要求：
1. 不调用真实 LLM。
2. 不引入复杂向量数据库。
3. 不读取大文件。
4. 路径使用 pathlib.Path。
5. 为后续 ResearchAgent / ReportAgent 提供接口。

验收标准：
1. pytest tests/test_memory_rag.py 通过。
2. 可以从 docs/ 中检索包含关键词的研究笔记。
```

---

*文档版本：2026-06-01（更新：本地/服务器分工、数据获取策略）*

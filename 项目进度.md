# Quant MAS 项目进度

更新时间：2026-06-02

## 项目定位

Quant MAS 是一个基于多智能体系统 MAS 的量化研究与交易智能体平台。

当前阶段重点不是让 LLM 自动交易，而是先建立一个可运行、可测试、可回测、可训练、可记录实验的最小量化闭环。

核心原则：

- Quant Engine 负责确定性计算：数据、特征、模型、策略、回测、风控、执行。
- Agent Layer 负责研究、规划、解释、报告和工具编排。
- LLM Agent 不允许直接实盘下单。
- 所有交易信号必须经过回测、风控、审计和人工确认。
- 第一阶段不引入 LangGraph / AgentScope，先使用自建轻量 Agent Core。
- 所有测试不依赖真实网络请求或真实 LLM API。

## 当前架构

当前项目已经形成以下基础结构：

```text
Quant MAS/
├── configs/                  # YAML 配置
├── docs/                     # 架构、实验、会议、研究记录
├── scripts/                  # CLI 脚本入口
├── src/quant_mas/
│   ├── core/                 # Message、LLMClient、Agent、事件
│   ├── agents/               # ReportAgent、SupervisorAgent
│   ├── tools/                # BaseTool、ToolRegistry、量化工具
│   ├── data/                 # ParquetStorage、DataCatalog、Fetcher、校验
│   ├── features/             # 技术指标、标签、特征流水线
│   ├── strategies/           # Strategy、MovingAverageCrossStrategy
│   ├── backtest/             # 成本模型、指标、回测引擎、报告保存
│   ├── models/               # BasePredictiveModel、LightGBM、训练工具
│   └── memory/               # ExperimentMemory
└── tests/                    # 离线测试
```

## 已完成模块

### Prompt 1：项目骨架

已完成：

- `src/quant_mas` 主包结构
- `pyproject.toml`
- `requirements.txt`
- `.env.example`
- `.gitignore`
- 基础 configs
- 基础 docs
- `tests/test_import.py`

验收：

- `pip install -e .` 已验证通过
- `import quant_mas` 已验证通过
- pytest 已通过

### Prompt 2：数据存储与目录管理

已完成：

- `ParquetStorage`
  - `save`
  - `load`
  - `exists`
- `DataCatalog`
  - 读取 `configs/storage.yaml`
  - 自动创建目录
  - 使用 `pathlib` 支持 Windows / Linux 路径
- `tests/test_storage.py`

### Prompt 3：数据下载与校验

已完成：

- `MarketDataFetcher` 抽象类
- `YFinanceFetcher`
- `validate_ohlcv()`
- `scripts/download_data.py`
- `tests/test_data_validation.py`

标准输出字段：

```text
date, symbol, open, high, low, close, volume
```

说明：

- 测试只使用 synthetic data。
- 不依赖真实网络请求。
- `yfinance` 延迟导入，只有真实下载时才需要。

### Prompt 4：特征工程

已完成：

- `technical.py`
  - returns
  - moving average
  - MA distance
  - volatility
  - volume features
  - RSI
- `labels.py`
  - future return label
  - future direction label
- `pipelines.py`
  - `build_feature_table`
  - 按 `symbol` 分组
  - 每个 symbol 内部按日期排序
- `scripts/build_features.py`
- `tests/test_features.py`

关键约束：

- rolling 特征只使用历史数据。
- 多 symbol 不互相污染。
- 只有 label 允许使用未来收益。

### Prompt 5：策略与回测

已完成：

- `Strategy` 抽象类
- `MovingAverageCrossStrategy`
- `CommissionModel`
- `SlippageModel`
- 绩效指标
  - total return
  - annualized return
  - sharpe
  - max drawdown
  - final equity
- `BacktestEngine`
- `scripts/run_backtest.py`
- `tests/test_backtest.py`

关键约束：

- 策略信号下一根 bar 成交。
- 避免同根信号同根成交。
- 当前为轻量回测引擎，适合 Phase 1 MVP。

### Prompt 6：实验记忆和报告

已完成：

- `ExperimentMemory`
  - JSON 存储实验记录
  - `add`
  - `list`
  - `latest`
- `save_backtest_report()`
  - `metrics.json`
  - `equity_curve.csv`
  - `trades.csv`
  - `summary.md`
- `scripts/generate_report.py`
- `tests/test_experiment_memory.py`

说明：

- 实验记录只存轻量元数据和路径。
- 不存大数据文件。

### Prompt 7：模型训练

已完成：

- `BasePredictiveModel`
- `LightGBMDirectionModel`
- 时间序列训练工具
  - `prepare_supervised_data`
  - `split_by_time`
  - `evaluate_direction_model`
- `scripts/train_model.py`
- `tests/test_train_model.py`

关键约束：

- 使用时间序列切分 `70/15/15`。
- 禁止随机切分。
- 自动排除 label 和标识列：
  - `date`
  - `symbol`
  - `future_return_*`
  - `future_direction_*`
- 训练结果写入 `ExperimentMemory`。

说明：

- LightGBM 延迟导入。
- 测试不强制安装 LightGBM。

### Prompt 8：轻量 Agent Core

已完成：

- `Message`
- `LLMClient`
- `MockLLMClient`
- `BaseAgent`
- `BaseTool`
- `ToolResult`
- `ToolRegistry`
- `ReportAgent`
- `tests/test_agent_core.py`

关键约束：

- 不调用真实 LLM API。
- 所有 Agent 循环都有 `max_steps`。
- 未引入 LangGraph / AgentScope。

### Prompt 9：量化 Tools

已完成：

- `DataSummaryTool`
- `BacktestTool`
- `TrainModelTool`
- `ReportTool`
- `tests/test_quant_tools.py`

工具返回内容：

- 摘要文本
- 指标
- 文件路径

工具不返回完整 DataFrame。

### Prompt 10：SupervisorAgent 和内部任务流

已完成：

- `AgentEvent`
- `ToolCallEvent`
- `AgentFinishEvent`
- `SupervisorAgent`
- `scripts/run_agent.py`
- `tests/test_supervisor_agent.py`

当前路由规则：

| 关键词 | 工具 |
|---|---|
| `backtest` / `回测` / `策略测试` | `BacktestTool` |
| `train` / `model` / `训练` / `模型` | `TrainModelTool` |
| `report` / `summary` / `报告` / `摘要` | `ReportTool` |
| `data` / `dataset` / `数据` / `概览` | `DataSummaryTool` |

说明：

- 当前 Supervisor 是规则路由，不调用真实 LLM。
- 不使用 LangGraph。
- 支持事件记录，便于后续审计与报告。

## 当前测试状态

| 环境 | Python | 结果 | 日期 |
|------|--------|------|------|
| 本地 Windows | 3.11+ | **44 passed** | 2026-06-02 |
| 服务器 Linux（a6000-9961） | 3.11.15 | **44 passed**（1.19s） | 2026-06-02 |

服务器验证命令：

```bash
conda activate /mnt/localDisk3/weizian/conda_envs/quant-mas
cd /mnt/localDisk3/weizian/Quant-MAS
python -m pytest -v
```

测试覆盖范围：

- 包导入
- Parquet 存储
- 数据目录管理
- OHLCV 数据校验
- 特征工程
- 策略信号
- 回测引擎
- 实验记忆
- 报告保存
- 模型训练切分和防 label 泄露
- Agent Core
- 量化 Tools
- SupervisorAgent 路由与事件
- 端到端 pipeline（`test_end_to_end_pipeline.py`）

## 服务器部署状态

已验证（2026-06-02）：

| 项目 | 值 |
|------|-----|
| 项目路径 | `/mnt/localDisk3/weizian/Quant-MAS` |
| Conda 环境 | `/mnt/localDisk3/weizian/conda_envs/quant-mas` |
| Python | 3.11.15 |
| GitHub | https://github.com/ytq0198/Quant-MAS |
| pytest | **44 passed** |

部署要点：

- 环境必须用 `conda create -p ... python=3.11` 创建。
- 安装依赖用 `python -m pip`，不用 bare `pip`。
- 测试用 `python -m pytest -v`，不用 bare `pytest`。
- 核心依赖：`requirements.txt` + `pip install -e .`
- 可选依赖：`requirements-data.txt`（yfinance）、`requirements-ml.txt`（lightgbm）

待完成：

- [ ] 安装并验证 `requirements-data.txt`
- [ ] 安装并验证 `requirements-ml.txt`
- [ ] 真实行情 pipeline 实验

## 当前可用 CLI

```powershell
python scripts/download_data.py --help
python scripts/build_features.py --help
python scripts/run_backtest.py --help
python scripts/train_model.py --help
python scripts/generate_report.py --help
python scripts/run_agent.py --help
python scripts/run_pipeline.py --help
```

## 当前最小闭环

当前代码已经具备第一阶段最小闭环的主体能力：

```text
数据读取 / 下载接口
  -> OHLCV 校验
  -> Parquet 存储
  -> 特征构建
  -> 均线策略
  -> 回测
  -> 指标输出
  -> 实验记录
  -> 报告生成
  -> Agent 工具编排
```

## 重要约束与注意事项

- 不要把大数据文件加入 git。
- `data/`、`outputs/`、`models/`、`logs/` 已在 `.gitignore` 中忽略（根目录产物；不忽略 `src/quant_mas/data/`、`src/quant_mas/models/` 源码包）。
- 测试不能依赖真实网络。
- 测试不能依赖真实 LLM API。
- 当前 Agent 只负责工具编排和报告，不直接交易。
- 实盘相关能力必须后续经过风控、审计和人工确认。

## 下一步建议

Phase 1 收口（进行中）：

1. ~~服务器 Python 3.11 环境 + 全量 pytest~~ ✅ 已完成（44 passed）
2. 安装 `requirements-data.txt`，用 yfinance 下载 AAPL/MSFT/SPY 真实数据。
3. 安装 `requirements-ml.txt`，跑 LightGBM 真实训练实验。
4. 在服务器执行 `run_pipeline.py` 真实数据 pipeline。
5. 增加基础风险检查模块（Prompt 18）。

后续 Phase 2–3：

1. LightGBM 真实训练实验。
2. 样本外回测。
3. 特征重要性输出。
4. 更严格的风险检查模块。
5. 更完整的实验报告。


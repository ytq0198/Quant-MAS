# Quant MAS 项目进度

更新时间：2026-06-04（**v3 M12.4 ✅** 双端闭环 · EXP-POP-010 OOS **0.387**）

**Plus v2**：M1–M8 ✅ · **v3 M9–M12.4** ✅ 双端

核心原则：

- Quant Engine 负责确定性计算：数据、特征、模型、策略、回测、风控、执行。
- Agent Layer 负责研究、规划、解释、报告和工具编排。
- LLM Agent 不允许直接实盘下单。
- 所有交易信号必须经过回测、风控、审计和人工确认。
- v1 阶段使用自建轻量 Agent Core；LangGraph 见 Plus M4。
- 所有 pytest 不依赖真实网络请求或真实 LLM API（M2 新增 fetcher 用 mock HTTP 测）。
- 论文/报告主指标：**Walk-forward OOS sharpe 0.586**（EXP-20260602-008）；新实验须经 M1 `compare_experiments.py` 对比。

---

## 阶段总览

> 与 `项目指导.md` §9 一致。括号内为历史 Phase 编号，便于对照旧文档与 commit。

| 阶段 | 名称 | 状态 | 关键 Prompt / 实验 | 下一步 |
|------|------|------|-------------------|--------|
| **第零阶段** | 项目骨架 | ✅ 完成 | Prompt 1 | — |
| **第一阶段** | 量化核心 MVP | ✅ 完成 | Prompt 2–6、11–12、14 | — |
| **第二阶段** | 机器学习实验 | ✅ 完成 | Prompt 15–17、15b；EXP-20260602-008 | — |
| **第二阶段扩展** | 基础风控 | ✅ 完成 | Prompt 18；EXP-20260601-009 | — |
| **第三阶段** | Agent 增强 | ✅ 完成 | Prompt 8–10、19；EXP-20260601-011 | — |
| **第四阶段** | Memory + RAG | ✅ 完成 | Prompt 20；EXP-20260601-013/014 | — |
| **文档** | Prompt 13 收口 | ✅ 完成 | EXP-20260601-015 | — |
| **Plus M1** | 研究基线 | ✅ 完成 | EXP-20260602-009/010；比较表 5 rows | — |
| **Plus M2** | 数据扩展 | ✅ 完成 | EXP-20260602-011/012，EXP-DATA-001 | — |
| **Plus M3** | Memory/RAG v2 | ✅ 完成 | EXP-20260602-013/014，**126 passed** | — |
| **Plus M4** | LangGraph 编排 | ✅ 完成 | EXP-20260602-015/016 | — |
| **Plus M5** | 上下文/LLM | ✅ 完成 | EXP-20260602-017/018，EXP-LLM-001，**150 passed** | — |
| **Plus M6** | 文本大模型 | ✅ 完成 | EXP-019/020 + **EXP-TEXT-001/WF-001** | — |
| **Plus M7** | RL 模拟 | ✅ | EXP-021/022，**180 passed** | — |
| **Plus M8** | MCP/A2A | ✅ | EXP-023/024，**195 passed** | — |
| **v3 M9** | 企业数据与数据库 | ✅ | EXP-025/026/028，Postgres/pgvector | — |
| **v3 M10** | LLM 生产化 | ✅ | EXP-027/028/LLM-002，local_vllm smoke | — |
| **v3 M11** | 竞争学习 / 策略种群 | ✅ | EXP-029/POP-002，225 双端 | — |
| **v3 M11.5** | 种群训练闭环 | ✅ | EXP-030/POP-003，**237 双端** | M11.6 |
| **v3 M11.6** | 候选验证桥 | ✅ | EXP-031/POP-004，**248 双端** | M11.7 |
| **v3 M11.7** | 候选 Walk-forward OOS | ✅ | EXP-032/POP-005，`oos.sharpe` **1.036** vs **0.586** | M11.8 |
| **v3 M11.8** | 批量候选 OOS 比较 | ✅ | EXP-033/POP-006，**266 双端**；best **1.039** | M12 |
| **v3 M12.1** | RL 训练 loop（simulation） | ✅ | EXP-034/POP-007/RL-003，**282 双端** | M12.2 |
| **v3 M12.2** | RL → StrategyCandidate 导出 | ✅ 双端 | EXP-035 / EXP-POP-008 | M12.3 |
| **v3 M12.3** | RL 候选 OOS 适配 | ✅ 双端 | EXP-POP-009 | M12.4 |
| **v3 M12.4** | Observation-aware RL | ✅ 双端 | EXP-036 / **EXP-POP-010**；OOS **0.387** | EXP-TEXT-WF-002 |
| **第六阶段** | 协议扩展 | ✅ Plus M8 | EXP-023/024 | 不做实盘 |

**测试基线**：**308 passed** 本地（EXP-036）；**296 passed** 服务器（M12.3 @ `6e8c507`）

---

## 论文可用表述（M12 RL 全链路）

M12 的结果应作为 **RL ablation / sanity check**，不作为主策略结果。建议论文中这样表述：

> We implemented a simulation-only RL training pipeline and exported the trained policy into the same `StrategyCandidate` schema used by the population-learning candidates. The exported RL candidate was then evaluated by the identical walk-forward OOS validation hook. The resulting OOS Sharpe was 0.0 because the learned logits selected the all-cash action, demonstrating that simulation metrics cannot be treated as OOS performance.

中文表述：

> 本项目实现了一个仿真层强化学习训练闭环，并将训练后的策略导出为统一的 `StrategyCandidate` 格式，再通过与竞争学习候选相同的 walk-forward OOS 验证钩子进行样本外检验。该 RL 候选的 OOS Sharpe 为 0.0，原因是当前短训练策略的最大 logit 对应 0% 仓位，形成全现金策略。这一结果说明：训练/仿真指标不能直接等价于样本外表现，所有候选策略必须经过独立 OOS 验证。

论文中可用的对比结论：

| 链路 | 指标 | 结果 | 论文含义 |
|------|------|------|----------|
| ML walk-forward baseline | `oos.sharpe` | **0.586** | 主结果 / main baseline |
| Population candidate OOS | `oos.sharpe` | **1.036–1.039** | 规则型候选 ablation，说明竞争学习可产生有价值候选 |
| RL training smoke | `simulation.sharpe_mean` | **6.31** | 仅为短窗仿真，不代表 OOS |
| RL exported candidate OOS | `oos.sharpe` | **0.0** | sanity check；当前 RL policy 为全现金，验证管道诚实 |

禁止写法：

> RL Sharpe 6.31 超过 OOS baseline 0.586。

推荐写法：

> RL smoke achieved high simulation metrics but failed to produce market exposure after OOS validation, which highlights the necessity of separating simulation-based policy search from walk-forward OOS evaluation.

---

## 第零阶段：项目骨架 ✅

**Prompt 1** — 项目可安装、`import quant_mas`、基础 configs / docs / tests。

---

## 第一阶段：量化核心 MVP ✅

### 数据与特征（Prompt 2–4）

- `ParquetStorage`、`DataCatalog`、`validate_ohlcv`
- `YFinanceFetcher`（备用）、`StooqFetcher` + `STOOQ_API_KEY`（服务器已验证）
- `download_data.py`、`server/download_data_resilient.sh`
- 技术指标、future label、`build_feature_table`（按 symbol 分组）

### 策略与回测（Prompt 5–6）

- `MovingAverageCrossStrategy`、`BacktestEngine`（下一根 bar 成交）
- 成本模型、绩效指标、`ExperimentMemory`、`save_backtest_report`

### 模型训练框架（Prompt 7，Prompt 15 增强前）

- `BasePredictiveModel`、`LightGBMDirectionModel` 框架
- 时间序列 70/15/15 切分、label 泄露防护

### Pipeline 收口（Prompt 11–12）

- `scripts/run_pipeline.py`
- `tests/test_end_to_end_pipeline.py`

### 服务器验证（第一阶段）

| 实验 | 结果 |
|------|------|
| EXP-20260602-003 | pytest 44 → 71 passed |
| EXP-20260601-004 | Stooq 6033 rows；`server_ma_cross_real_001` sharpe ≈ 1.00 |

---

## 第二阶段：机器学习实验 ✅

### Prompt 15 — ML 训练产物

- `metrics.json`、`feature_importance.csv`、`model.pkl`、ExperimentMemory
- 服务器 CPU：`server_lgbm_001`（EXP-20260601-006，test AUC 0.466）

### Prompt 15b — GPU/CUDA 训练

- `device.py`、`--device`；服务器 `server_lgbm_gpu_001`（EXP-20260602-004，device=cuda）

### Prompt 16 — ML 信号回测

- `MLSignalStrategy`、`run_ml_backtest.py`
- 服务器 `server_ml_backtest_001`（EXP-20260602-005，单段 sharpe 2.78）

### Prompt 17 — Walk-forward 样本外

- `walk_forward.py`、`run_walk_forward.py`、`save_walk_forward_report`
- 本地 EXP-20260602-007（71 passed）
- 服务器 EXP-20260602-008：**OOS sharpe 0.586**，total_return 0.443，19 窗，~17s

**研究结论**：单段 ML 回测 sharpe 2.78 **不能代表样本外**；报告以 walk-forward OOS 为准。

---

## 第二阶段扩展：基础风控 ✅

### Prompt 18 — RiskLimits / RiskTool

- `RiskLimits`、`RiskDecision`（可审计 dataclass）
- `calculate_total_exposure`、`check_position_limits`（裁剪 / 拒绝）
- `check_drawdown`（回撤守卫）
- `RiskTool`（`risk_check`，可注册 ToolRegistry）
- `configs/risk.yaml`（含 `max_total_exposure`、`allow_short`）
- `tests/test_risk.py`：**5 passed**；全量 **76 passed**（EXP-20260601-009）

**设计要点**：策略只产出 `target_weight`；风控独立裁剪或拒绝；不接 broker、不调 LLM。

---

## 第三阶段：Agent 增强 ✅

**Prompt 8–10（基础）**

- `Message`、`MockLLMClient`、`BaseAgent`、`ToolRegistry`
- `DataSummaryTool`、`BacktestTool`、`TrainModelTool`、`ReportTool`
- `SupervisorAgent` 规则路由、`run_agent.py`

**Prompt 19 — Supervisor 路由扩展**

- `MLBacktestTool`（`ml_backtest`）→ 封装 `run_ml_backtest`
- `PipelineTool`（`pipeline`）→ 封装 `run_quant_pipeline`，默认 `skip_download=True`
- `RiskTool`（`risk_check`）已接入路由
- `run_agent.py` 注册 **7 个工具**；扩展 CLI 参数
- `test_supervisor_agent.py` **17 passed**；全量 **87 passed**（EXP-20260601-011）

| 关键词 | 工具 |
|--------|------|
| ML回测 / ml backtest | MLBacktestTool |
| 风控 / risk | RiskTool |
| 全流程 / pipeline | PipelineTool |
| backtest / 回测 | BacktestTool |
| train / 训练 | TrainModelTool |
| report / 报告 | ReportTool |
| data / 数据 | DataSummaryTool |

---

## 第四阶段：Memory + RAG ✅

### Prompt 20 — ExperimentMemory 增强 + SimpleRetriever

- `get` / `search_by_name` / `sort_by_metric` / `list_artifact_paths` / `find_best`（支持 `oos.sharpe` 嵌套 metric）
- `TradeMemory`（JSONL 空壳）、`TradeRecord`
- `Document` + `document_loader`（md/txt/json，可截断）
- `SimpleRetriever` 关键词检索（无向量库、无 LLM）
- `tests/test_memory_rag.py`：**11 passed**；全量 **98 passed**（EXP-20260601-013/014）

---

## 文档收口（Prompt 13）✅

- 同步 `docs/progress.md`、`docs/architecture.md`、`docs/experiment_log.md`
- 与 `项目进度.md`、`项目指导.md` 阶段表 / 测试基线 / EXP 一致
- EXP-20260601-015

---

## Plus M1：研究基线与实验规范 ✅

**EXP-20260602-009（本地）** + **EXP-20260602-010（服务器）**

| 组件 | 路径 |
|------|------|
| BaselineRegistry | `src/quant_mas/research/baseline.py` |
| MetricsTable | `src/quant_mas/research/metrics_table.py` |
| CLI | `scripts/compare_experiments.py` |
| 规范 | `docs/research_protocol.md` |
| 测试 | `tests/test_research_baseline.py`（**4 passed**） |

- 全量 pytest：**102 passed**（+4，98→102）
- `compare_experiments.py --help` 正常
- **规则**：后续实验须与 **EXP-20260602-008 OOS sharpe 0.586** 对比
- 服务器验收 ✅：**EXP-20260602-010**（102 passed / 1.64s；比较表 **5 rows**；`oos.sharpe` **0.586**）
- GitHub：`3a7d6df`（M1 代码）→ `18ba1a7`（文档）

---

## Plus M2：数据扩展 ✅

**EXP-20260602-011**（本地）+ **EXP-20260602-012**（服务器）+ **EXP-DATA-001**（API smoke）

| 组件 | 路径 |
|------|------|
| Fetcher 子包 | `src/quant_mas/data/fetchers/`（兼容旧 `quant_mas.data.fetchers` 导入） |
| OHLCV | `AlphaVantageFetcher`、`FinnhubFetcher` |
| 宏观 / 监管 | `FREDFetcher`、`SECEDGARFetcher` |
| Registry | `DataSourceRegistry`、`default_data_source_registry()` |
| 配置 | `configs/data_sources.yaml` |
| 文档 | `docs/data_sources.md` |
| CLI | `download_data.py` — `--source` 增 alpha_vantage/finnhub/fred/sec_edgar；`--series-id`；`--cik` |
| 测试 | `tests/test_data_sources.py`（**13 passed**） |

- 全量 pytest：**115 passed**（+13，102→115；含 AV 日期区间提示测试）
- `test_stooq_fetcher.py`：**6 passed**（未破坏）
- FRED 输出：`raw_data_dir/macro/{series_id}.parquet`
- SEC 输出：`raw_data_dir/sec/{cik}_{kind}.json`
- **服务器 EXP-DATA-001**：FRED 262 行、Stooq 105 行、Alpha Vantage 100 行；Finnhub 免费 tier blocked；SEC 未测
- GitHub：`7514cdc`（AV auto outputsize + 日期提示）

---

## Plus M3：Memory/RAG v2 ✅

**EXP-20260602-013**（本地）+ **EXP-20260602-014**（服务器）

| 组件 | 路径 |
|------|------|
| MemoryStore 抽象 | `src/quant_mas/memory/store_base.py` |
| JSON 后端 | `json_store.py`（包装 ExperimentMemory） |
| SQLite 后端 | `sqlite_store.py`（嵌套 metric、`find_best("oos.sharpe")`） |
| 工厂 | `factory.py` — `create_memory_store` / `create_memory_store_from_yaml` |
| Embedding | `rag/embedding_client.py` — Hash + OpenAI-compatible 骨架 |
| 向量存储 | `vector_store_base.py`、`in_memory_vector_store.py`、`faiss_store.py`（可选） |
| 检索 | `chunking.py`、`hybrid_retriever.py`（关键词 + 向量） |
| 配置 | `configs/memory.yaml` |
| CLI | `scripts/index_documents.py`、`scripts/query_memory.py` |
| 文档 | `docs/database_setup.md` |
| 测试 | `tests/test_memory_store_v2.py`（**11 passed**） |

- 全量 pytest：**126 passed**（本地 + 服务器 3.04s）
- 服务器 smoke：index 132 chunks；RAG 命中 `experiment_log.md` 等；`oos.sharpe` 需 `--json-path /mnt/localDisk3/weizian/reports/experiments.json`
- **下一步**：~~M4~~ ✅ 见下

---

## Plus M4：LangGraph 编排 ✅（本地+服务器）

**EXP-20260602-015**（本地）· **EXP-20260602-016**（服务器 langgraph backend @ `c0fa5e3`）

| 组件 | 路径 |
|------|------|
| State | `orchestration/langgraph_state.py` — `QuantWorkflowState` / `initial_state` |
| 节点 | `nodes.py` — 6 步：data_check → … → report |
| Sequential | `sequential_workflow.py` |
| LangGraph | `langgraph_workflow.py`（可选，`pip install -e ".[orchestration]"`） |
| 配置 | `configs/langgraph_workflow.yaml` |
| CLI | `scripts/run_langgraph_workflow.py` |
| 文档 | `docs/langgraph_workflow.md` |
| 测试 | `tests/test_langgraph_workflow.py`（核心 **11+1 skip**；含 orchestration **12 passed**） |

- 全量 pytest：**137 passed, 1 skipped**（138 项；+11 workflow 测试，126→137）
- 含 orchestration：**138 passed**
- `test_supervisor_agent.py`：**17 passed**（未替换 Supervisor）
- dry-run sequential / langgraph：6 节点跑通，不联网、不调 LLM
- 建边 bug **M-016** 已于 `c0fa5e3` 修复；服务器 EXP-016 已验收

---

## Plus M5：上下文/LLM ✅（本地）

**EXP-20260602-017** — Context Layer + ResearchAgent + 可选真实 LLM

| 组件 | 路径 |
|------|------|
| Schema / Builder | `context/context_schema.py`、`context_builder.py`、`compression.py` |
| LLM | `core/llm.py` — `OpenAICompatibleLLMClient`、`resolve_llm_client` |
| Agent | `agents/research_agent.py`；`ReportAgent` + `ReportResult` |
| CLI | `run_research_agent.py`；`generate_report.py --use-llm` |
| 配置 | `configs/context.yaml`、`configs/llm.yaml` |
| 文档 | `docs/context_engineering.md` |
| 测试 | `tests/test_context_engineering.py`（**12 passed, 1 warning**） |

- 全量 pytest：**150 passed, 1 warning**（+12，138→150）
- 默认 `use_llm=False`；无 key 回退 Mock，不联网
- Supervisor **未替换**；LLM 不覆盖 metrics
- 服务器 EXP-018 / EXP-LLM-001：DeepSeek ResearchAgent smoke ✅

---

## Plus M6：文本信号 ✅（本地+服务器+科研）

**EXP-20260602-019**（本地 mock）· **EXP-20260602-020**（服务器 pytest）· **EXP-TEXT-001**（FinBERT smoke）· **EXP-TEXT-WF-001**（walk-forward OOS）

| 组件 | 路径 |
|------|------|
| Text schema | `src/quant_mas/text/` — `FinancialTextRecord`、`TextSignalRecord` |
| Classifiers | `mock_classifier.py`、`finbert_baseline.py`、`lora_finetune.py`（骨架） |
| Feature merge | `features/text_signals.py` — left join、leakage 检查 |
| CLI | `scripts/train_text_model.py` — `--mode mock|finbert_baseline|lora` |
| 配置 | `configs/text_model.yaml`；服务器示例 `text_model.server.yaml.example` |
| 文档 | `docs/text_model_plan.md`、`docs/codex_prompt_M6.md` |
| 测试 | `tests/test_text_signals.py`（**11 passed**） |

- 全量 pytest：**161 passed**（本地 + 服务器）
- **EXP-TEXT-001**：ModelScope 本地 FinBERT（HF Hub 不可达，见 M-018）；200 signals → `signals_finbert.parquet`
- **EXP-TEXT-WF-002**：100% 覆盖 + 占位 JSONL；OOS sharpe **0.579** vs baseline **0.586**（Δ **-0.007**）
- **EXP-TEXT-WF-003**：Finnhub 真实新闻 2.42% 覆盖；OOS sharpe **0.565** vs baseline **0.586**（Δ **-0.021**）
- 插曲：M-019 `market_data.parquet` 误覆盖 105 行 → 年度分片 re-merge 6033 行
- **下一步**：~~服务器 EXP-20260602-022~~ ✅；~~M8~~ ✅ EXP-023/024；EXP-TEXT-WF-002 / Release v0.1.0

---

## Plus M7：RL 模拟 ✅（本地+服务器）

**EXP-20260602-021/022** — TradingEnv + baseline policies + GRPO-style ranking

| 组件 | 路径 |
|------|------|
| Env / reward | `src/quant_mas/rl/trading_env.py`、`reward.py`、`env_schema.py` |
| Policies | `baseline_policy.py` — Random / BuyAndHold / MLCopy |
| GRPO ranking | `grpo_experiment.py` — group-relative candidate ranking |
| CLI | `scripts/run_rl_baseline.py` |
| 配置 | `configs/rl.yaml` |
| 测试 | `test_trading_env.py` **13** + `test_grpo_experiment.py` **6** |

- 全量 pytest：**180 passed**（161→180，+19）；服务器 **180 passed**（10.15s，EXP-022）
- **simulation only**；metrics `simulation.*`；**不替代** OOS sharpe **0.586**

---

## Plus M8：MCP / A2A ✅

**EXP-20260602-023/024** — MCP adapter + Policy + AgentCard

| 组件 | 路径 |
|------|------|
| MCP | `protocols/mcp/types.py`、`policy.py`、`adapter.py` |
| A2A | `protocols/a2a/agent_card.py` |
| CLI | `scripts/export_agent_cards.py` |
| 测试 | `test_protocols.py` **15 passed** |

- 全量 pytest：**195 passed**（本地 + 服务器 12.41s）
- export_agent_cards → `/mnt/localDisk3/weizian/reports/protocols/`
- 不接外部 MCP server；deny shell/broker/secrets

---

## Plus v3：M9 企业数据与数据库 ✅（本地）

**EXP-20260602-025** — Postgres + pgvector + Neo4j 骨架

| 组件 | 路径 |
|------|------|
| Postgres Memory | `memory/postgres_store.py` — 嵌套 metric、`find_best("oos.sharpe")` |
| pgvector | `rag/vector_store_pgvector.py` — upsert/search/delete |
| Neo4j | `memory/neo4j_store.py` — 实验/策略/特征图 CRUD |
| Factory | `backend: json \| sqlite \| postgres` |
| 配置 | `configs/memory.enterprise.yaml.example` |
| CLI | `query_memory.py --backend postgres`；`index_documents.py --vector-store pgvector` |
| 文档 | `docs/database_setup.md` §M9 |
| 测试 | `test_memory_enterprise.py` **12 passed** |

- 全量 pytest：**207 passed**（195→207，+12）
- 企业后端测试全 mock，不联网；DB URL 仅环境变量
- **下一步**：~~M10~~ ✅ EXP-027；服务器 EXP-026 / EXP-028

---

## Plus v3：M10 LLM 生产化 ✅（本地+服务器+vLLM smoke）

**EXP-20260602-027/028 / EXP-LLM-002** — local_vllm + ResearchAgent 生产路径

| 组件 | 路径 |
|------|------|
| LLM | `core/llm.py` — `mock` \| `openai_compatible` \| **`local_vllm`** |
| Agent | `agents/research_agent.py` — provider + LLM 失败回退 Mock |
| CLI | `run_research_agent.py` / `generate_report.py` — `--provider` |
| 配置 | `configs/llm.server.yaml.example` |
| 文档 | `docs/context_engineering.md` |
| 测试 | `test_context_engineering.py` **17 passed** |

- 全量 pytest：**212 passed**（207→212，+5）
- vLLM 可无 API key；无 `VLLM_BASE_URL` → warning + Mock
- **服务器 vLLM**：独立 conda `vllm`；`VLLM_USE_FLASHINFER_SAMPLER=0` + `--enforce-eager`；模型 `/mnt/localDisk3/weizian/models/Qwen2.5-7B-Instruct`
- **EXP-LLM-002** ✅：`llm_provider=local_vllm`；产物 `reports/llm/EXP-LLM-002*.json`（服务器路径）
- **下一步**：~~EXP-026 DB smoke~~ ✅；EXP-TEXT-WF-002

---

## Plus v3：M11 竞争学习链路 ✅（M11–M11.8 双端闭环）

> 设计：[项目v3设计.md](项目v3设计.md) · 详录：[docs/experiment_log.md](docs/experiment_log.md)

| 子模块 | 实验 | pytest | 关键结果 |
|--------|------|--------|----------|
| **M11** 竞争评估 | EXP-029 / POP-002 | 225 | Population + Elo mock；`simulation.*` |
| **M11.5** 多代训练 | EXP-030 / POP-003 | 237 | 3-gen loop dry-run |
| **M11.6** 候选验证桥 | EXP-031 / POP-004 | 248 | Top-K → `StrategyCandidate`；smoke `backtest.*` |
| **M11.7** 单候选 OOS | EXP-032 / POP-005 | 259 | `cand_mean_rev_1` **oos.sharpe 1.036**（77 窗） |
| **M11.8** 批量 OOS | EXP-033 / POP-006 | **266** | 4 候选均 > **0.586**；best **1.039** |

**指标边界**：`population.*` / `simulation.*` / smoke `backtest.*` **≠** `oos.*`；论文主指标仍为 ML walk-forward **0.586**（EXP-008）。候选 OOS 用于 ablation / 机制分析。

### M11.8 批量 OOS 比较表（EXP-POP-006，服务器 2026-06-04）

| OOS Rank | Candidate | scale | oos.sharpe | vs 0.586 |
|----------|-----------|-------|------------|----------|
| 1 | `cand_mean_rev_1_g1_1_g2_2` | 1.03 | **1.039** | +0.453 |
| 2 | `cand_mean_rev_1_g2_1` | 1.015 | **1.037** | +0.451 |
| 3 | `cand_mean_rev_1_g1_1` | 1.01 | **1.037** | +0.451 |
| 4 | `cand_mean_rev_1` | 1.0 | **1.036** | +0.450 |

Population 输入 rank 与 OOS rank **不一致**（scale 微调在 OOS 上略优）。产物：`outputs/candidate_oos_batch/candidate_oos_comparison.md`。

| 组件 | 路径 |
|------|------|
| 竞争 / Elo | `rl/competitive_experiment.py`、`population/` |
| 训练闭环 | `scripts/run_population_training.py` |
| 候选导出 | `scripts/export_population_candidates.py` |
| 单候选 OOS | `scripts/validate_candidate_oos.py` |
| **批量 OOS** | `scripts/batch_validate_candidates.py` |
| 文档 | `competitive_learning.md` → `population_training.md` → `strategy_candidate_bridge.md` → `strategy_candidate_oos.md` → **`candidate_oos_batch.md`** |

- 全量 pytest：**266 passed**（259→266，+7）；服务器 **266 passed**（45.63s，EXP-POP-006）
- **下一步**：服务器 **EXP-POP-007** RL smoke；EXP-TEXT-WF-002；M12.2 policy export bridge

---

## Plus v3：M12 RL 训练（M12.1 本地 ✅）

> 设计：[docs/rl_experiment.md](docs/rl_experiment.md) · Codex：[docs/codex_prompt_M12.md](docs/codex_prompt_M12.md)

M7 只有 GRPO **排名**；M12.1 增加可迭代 **训练 loop** + checkpoint，仍 **simulation only**。

| 组件 | 路径 |
|------|------|
| 可训练策略 | `rl/grpo_agent.py` — `GRPOPolicyAgent`、`PolicyState` |
| 训练循环 | `rl/training_loop.py` — `RLTrainingLoop`、checkpoint、OOS stub |
| PPO / MARL | `rl/ppo_trainer.py`（stub）、`rl/marl_stub.py` |
| CLI | `scripts/run_rl_experiment.py` |
| 配置 | `configs/rl_training.yaml` |
| 测试 | `tests/test_rl_training.py` — **16** 项 |

**指标边界**：`training.*` / `simulation.*` only；**禁止**训练 loop 写 `oos.*`。OOS 须事后走 M11.7 / M11.8。

- 本地 + 服务器：**282 passed** 双端；EXP-20260602-034 / EXP-POP-007 ✅
- 服务器 GRPO smoke：`simulation.sharpe_mean` **6.31**（**≠ OOS 0.586**）
- **下一步**：~~EXP-POP-008 export~~ ✅ · ~~RL 候选 M11.7 OOS~~ ✅ EXP-POP-009；EXP-TEXT-WF-002；可选更长 RL 训练

---

## Plus v3：M12.2 RL Policy Export（双端 ✅）

> 设计：[docs/rl_policy_export.md](docs/rl_policy_export.md)

```text
M12.1 run_rl_experiment → policy_state.json + metrics.json
  → M12.2 export_rl_policy_candidate → StrategyCandidate（source=rl_training）
  → M12.3 CandidateStrategyAdapter（grpo_policy）
  → M11.7 validate_candidate_oos / M11.8 batch（独立 OOS，才写 oos.*）
```

| 组件 | 路径 |
|------|------|
| Export bridge | `rl/policy_export.py` |
| OOS adapter | `research/candidate_validation.py` — `grpo_policy` 分支 |
| CLI | `scripts/export_rl_policy_candidate.py`、`scripts/validate_candidate_oos.py` |
| 配置 | `configs/rl_policy_export.yaml`、`configs/candidate_oos.yaml` |
| 测试 | `tests/test_rl_policy_export.py`（12）+ `tests/test_candidate_oos_validation.py`（+2 grpo） |

- 本地：**310 passed**（296→310，M12.4）；EXP-20260602-035 / EXP-036 ✅
- 服务器：**EXP-POP-008** export ✅（`rl_grpo_policy_001_1`）；**EXP-POP-009** OOS ✅（`oos.sharpe=0.0`，全现金 ablation）
- 边界：export **不写 oos.***；OOS 仅 M11.7/M11.8 写 `oos.*`；`simulation.sharpe_mean=6.31` **≠ OOS**

---

## Plus v2 收官（V2 结尾）

> **定稿日期**：2026-06-01 · **验收**：EXP-023（本地）+ EXP-024（服务器）· **pytest**：195 passed · 详图见 [`docs/architecture.md`](docs/architecture.md)

### 设计原则

> **Quant Engine computes. Agent Layer explains, orchestrates, and reports.**

- **Quant Engine** 负责确定性计算：数据、特征、模型、策略、回测、风控。
- **Agent Layer** 负责研究、规划、解释、报告和工具编排。
- **LLM Agent 不允许直接实盘下单**；所有信号须经回测、风控、审计和人工确认。
- **论文/报告主指标**：Walk-forward OOS sharpe **0.586**（EXP-20260602-008）；新实验须经 M1 `compare_experiments.py` 对比。
- **指标族不可混比**：`oos.*`（论文）≠ `simulation.*`（RL）≠ 单段 ml_backtest（in-sample）。

### v2 里程碑一览

| 编号 | 名称 | 状态 | 关键交付 |
|------|------|------|----------|
| v1 Prompt 1–20 | 量化核心 + Agent + Memory | ✅ | 98 passed 基线 |
| **M1** | 研究基线 | ✅ | BaselineRegistry、compare_experiments |
| **M2** | 数据扩展 | ✅ | 多数据源 fetcher + registry |
| **M3** | Memory/RAG v2 | ✅ | SQLite、HybridRetriever、index/query CLI |
| **M4** | LangGraph 编排 | ✅ | 6 节点 ResearchWorkflow DAG |
| **M5** | 上下文/LLM | ✅ | ContextBuilder、ResearchAgent |
| **M6** | 文本信号 | ✅ | FinBERT smoke + WF OOS 0.563 vs 0.586 |
| **M7** | RL 模拟 | ✅ | TradingEnv + GRPO ranking |
| **M8** | MCP/A2A | ✅ | MCP adapter + AgentCard 导出 |

**v2 不做**：实盘 broker、外部 MCP server、ShellTool、网络 listener。

### 系统分层（定稿）

```text
Quant MAS（Plus v2）
│
├── Quant Engine Layer          确定性核心
│   ├── data/                   ParquetStorage, DataCatalog, fetchers/, validate_ohlcv
│   ├── features/               technical, labels, text_signals（M6）, build_feature_table
│   ├── models/                 LightGBMDirectionModel, training helpers
│   ├── strategies/             MovingAverageCrossStrategy, MLSignalStrategy
│   ├── backtest/               BacktestEngine, walk_forward, metrics, report
│   ├── risk/                   RiskLimits, exposure, drawdown_guard
│   ├── utils/                  device.py（GPU/CUDA）
│   └── pipeline.py             run_quant_pipeline
│
├── Tool Layer（7 tools）       Agent 可调用的量化能力封装
│   data_summary | backtest | train_model | report
│   risk_check | ml_backtest | pipeline
│
├── Agent Layer                 轻量编排（不替代 Quant Engine）
│   core/                       Message, MockLLMClient, OpenAICompatibleLLMClient, BaseAgent
│   agents/                     SupervisorAgent（规则路由）, ResearchAgent（M5）, ReportAgent
│
├── Text Signal Layer（M6）     FinancialTextRecord → sentiment → merge_text_signals
│   text/                       mock_classifier, finbert_baseline, lora_finetune（骨架）
│
├── RL Simulation Layer（M7）   TradingEnv + baseline policies + GRPO ranking（simulation only）
│   rl/                         trading_env, reward, baseline_policy, grpo_experiment
│
├── Protocol Layer（M8）        内部 adapter only
│   protocols/mcp/              types, policy, adapter
│   protocols/a2a/              agent_card
│
├── Context Layer（M5）         ContextBuilder → AgentContextBundle；metrics 与 LLM 叙事分离
│   context/                    context_schema, context_builder, compression
│
├── Orchestration Layer（M4）   ResearchWorkflow — sequential / 可选 LangGraph
│   orchestration/              6 节点：data_check → feature → train → ml_backtest → risk → report
│
├── Memory Layer（M3）          可插拔 MemoryStore（JSON / SQLite）；ExperimentMemory 兼容
│   memory/                     json_store, sqlite_store, factory, trade_memory
│
├── RAG Layer（M3）             SimpleRetriever + HybridRetriever（关键词 + 向量）
│   rag/                        document_loader, HashEmbeddingClient, InMemoryVectorStore
│
└── Research Layer（M1）        实验基线注册、指标汇总、跨实验比较
    research/                   baseline.py, metrics_table.py, compare_experiments.py
```

### 典型数据流

**论文主路径（Walk-forward OOS）：**

```text
download_data → merge_parquet → build_features
    → train_model（LightGBM）
    → run_walk_forward（OOS metrics，主指标 oos.sharpe 0.586）
    → ExperimentMemory
    → compare_experiments → docs/experiment_log.md
```

**Agent 交互路径：**

```text
用户自然语言 / run_agent.py
    → SupervisorAgent（关键词规则路由）
    → ToolRegistry（7 tools）
    → Quant Engine 执行
    → metrics + 报告路径 → ExperimentMemory
```

**LangGraph 工作流（M4，可选）：**

```text
run_langgraph_workflow.py
    → 6 节点 DAG（dry-run 默认，不联网、不调 LLM）
    → 内部复用 ToolRegistry
```

**文本信号路径（M6，exploratory）：**

```text
train_text_model.py（mock / finbert_baseline / lora）
    → signals parquet
    → merge_text_signals_into_features
    → run_walk_forward（EXP-TEXT-WF-001：OOS 0.563 vs baseline 0.586）
```

**RL 模拟路径（M7，不可与 OOS 混比）：**

```text
run_rl_baseline.py --policy random|buy_hold|ml_copy --dry-run
    → TradingEnv rollouts
    → simulation.* metrics（非 oos.*）
```

**协议导出（M8）：**

```text
export_agent_cards.py
    → supervisor / research / report AgentCard JSON
    → mcp_tools.json（MCPToolSpec + ToolPolicy 白名单）
```

### Supervisor 路由表

| 关键词示例 | 工具 |
|------------|------|
| ML回测 / ml backtest | ml_backtest |
| 风控 / risk | risk_check |
| 全流程 / pipeline | pipeline |
| 回测 / backtest | backtest |
| 训练 / train | train_model |
| 报告 / report | report |
| 数据 / data | data_summary |

### CLI 入口（v2 全集）

| 脚本 | 用途 |
|------|------|
| `download_data.py` / `merge_parquet.py` | 数据获取与合并 |
| `build_features.py` | 特征构建 |
| `train_model.py` | LightGBM 训练（`--device auto/cpu/gpu/cuda`） |
| `run_backtest.py` / `run_ml_backtest.py` | 策略 / ML 回测 |
| `run_walk_forward.py` | **论文级 OOS 评估** |
| `run_pipeline.py` | 端到端 pipeline |
| `run_agent.py` | Supervisor 规则路由 |
| `run_langgraph_workflow.py` | M4 工作流 DAG |
| `run_research_agent.py` | M5 ResearchAgent + Context |
| `generate_report.py` | 报告（`--use-llm` 可选） |
| `train_text_model.py` | M6 文本信号 |
| `run_rl_baseline.py` | M7 RL 模拟 |
| `export_agent_cards.py` | M8 MCP/A2A 导出 |
| `compare_experiments.py` | M1 实验对比表 |
| `index_documents.py` / `query_memory.py` | M3 文档索引与检索 |

### 仓库目录（v2）

```text
Quant-MAS/
├── src/quant_mas/          # 核心包（data / features / models / strategies / backtest / risk /
│                           #   tools / agents / core / memory / rag / research / context /
│                           #   orchestration / text / rl / protocols）
├── scripts/                # CLI 入口
├── configs/                # YAML（backtest, train, memory, rl, protocols, …）
├── tests/                  # 195 pytest
├── docs/                   # 架构、实验日志、各 M 里程碑文档
├── server/                 # 服务器 shell 脚本
└── outputs/                # 运行时产物（gitignore）
```

### v2 关键实验指标

| 指标 | 值 | 实验 | 说明 |
|------|-----|------|------|
| pytest 基线 | **195 passed** | EXP-023/024 | 本地 + 服务器 |
| **OOS baseline** | **sharpe 0.586** | EXP-20260602-008 | **论文主指标**，19 walk-forward 窗 |
| OOS + FinBERT text (wf001) | sharpe 0.563 | EXP-TEXT-WF-001 | exploratory（3.32% 覆盖） |
| OOS + FinBERT text (wf002) | sharpe **0.579** | EXP-TEXT-WF-002 | 100% 覆盖，占位文本；Δ **-0.007** |
| OOS + FinBERT text (wf003) | sharpe **0.565** | EXP-TEXT-WF-003 | 2.42% 覆盖，Finnhub 真实新闻；Δ **-0.021** |
| 单段 ML backtest | sharpe 2.78 | EXP-20260602-005 | in-sample，**不可**替代 OOS |
| RL simulation | simulation.sharpe ~9.54 | EXP-022 dry-run | synthetic 短 episode，**不可**与 OOS 混比 |

### v2 安全边界（定稿）

1. LLM / Agent **不直接实盘下单**
2. M8 ToolPolicy 默认 deny：shell、broker、order、secrets、`.env` 路径
3. M8 **不接**外部 MCP server，**不**启动 network listener
4. pytest 默认 Mock LLM / mock HTTP，不依赖真实网络或 HF 权重下载
5. 实盘须：回测 → 风控 → 审计 → 人工确认（v2 未实现 broker 层）

---

## 测试与 CLI

### pytest

| 环境 | Python | 结果 | 日期 |
|------|--------|------|------|
| 本地 Windows | 3.11+ | **331 passed** | 2026-06-04 | EXP-TEXT-WF-003 docs |
| 服务器 a6000-9961 | 3.11.15 | **331 passed** | 2026-06-04 | EXP-TEXT-WF-003 OOS **0.565** @ `561e104` |

```bash
conda activate /mnt/localDisk3/weizian/conda_envs/quant-mas
cd /mnt/localDisk3/weizian/Quant-MAS
python -m pytest -v
```

### 可用 CLI

```powershell
python scripts/download_data.py --help
python scripts/build_features.py --help
python scripts/run_backtest.py --help
python scripts/train_model.py --help
python scripts/generate_report.py --help
python scripts/run_agent.py --help
python scripts/run_pipeline.py --help
python scripts/run_ml_backtest.py --help
python scripts/run_walk_forward.py --help
python scripts/compare_experiments.py --help
python scripts/index_documents.py --help
python scripts/run_research_agent.py --help
python scripts/run_langgraph_workflow.py --help
python scripts/train_text_model.py --help
python scripts/run_rl_baseline.py --help
python scripts/export_population_candidates.py --help
python scripts/validate_candidate_oos.py --help
python scripts/batch_validate_candidates.py --help
python scripts/run_rl_experiment.py --help
python scripts/export_rl_policy_candidate.py --help
python scripts/export_agent_cards.py --help
python scripts/query_memory.py --help
```

---

## 服务器实验一览

| 项目 | 值 |
|------|-----|
| 路径 | `/mnt/localDisk3/weizian/Quant-MAS` |
| Conda | `/mnt/localDisk3/weizian/conda_envs/quant-mas` |
| 真实数据 | Stooq AAPL+MSFT+SPY 2018–2025，6033 rows |
| CPU 训练 | `server_lgbm_001` |
| GPU 训练 | `server_lgbm_gpu_001`（见 M-010 CUDA 安装） |
| ML 回测 | `server_ml_backtest_001` |
| Walk-forward | `server_walk_forward_001`（OOS sharpe **0.586**） |
| Walk-forward + text | `server_walk_forward_text_001`（OOS sharpe **0.563**，EXP-TEXT-WF-001） |
| v3 M11.8 batch candidate OOS | EXP-POP-006 — 4/4 超 baseline；best **1.039** vs **0.586** |
| v3 M12.1 RL training smoke | EXP-POP-007 — **simulation.sharpe_mean 6.31**（**≠ OOS**） |
| v3 M11.7 candidate OOS | EXP-POP-005 — `cand_mean_rev_1` **1.036** vs **0.586** |
| FinBERT text smoke | EXP-TEXT-001 — ModelScope 本地 FinBERT，200 signals |
| Plus M8 pytest | EXP-023/024，**195 passed**（本地+服务器） |
| Plus v3 M9/M10 pytest | EXP-027/028，**212 passed**（本地+服务器，11.39s） |
| Plus M7 pytest | EXP-021/022，**180 passed** |
| Plus M6 pytest | EXP-20260602-019/020，**161 passed** |
| Prompt 20 pytest | EXP-20260601-013/014，**98 passed**（服务器 1.93s） |
| Plus M1 pytest | EXP-20260602-009/010，**102 passed** |
| Plus M5 pytest | EXP-20260602-017，**150+1 warning**（+12） |
| Plus M3 pytest | EXP-20260602-013/014，**126 passed** |
| Plus M2 pytest | EXP-20260602-011/012，**115 passed**（+13） |
| Plus M2 API smoke | EXP-DATA-001 ✅ | FRED+Stooq+AV ✅；Finnhub 免费 blocked；SEC 未测 |

---

## 后续工作（v2 之后，非 v2 骨架）

> **Plus v2 工程主线（M1–M8）已收官。** v3 研究与工程升级见 **[`项目v3设计.md`](项目v3设计.md)**（M9–M13：企业 DB、LLM、竞争学习、RL 训练、编排扩展）。

| 优先级 | 任务 | 类型 | 状态 |
|--------|------|------|------|
| **1** | ~~Plus v2 M1–M8 本地+服务器验收~~ | 工程 | ✅ **v2 收官** |
| **2** | ~~v3 **M9** 企业 DB 本地~~ | 工程 | ✅ EXP-025 |
| **3** | ~~v3 **M10** LLM / vLLM~~ | 工程 | ✅ EXP-027/028/LLM-002 |
| **4** | ~~服务器 pull M9+M10 → pytest **212**~~ | 服务器 | ✅ EXP-028 |
| **5** | M11 competitive dry-run | 服务器 | ✅ EXP-POP-002 |
| **6** | M11.5 population training | 服务器 | ✅ EXP-POP-003 |
| **7** | M11.6 candidate bridge | 服务器 | ✅ EXP-POP-004 |
| **8** | M11.7 candidate OOS（真实 features） | 服务器 | ✅ EXP-POP-005 |
| **9** | M11.8 批量候选 OOS + 比较表 | 服务器 | ✅ EXP-POP-006 |
| **10** | M12.1 RL training loop | 双端 | ✅ EXP-034 / EXP-POP-007 |
| **11** | M12.2 RL policy export | 双端 | ✅ EXP-035 / EXP-POP-008 |
| **12** | M12.3 RL 候选 OOS | 双端 | ✅ EXP-POP-009 |
| **13** | M12.4 Observation-aware RL | 双端 | ✅ EXP-036 / **EXP-POP-010** |
| **14** | EXP-TEXT-WF-002 prep | 本地 | ✅ 覆盖率审计工具（314 pytest） |
| **15** | EXP-TEXT-WF-002 服务器 OOS | 服务器 | ✅ oos.sharpe **0.579** vs **0.586** |
| **16** | EXP-TEXT-WF-003 prep | 本地 | ✅ 真实新闻 JSONL 对齐工具（326 pytest） |
| **17** | EXP-TEXT-WF-003 服务器 OOS | 服务器 | ✅ oos.sharpe **0.565** vs **0.586** |

---

## EXP-TEXT-WF-002 准备 ✅（覆盖率审计工具）

**日期**：2026-06-04  
**定位**：在 EXP-TEXT-WF-001（`oos.sharpe=0.563` vs baseline `0.586`）基础上，先审计文本信号覆盖率，再用同一 walk-forward 协议验证高覆盖文本信号是否改善 OOS。

### 已实现

| 模块 | 路径 | 说明 |
|------|------|------|
| 覆盖率审计函数 | `src/quant_mas/features/text_signals.py` | `summarize_text_signal_coverage()` |
| CLI | `scripts/audit_text_signals.py` | 输出 `metrics.json` / `summary.md` |
| 测试 | `tests/test_text_signals.py` | 文本模块 **15 passed**（新增覆盖率审计 4 项） |
| 文档 | `docs/text_model_plan.md` / `docs/server_commands.md` | EXP-TEXT-WF-002 标准流程 |

### 论文可用表述

English:

> Before re-evaluating text-enhanced models, we added a coverage audit for text-derived signals. The audit reports feature rows, signal rows, matched rows, coverage ratio, matched symbols, and date ranges. This prevents sparse text coverage from being hidden inside the feature table and ensures that any future text-enhanced walk-forward result can be interpreted together with its actual information coverage.

中文：

> 在重新评估文本增强模型之前，我们加入了文本信号覆盖率审计。该审计报告特征行数、信号行数、匹配行数、覆盖率、覆盖标的数量和日期范围。这样可以避免稀疏文本覆盖被隐藏在特征表中，并确保未来文本增强 walk-forward 结果能够结合真实信息覆盖率进行解释。

### 下一步服务器实验

1. 生成更高覆盖率 `signals_finbert_wf002.parquet`。
2. 运行 `scripts/audit_text_signals.py`，记录 `coverage_ratio`。
3. 构建 `features_with_text_wf002.parquet`。
4. 运行 `server_walk_forward_text_002`。
5. 与 EXP-20260602-008 的 `oos.sharpe=0.586` 对比。

---

## EXP-TEXT-WF-003 ✅：真实 Finnhub 新闻 + Walk-forward OOS

**日期**：2026-06-04  
**定位**：Finnhub 真实公司新闻 + 发布时间对齐 + 与 WF-001/WF-002 同协议 walk-forward OOS。

### 结果摘要

| 阶段 | 数值 |
|------|------|
| Fetch | **9434** 条（`--start 2025-06-04 --end 2026-06-04`） |
| 有效重叠 | **~2025-06-04 ~ 2025-12-31**（features 至 2025-12-31） |
| Align | **5088** / **4346** dropped |
| FinBERT signals | **146** 行 |
| Coverage | **2.42%** |
| **oos.sharpe** | **0.565**（Δ vs **0.586** = **-0.021**） |
| oos.total_return | **0.421** |

**结论（exploratory）**：稀疏覆盖下真实新闻 ≈ WF-001（**0.563**），低于 WF-002 占位（**0.579**）与主基线。须同时报告 fetch/align/coverage 与 `oos.*`。

详见 [docs/real_news_text_experiment.md](docs/real_news_text_experiment.md)。

## 约束备忘

- 大数据不入 git；`python -m pytest` / `python -m pip`（勿裸敲 pytest / pip）
- Agent 只编排工具，不直接下单；实盘须风控 + 审计 + 人工确认

---

## M12.4 Observation-aware RL Policy ✅（双端）

**日期**：2026-06-04  
**定位**：观测感知线性 RL 策略，产生状态相关 exposure，经 M11.7 walk-forward OOS 验证。  
**状态**：本地 + 服务器 ✅（**EXP-POP-010**）

### 已实现

| 模块 | 路径 | 说明 |
|------|------|------|
| Feature policy | `src/quant_mas/rl/feature_policy.py` | `FeatureLinearPolicyAgent` / `FeaturePolicyState` |
| 训练入口 | `scripts/run_rl_experiment.py` | 新增 `--policy-type logits|feature_linear` |
| 导出桥 | `src/quant_mas/rl/policy_export.py` | 自动识别 feature policy，导出 `agent_type="feature_linear_policy"` |
| OOS replay | `src/quant_mas/research/candidate_validation.py` | M11.7 adapter 支持 feature-linear candidate |
| 配置 | `configs/rl_training.yaml` | 新增 `policy.type` 与 `policy.feature_names` |
| 测试 | `tests/test_rl_observation_policy.py` | 14 项测试 |

### 验证

| 命令 | 结果 |
|------|------|
| `python -m pytest tests/test_rl_observation_policy.py -v` | **14 passed** |
| `python -m pytest tests/test_rl_training.py tests/test_rl_policy_export.py -v` | **28 passed** |
| `python -m pytest tests/test_candidate_oos_validation.py tests/test_candidate_oos_batch.py -v` | **20 passed** |
| `python -m pytest -v` | **308 passed**（服务器） / **310 passed**（本地） |

### 服务器 OOS（EXP-POP-010）

| 字段 | 值 |
|------|-----|
| `candidate_id` | `rl_feature_linear_policy_001_1` |
| `oos.sharpe` | **0.387** |
| `oos.total_return` | **0.338** |
| `vs_baseline_sharpe` | **-0.199**（baseline **0.586**） |
| `simulation.sharpe_mean` | **12.13**（**≠ OOS**） |

- 下一步：EXP-TEXT-WF-002；可选更长 RL 训练
- 备注：服务器 summary 中若仍出现 `agent_type="grpo_policy"`，属于旧导出标签残留；本次 OOS 回放已通过 `params.policy_type=feature_linear` 正确执行。

### 论文可用表述

English:

> We further implemented an observation-aware feature-linear RL policy that maps market state variables (`position_weight`, `last_return`, `rolling_vol_5`, `volume`, and `close`) to discrete target exposure levels. Unlike the earlier logits-only RL smoke candidate, this policy can produce state-dependent positions. Training-stage metrics remain simulation-only, and all `oos.*` metrics are generated exclusively through the independent walk-forward validation hook.

> In the server OOS ablation, the observation-aware RL policy improved the exported RL candidate from an all-cash OOS result (`Sharpe = 0.0`) to a state-dependent trading policy (`Sharpe = 0.387`). However, it still underperformed the primary ML walk-forward baseline (`Sharpe = 0.586`), indicating that the current one-step smoke RL training validates the bridge mechanism but is not yet competitive as a primary research signal.

中文：

> 在 M12.4 中，我们实现了第一版观测感知的线性 RL 策略，将仓位、最近收益、滚动波动率、成交量和价格等市场状态映射为离散目标仓位。该策略解决了早期 logits-only RL smoke 候选容易退化为全现金常数仓位的问题。训练阶段仍只记录 `training.*` 与 `simulation.*`，所有 `oos.*` 指标仍必须通过独立 walk-forward 验证模块产生。

> 在服务器 OOS 消融中，观测感知 RL 策略将导出的 RL 候选从全现金结果（`Sharpe = 0.0`）提升为具有状态相关仓位的策略（`Sharpe = 0.387`）。但该结果仍低于 ML walk-forward 主基线（`Sharpe = 0.586`），因此当前 1-step smoke RL 训练应被解释为桥接机制验证，而不是论文主研究信号的替代。

### 边界

- M12.4 不调用 LLM、不联网、不接 broker、不需要 torch/GPU。
- M12.4 训练不写 `oos.*`。
- 论文主 baseline 仍是 EXP-20260602-008：`oos.sharpe = 0.586`。
- 下一步建议：优先做 EXP-TEXT-WF-002 或 M13 编排；RL 若继续推进，应做更长训练 / 多 seed / 多 action-space 的系统 ablation，而不是直接改写主结论。

## M13 ��һ�׶���ƣ���ҵ��������Э����չ

��ǰ������ʵ�� **M13.0 MCP Scheduler Minimal**��������һ����ʵ������ recipe / LangGraph ��չ��

### Ϊʲô���� M13.0

Quant MAS �Ѿ���� ML baseline���ı��������ڡ�Population OOS��RL OOS ablation �ȶ����о���·����ǰʹ�㲻��ȱ�ٵ���ģ�ͣ����Ƿ�����������Ȼ�����ֹ������ű�������©����Ҳ��������ơ���� M13.0 �Ƚ�������ȡ���Ϣ����ơ���ȫ�߽硱���ļ��¡�

### M13.0 ��Χ

- �ڲ� Agent message �� in-memory bus��
- mock-first scheduler��Ĭ�� dry-run��
- audit JSONL���ɲ�ѯ����¼��ͻ���״̬��
- `run_mcp_pipeline.py` CLI��
- ���� ToolPolicy�������ܾ� shell / broker / order / secrets��
- �����ⲿ MCP server listener��
- ��������ʵ LLM��
- ��д���µ� OOS �о����ۡ�

### �������

| �׶� | ���� |
|------|------|
| M13.0 | MCP Scheduler Minimal����Ϣ�����ȡ���ơ���ȫ�߽� |
| M13.1 | Pipeline Recipe Scheduler��ML/Text/Population/RL YAML recipe |
| M13.2 | LangGraph Extended DAG��Population��RL��batch walk-forward ��ѡ�ڵ� |
| M13.3 | Paper Artifact Export�����ı������ڱ�����ư� |

��һ��ʵ��Ŀ�꣺`tests/test_mcp_scheduler.py` ͨ����`python scripts/run_mcp_pipeline.py --help` ������ȫ�� pytest ����ͨ����

## M13.0 ����ɣ�MCP Scheduler Minimal

M13.0 ����ɱ�����С�ջ���ʵ���ڲ� dry-run ���ȡ�Agent message��audit JSONL �� ToolPolicy ��ȫ�߽硣

�����ļ���

- `src/quant_mas/orchestration/agent_communication.py`
- `src/quant_mas/orchestration/audit_log.py`
- `src/quant_mas/orchestration/mcp_scheduler.py`
- `scripts/run_mcp_pipeline.py`
- `tests/test_mcp_scheduler.py`

���ս����

- `python -m pytest tests/test_mcp_scheduler.py -v` �� **11 passed**
- `python scripts/run_mcp_pipeline.py --help` �� OK
- `python scripts/run_mcp_pipeline.py --list-recipes` �� `mock_research`, `text_smoke`
- `python scripts/run_mcp_pipeline.py --recipe mock_research --dry-run` �� д�� `audit.jsonl`
- `python -m pytest -v` �� **342 passed**

���б߽磺M13.0 ֻ��֤���Ⱥ���ƣ������� OOS �о����ۣ������ M4 ResearchWorkflow��SupervisorAgent �� Quant Engine��

��һ�����飺M13.1 Pipeline Recipe Scheduler���� ML/Text/Population/RL �����������ֹ����� YAML recipe ����

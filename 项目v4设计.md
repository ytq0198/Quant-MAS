# Quant MAS v4 全栈设计方案

> 目标：将 Quant MAS 从 v3 的多智能体量化研究平台，升级为 v4 全栈研究系统，补齐前端 UI、后端 API、数据库服务、Agent/Tool/Memory/RAG 调用链路、实验可视化、部署与文档协同。

---

## 1. v4 版本定位

Quant MAS v4 不改变 v3 的核心安全边界：Quant Engine 负责确定性量化计算，Agent Layer 负责研究编排、解释、工具调用和报告生成，LLM Agent 不直接实盘下单。

v4 的重点是把已有能力从“命令行 + 文档 + 实验脚本”升级为“可交互、可展示、可部署、可审计”的全栈系统：

- 前端 UI：Dashboard、Agent 控制台、Tool 调用面板、Memory/RAG 查询、回测与 Walk-forward 可视化。
- 后端 API：REST / WebSocket 接口，封装实验查询、Agent 调用、Tool 调用、报告生成和审计日志读取。
- 数据库：Postgres、pgvector、Neo4j 可选接入，用于实验记录、向量检索和关系图谱扩展。
- MAS Agent：通过受控 ToolRegistry 调用 Quant Engine、Memory/RAG 和报告模块。
- Orchestration：保留 LangGraph / MCP / YAML Pipeline / Audit JSONL 的 v3 设计，并暴露给 UI。
- Paper artifact：继续支持论文级表格、审计摘要和实验索引导出。

---

## 2. 前端应该跑在本地主机还是服务器？

### 2.1 开发 / 设计阶段：推荐在本地主机

前端设计和开发通常应该在本地主机进行，而不是一开始就放到服务器上。

推荐技术栈：

- React + Vite
- Vue + Vite
- SvelteKit
- Next.js

本地开发优点：

1. 即时热更新：修改 UI 后可以立即在浏览器看到效果。
2. 调试方便：可以直接用浏览器开发者工具调试 JS、CSS、DOM、网络请求。
3. 不需要频繁上传服务器：减少部署时间和带宽消耗。
4. 版本控制方便：前端代码直接放在 Git 仓库内，和后端、docs 一起管理。
5. 适合快速试错：Dashboard、表格、图表、Agent 面板都可以快速迭代。

推荐开发方式：

```bash
cd frontend
npm install
npm run dev
```

开发环境中：

- 前端运行在 `http://localhost:3000` 或 `http://localhost:5173`。
- 后端 API 可以运行在本机，也可以运行在服务器。
- 如果后端在服务器，例如 `http://server-ip:8000/api`，前端通过 proxy 或 CORS 访问。

### 2.2 部署 / 测试 / 共享阶段：推荐放到服务器

当前端开发完成后，如果需要给团队成员、导师、答辩评委或测试用户访问，就应该部署到服务器。

部署方式：

- 前端执行 `npm run build` 生成静态资源。
- 使用 Nginx / Apache / Caddy 托管前端静态文件。
- 或使用 Docker / Node.js / FastAPI / Flask 统一部署前后端服务。
- 后端 API、数据库、向量库、Agent 服务可以部署在同一服务器或内部网络。

服务器端优点：

1. 多人访问：公网 IP / 域名可以让团队成员访问。
2. 统一管理：前端、后端、数据库、日志、模型服务统一部署。
3. 安全可控：可通过防火墙、VPN、反向代理和鉴权限制访问。
4. 便于演示：答辩、项目展示、远程协作更方便。

### 2.3 推荐结论

| 阶段 | 建议位置 | 理由 |
|---|---|---|
| 开发 / 设计 | 本地主机 | 快速迭代、热更新、调试方便、Git 管理简单 |
| 联调 / 测试 | 本地主机前端 + 服务器后端 | 前端开发效率高，同时可对接真实服务器 API |
| 部署 / 展示 / 共享 | 服务器 | 多人访问、统一管理、安全可控 |

结论：v4 前端建议先在你的主机上开发，后端 API 可以本地或服务器运行；等 UI 稳定后，再 build 并部署到服务器。

---

## 3. v4 全栈架构草图

```text
User Browser
    |
    v
Frontend UI
    |-- Dashboard
    |-- Agent Console
    |-- Tool Console
    |-- Memory/RAG Search
    |-- Backtest / Walk-forward Visualization
    |-- Risk / Audit / Human Review
    |
    v
Backend API
    |-- REST API
    |-- WebSocket Stream
    |-- Auth / Config / Safety Policy
    |
    v
Quant MAS Core
    |-- Data Layer
    |-- Quant Engine
    |-- Backtest / Walk-forward / Risk
    |-- ToolRegistry
    |-- Agent Layer
    |-- Memory/RAG
    |-- Orchestration
    |-- Paper Artifact Export
    |
    v
Storage / Services
    |-- Postgres
    |-- pgvector
    |-- Neo4j
    |-- Parquet / JSONL
    |-- Local vLLM / DeepSeek optional
```

核心原则：

- UI 只发起请求，不直接改写交易状态。
- Backend API 负责权限、安全策略、参数校验和审计记录。
- Agent 只能通过 ToolRegistry 调用受控工具。
- Quant Engine 仍然是唯一的确定性计算层。
- 所有交易候选必须经过 backtest、risk check、audit log、human confirmation。
- `oos.*` 指标与 `simulation.*`、`training.*`、`population.*`、`audit.*` 指标继续严格分离。

---

## 4. v4 功能模块规划

### 4.1 前端 UI 模块

| UI 模块 | 功能 | 对接 API |
|---|---|---|
| Dashboard | 展示系统状态、测试基线、最近实验、OOS 基线、Agent 状态 | `/api/status`, `/api/experiments` |
| Agent Console | 查看 Agent 列表，提交研究任务，查看 LLM 分析输出 | `/api/agents`, `/api/agents/run` |
| Tool Console | 查看 Tool 列表，运行 BacktestTool / RiskTool / PipelineTool 等受控工具 | `/api/tools`, `/api/tools/run` |
| Memory/RAG Search | 输入问题，检索实验记忆、文档块、历史报告 | `/api/memory/search`, `/api/rag/query` |
| Backtest View | 展示回测指标、权益曲线、交易摘要和风险信息 | `/api/backtests/{id}` |
| Walk-forward View | 展示 OOS 窗口、OOS Sharpe、窗口级指标和基线对比 | `/api/oos/{id}` |
| Population / RL View | 展示策略种群、自博弈、RL / GRPO / MARL 实验摘要 | `/api/population`, `/api/rl` |
| Audit / Human Review | 展示审计日志、候选状态、人工确认记录 | `/api/audit`, `/api/review` |
| Paper Export | 触发论文产物导出，查看 paper tables 和 experiment index | `/api/paper/export`, `/api/paper/artifacts` |

### 4.2 后端 API 模块

建议使用 FastAPI 作为 v4 后端入口，原因是类型清晰、自动生成 OpenAPI、适合 REST + WebSocket。

建议 API 分组：

```text
src/backend/
|-- app.py
|-- api/
|   |-- status.py
|   |-- experiments.py
|   |-- agents.py
|   |-- tools.py
|   |-- memory.py
|   |-- rag.py
|   |-- backtests.py
|   |-- oos.py
|   |-- risk.py
|   |-- audit.py
|   |-- paper.py
|   `-- websocket.py
|-- schemas/
|-- services/
|-- security/
`-- README.md
```

后端职责：

- 参数校验。
- 调用 Quant MAS core。
- 调用 ToolRegistry。
- 读取 ExperimentMemory。
- 写入 Audit JSONL。
- 返回 JSON / 表格数据。
- 为前端提供 WebSocket 任务进度。
- 拒绝不安全请求，例如 broker/order/secrets/shell 直连。

### 4.3 数据库与存储模块

v4 不强制所有数据库一次性接入，可以分阶段完成。

推荐分层：

| 存储 | 用途 | 是否必需 |
|---|---|---|
| Parquet / JSONL | 数据、实验、审计日志的本地默认存储 | 必需 |
| SQLite | 轻量实验记忆与本地开发 | 推荐 |
| Postgres | 服务器端实验记录、用户任务、状态管理 | 可选但推荐 |
| pgvector | RAG 向量检索 | 可选 |
| Neo4j | Agent / Tool / Experiment / Document 关系图谱 | 可选 |

---

## 5. 给 Codex 的 v4 全栈开发提示词

下面提示词用于让 Codex 生成或重构 v4 后端、前端、Agent、Tool、Memory/RAG、测试和部署文件。

```text
你现在是 Quant MAS v4 全栈开发助手。

背景：
- 当前项目是 Quant MAS v3，多智能体量化研究平台。
- v3 已包含确定性 Quant Engine、Agent Orchestration、Memory/RAG、LLM 接入、竞争学习、RL 实验、MCP/A2A 风格协议、Walk-forward OOS、审计日志和 Paper artifact 导出。
- v4 的目标是增加完整全栈界面与服务层，而不是改变安全边界。

任务：
将现有 v3 Quant MAS 系统扩展为 v4 全栈架构，支持：
- 前端 UI 界面
- 后端 API 与服务
- 数据库集成：Postgres + pgvector + Neo4j 可选
- MAS Agent 调用 Tool、Memory/RAG
- LLM 辅助分析与报告生成：DeepSeek / local vLLM / MockLLMClient
- 竞争学习 / 自博弈 / 策略种群
- RL / GRPO / MARL 实验
- 实验工作流 Orchestration：LangGraph / MCP / YAML Pipeline
- 可审计 Walk-forward OOS 实验
- Paper artifact 导出

输出：
- 后端 Python 代码，优先 FastAPI；如果项目已有更合适框架，可说明原因后沿用。
- 前端 React / Vue / Svelte 示例界面，优先 React + Vite。
- Dashboard / Agent 控制台 / Tool 控制台 / Backtest 可视化 / Walk-forward 可视化 / Memory/RAG 查询界面。
- 数据库接口模块：Postgres / pgvector / Neo4j。
- MAS Agent 层调用示例。
- pytest 测试示例。
- Dockerfile 或 docker-compose 示例。
- README Quick Start 更新建议。

前端 UI 必须显示：
- Agent 列表
- Tool 列表
- Memory/RAG 检索
- Backtest / Walk-forward / Risk 结果可视化
- LLM 分析输出
- Population / Self-play 指标
- Audit Logs / Human Confirmation 状态

后端必须提供 REST / WebSocket API：
- 查询实验 / 回测结果
- 调用 Agent / Tool
- 查询 Memory/RAG
- 触发 Paper artifact export
- 返回 JSON / 表格数据
- 通过 WebSocket 返回长任务进度

安全要求：
1. 所有交易候选仍然必须经过 backtest、risk check、audit log、human confirmation。
2. LLM Agent 不允许直接实盘下单。
3. ToolPolicy 必须拒绝 broker / order / secrets / shell 等危险路径。
4. 不允许将 simulation.*、training.*、population.*、audit.* 指标混写成 oos.*。
5. 不虚构实验结果，不承诺收益，不暗示系统具备自动获利能力。

架构分层必须清晰：
Data Layer
→ Quant Engine
→ Backtest / Walk-forward / Risk
→ Memory/RAG
→ Tool Layer
→ Agent Layer
→ Orchestration
→ Outputs / Human Review
→ Frontend UI / Backend API

代码要求：
1. 每个新增模块文件夹内生成示例代码和 README 注释。
2. 英文注释后加中文简述，保持 English first, 中文紧随。
3. 保留现有 src/quant_mas/ 核心代码，不要破坏 v3 测试基线。
4. 新增代码应尽量使用 mock-safe 默认配置，测试不依赖真实网络或真实 LLM API。
5. 先做最小可运行 fullstack skeleton，再逐步接入真实 Quant MAS core。

请生成完整代码结构和示例文件内容，包含：
- src/backend/
- src/frontend/ 或 frontend/
- src/quant_mas/
- tests/
- docs/
- docker-compose.yml
- Dockerfile 示例
- README 更新片段

请在输出前自查：
- 前端是否能调用后端 mock API。
- 后端是否能调用 Quant MAS core 的示例服务。
- 是否保留 v3 安全边界。
- 是否没有虚构实验结果。
- 是否没有投资建议或实盘承诺。
```

---

## 6. 给 Cursor 的 v4 文档与实验提示词

下面提示词用于让 Cursor 更新文档、实验日志、README、UI 说明、部署说明和仓库 polish checklist。

```text
你是 Quant MAS v4 全栈文档和实验助手。

背景：
- 当前项目是 Quant MAS v3，多智能体量化研究平台。
- v4 目标是在 v3 基础上增加全栈 UI、后端 API、数据库服务、Agent/Tool/Memory/RAG 前后端交互、实验可视化和部署文档。
- 文档必须保持科研项目和工程项目的可信感，不虚构实验结果，不暗示 LLM 可以直接交易。

任务 1：文档整理与生成
请更新或生成以下文档：
- docs/progress.md：记录 v4 全栈开发进度。
- docs/architecture.md：更新全栈架构图说明，标注 Frontend / Backend API / Agent / Tool / Memory/RAG / Quant Engine / Database / Human Review。
- docs/experiment_log.md：记录 Walk-forward OOS、RL / Population / Simulation / Training 指标，并严格区分指标族。
- docs/context_engineering.md：记录 LLM 配置、Prompt、DeepSeek / local vLLM / MockLLMClient、可选 LoRA 或微调说明。
- docs/ui_design.md：前端 UI 模块设计、交互示意、页面结构、组件职责。
- docs/database_setup.md：Postgres / pgvector / Neo4j 连接方式、schema 草案和本地开发配置。
- docs/mcp_protocol.md：Orchestration / MCP / A2A / ToolPolicy / Audit JSONL 协议说明。
- docs/fullstack_quickstart.md：本地开发、Docker、服务器部署、前后端联调示例。

任务 2：实验记录
请记录每轮服务器真实训练与回测结果：
- 明确实验 ID。
- 标注是否为 OOS 指标。
- 标注风险约束。
- 标注 Population / Self-play 指标。
- 标注 RL / GRPO / MARL 指标。
- 确保 RL / GRPO / MARL / simulation / training 指标不与 OOS 指标混用。

任务 3：UI 前端文档
请在 docs/ui_design.md 中说明：
- Dashboard
- Agent 列表
- Tool 列表
- Memory/RAG 查询
- Backtest 可视化
- Walk-forward OOS 可视化
- Risk / Audit / Human Review
- Population / Self-play / RL 指标页面
- Paper artifact export 页面
- React / Vue / Svelte 可选方案对比，优先推荐 React + Vite
- English first, 中文紧随

任务 4：检查仓库
请检查：
- 所有 docs 文件路径是否正确。
- README 是否包含最新 v4 全栈介绍。
- README 是否有 Table of Contents、Quick Start、Badge、Contact。
- architecture.png 路径是否正确。
- 是否没有虚构实验结果。
- 是否没有投资建议或实盘承诺。
- 是否清楚表达适合 AI Agent / Quant / Financial AI 实习项目练习。

任务 5：输出
请输出：
- 可直接覆盖的 Markdown 文档。
- 完整 README 更新提示。
- docs/repo_polish_checklist.md：列出已完成 / 待补充 / 高优先级修改。

写作要求：
- English first, 中文紧随。
- 不堆砌长段落。
- 不写收益承诺。
- 不写自动获利暗示。
- 不暗示 LLM 可以直接交易。
- 对实验结果保持保守、真实、可复现。
```

---

## 7. v4 建议目录结构

建议先采用“最小可运行全栈骨架”，不要一次性把所有复杂能力全塞进去。

```text
Quant-MAS/
|-- frontend/
|   |-- package.json
|   |-- vite.config.ts
|   |-- src/
|   |   |-- main.tsx
|   |   |-- App.tsx
|   |   |-- api/
|   |   |-- components/
|   |   |-- pages/
|   |   |   |-- Dashboard.tsx
|   |   |   |-- AgentConsole.tsx
|   |   |   |-- ToolConsole.tsx
|   |   |   |-- MemorySearch.tsx
|   |   |   |-- BacktestView.tsx
|   |   |   |-- WalkForwardView.tsx
|   |   |   |-- AuditReview.tsx
|   |   |   `-- PaperExport.tsx
|   |   `-- styles/
|   `-- README.md
|
|-- src/
|   |-- backend/
|   |   |-- app.py
|   |   |-- api/
|   |   |-- schemas/
|   |   |-- services/
|   |   |-- security/
|   |   `-- README.md
|   |
|   `-- quant_mas/
|       |-- existing v3 core modules
|
|-- tests/
|   |-- test_backend_status.py
|   |-- test_backend_agents.py
|   |-- test_backend_tools.py
|   `-- test_backend_safety.py
|
|-- docs/
|   |-- ui_design.md
|   |-- fullstack_quickstart.md
|   |-- database_setup.md
|   `-- repo_polish_checklist.md
|
|-- docker-compose.yml
|-- Dockerfile.backend
|-- Dockerfile.frontend
`-- README.md
```

---

## 8. v4 执行顺序建议

### Phase 1：全栈最小骨架

目标：证明前端可以调用后端，后端可以返回 Quant MAS 系统状态。

任务：

1. 创建 FastAPI 后端。
2. 创建 React + Vite 前端。
3. 实现 `/api/status`。
4. 前端 Dashboard 显示系统状态、测试基线、OOS 基线、安全边界。
5. 添加最小 pytest。

验收：

- 本地前端能打开。
- 前端能请求后端。
- 后端测试不依赖真实网络。
- README 有全栈 Quick Start。

### Phase 2：Agent / Tool / Memory API

目标：让 UI 可以看到 Agent、Tool、Memory/RAG 检索能力。

当前落地状态：

- 已实现 `GET /api/agents`，返回 mock-safe Agent 列表。
- 已实现 `POST /api/agents/run`，返回受控、可审计的 mock-safe 智能体任务结果。
- 已实现 `GET /api/tools`，返回受控 Tool 列表和允许操作。
- 已实现 `GET /api/memory/search`，返回本地夹具 Memory/RAG 检索结果。
- 前端 Dashboard 已加入 Agents、Controlled Tools、Memory/RAG Search 三个 Phase 2 面板。
- Phase 2 暂不调用真实 LLM、真实 broker、订单接口或 secrets。

任务：

1. `/api/agents` 返回 Agent 列表。
2. `/api/tools` 返回 Tool 列表。
3. `/api/memory/search` 返回 mock-safe 或本地 Memory 检索结果。
4. `/api/agents/run` 支持受控研究任务。
5. 所有调用写入 audit log。

验收：

- Agent Console 能展示任务输入和输出。
- Tool Console 能展示工具调用结果。
- Memory/RAG 页面能查询历史实验或文档。

### Phase 3：Backtest / Walk-forward / Risk 可视化

目标：将已有量化结果以表格和图表展示。

当前落地状态：

- 已实现 `GET /api/backtests/{id}`，返回 research-only、非 OOS 的回测摘要夹具。
- 已实现 `GET /api/oos/{id}`，返回 `EXP-20260602-008` 的 Walk-forward OOS 基线摘要，Sharpe = `0.586`，窗口数 = `19`。
- 已实现 `GET /api/risk/{id}`，返回风险关卡、审计日志要求和人工确认要求。
- 前端 Dashboard 已加入 Backtest Summary、Walk-forward OOS、Risk Review 三个 Phase 3 摘要面板。
- Phase 3 仍然不调用真实交易执行路径，也不把 backtest.summary 写成 oos.* 结论。

任务：

1. `/api/backtests/{id}` 返回回测摘要。
2. `/api/oos/{id}` 返回 walk-forward OOS 摘要。
3. `/api/risk/{id}` 返回风险检查结果。
4. 前端展示指标表、窗口表、风险状态。
5. 明确标注 OOS 与非 OOS 指标。

验收：

- 不混用指标。
- 不把 simulation / training / population 结果写成 OOS 结论。
- UI 中明确显示 “research only / not financial advice”。

### Phase 4：Database / RAG / Deployment

目标：接入可选数据库，并支持服务器部署。

当前落地状态：

- 已实现 `GET /api/database/status`，列出 local_files、SQLite、Postgres、pgvector、Neo4j 的可选接入状态。
- 已实现 `GET /api/deployment/status`，列出 React/Vite 前端、FastAPI 后端和 Docker 产物。
- 已新增 `docker-compose.yml`，包含 backend、frontend、Postgres/pgvector 和 Neo4j 服务骨架。
- 已新增 `Dockerfile.backend` 和 `Dockerfile.frontend`。
- 已新增 `docs/database_setup.md`，说明数据库接入原则和 Docker Compose 使用方式。
- 前端 Dashboard 已加入 Database Backends 和 Deployment Skeleton 面板。
- Phase 4 默认仍使用 local files，不要求单元测试连接真实数据库。

任务：

1. docker-compose 增加 Postgres。
2. 可选增加 pgvector。
3. 可选增加 Neo4j。
4. 增加数据库 schema 文档。
5. 增加 Nginx 或前端静态部署说明。

验收：

- 本地 mock-safe 仍可运行。
- 服务器部署路径清晰。
- 文档说明本地开发和服务器生产环境的区别。

---

## 9. v4 README 更新逻辑框架

README 可以在 v4 开始实施后加入以下新章节或更新点：

1. 顶部 tagline 增加 full-stack research platform。
2. Badge 增加 FastAPI、React/Vite、Postgres optional。
3. Architecture 增加 Frontend UI / Backend API / Database / Deployment。
4. Quick Start 增加：

```bash
python -m pip install -e ".[api]"
python -m uvicorn backend.app:app --reload

cd frontend
npm install
npm run dev
```

5. Features 增加：

- Full-stack UI
- REST / WebSocket API
- Agent Console
- Memory/RAG Search UI
- Backtest Visualization
- Walk-forward OOS Visualization
- Audit / Human Review UI

6. Disclaimer 保持不变并加强：

- Research and education only.
- Not financial advice.
- No direct live trading.
- LLM agents do not place live orders.

---

## 10. v4 自查清单

### 10.1 架构与代码

- [ ] 前端 UI 是否能本地运行。
- [ ] 后端 API 是否能本地运行。
- [ ] 前端是否通过 API 调用后端，而不是直接读写核心文件。
- [ ] 后端是否通过服务层调用 `src/quant_mas/`。
- [ ] Agent 是否只能通过 ToolRegistry 调用工具。
- [ ] ToolPolicy 是否拒绝 broker / order / secrets / shell 等危险路径。
- [ ] WebSocket 是否只用于任务状态和日志流，不用于绕过安全策略。

### 10.2 实验与指标

- [ ] 是否只把 audited walk-forward OOS 作为论文级结论。
- [ ] 是否区分 `oos.*`、`simulation.*`、`training.*`、`population.*`、`audit.*`。
- [ ] 是否没有虚构实验结果。
- [ ] 是否没有把回测或模拟结果描述成真实收益。

### 10.3 文档与展示

- [ ] README 是否包含 v4 全栈介绍。
- [ ] README 是否包含 Table of Contents。
- [ ] README 是否包含 Quick Start。
- [ ] README 是否保留 Contact。
- [ ] `architecture.png` 路径是否正确。
- [ ] docs 是否新增 `ui_design.md` 和 `fullstack_quickstart.md`。
- [ ] 是否清楚表达适合 AI Agent / Quant / Financial AI 实习项目练习。

### 10.4 安全与合规

- [ ] 是否没有投资建议。
- [ ] 是否没有实盘承诺。
- [ ] 是否没有收益承诺类表述或自动获利暗示。
- [ ] 是否明确写出 LLM Agent 不直接下单。
- [ ] 是否明确写出所有交易候选必须经过 backtest、risk check、audit log、human confirmation。

---

## 11. 最终建议

v4 不建议一开始就追求“大而全”的前端，而应该先做一个最小闭环：

```text
Dashboard
→ Backend /api/status
→ Quant MAS core status
→ Agent / Tool list
→ Memory search mock
→ Backtest / OOS result mock or local fixture
→ Audit / Human Review placeholder
```

等最小闭环稳定后，再逐步接入真实 ExperimentMemory、真实 Backtest、真实 Walk-forward OOS、真实 RAG、真实 Agent 调用和服务器部署。

这样既能保持工程可控，也更适合作为实习项目、科研项目和答辩展示项目继续推进。

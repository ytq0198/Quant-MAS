# Quant MAS v3 设计（研究与工程升级规划）

更新时间：2026-06-01

> 本文档在 **Plus v2 收官**（M1–M8 ✅，195 pytest 本地+服务器，见 [`项目进度.md`](项目进度.md) §Plus v2 收官）基础上，规划 **Quant MAS v3** 的研究升级与工程扩展路线。  
> 与现有文档关系：
> - [`项目进度.md`](项目进度.md) — 记录「已完成什么」
> - [`项目plus设计.md`](项目plus设计.md) — v2（M1–M8）原始规划（**已收官**）
> - **本文档** — v3「接下来做什么、怎么给 Codex / Cursor 下指令」

---

## 目录

1. [定位与原则](#1-定位与原则)
2. [v2 基线（不可遗忘）](#2-v2-基线不可遗忘)
3. [v3 核心目标](#3-v3-核心目标)
4. [v3 模块分层](#4-v3-模块分层)
5. [v3 主线与优先级（M9–M13）](#5-v3-主线与优先级m9m13)
6. [M9：企业数据与数据库](#m9企业数据与数据库)
7. [M10：LLM 生产化与文本增强](#m10llm-生产化与文本增强)
8. [M11：竞争学习 / 自博弈 / 策略种群](#m11竞争学习--自博弈--策略种群)
9. [M12：强化学习训练实验](#m12强化学习训练实验)
10. [M13：企业化编排与协议扩展](#m13企业化编排与协议扩展)
11. [Codex 提示词模板（按模块）](#11-codex-提示词模板按模块)
12. [Cursor 维护清单](#12-cursor-维护清单)
13. [API / 服务一览](#13-api--服务一览)
14. [总执行路线表](#14-总执行路线表)
15. [文献与科研方向备忘](#15-文献与科研方向备忘)

---

## 1. 定位与原则

### 1.1 从 v2 研究平台到 v3 企业化研究系统

| 维度 | v2（已收官） | v3（本文档） |
|------|-------------|-------------|
| 目标 | 可运行、可测试、可比较、八层骨架落地 | **真实数据规模化**、**企业级存储**、**多 Agent 竞争**、**RL 训练闭环** |
| 数据 | Parquet + 多 fetcher；6033 行服务器样本 | Postgres 持久化 + 全量 walk-forward 复跑 |
| Memory/RAG | JSON / SQLite + InMemory 向量 + HybridRetriever | **Postgres + pgvector + Neo4j** 三元存储 |
| LLM | Mock 默认 + DeepSeek smoke（EXP-LLM-001） | **本地 vLLM** + 云端 DeepSeek；报告 / 解释 / 文本分析 |
| Agent | Supervisor 规则路由 + 7 tools | **策略 Agent 池** + Population Controller + 竞争评分 |
| RL | TradingEnv + baseline + GRPO **ranking**（simulation） | GRPO / PPO / **MARL** **训练循环** + league |
| 编排 | LangGraph 6 节点 DAG + MCP 内部 adapter | DAG **调度多实验** + 任务流日志 + 权限策略 |
| 交易 | 回测 + 风控，**不做实盘** | 仍 **不做实盘**；竞争 / RL 均在模拟或 OOS 框架内 |

### 1.2 硬性原则（全程遵守）

1. **LLM / Agent 不直接下单**；所有仓位建议须经回测、RiskTool、审计、人工确认。
2. **论文/报告主指标** 必须用 Walk-forward **OOS**（baseline：**oos.sharpe 0.586**，EXP-20260602-008）；`simulation.*`、单段 ml_backtest、Population Elo **不可**替代 OOS 结论。
3. **pytest 默认不联网、不调真实 LLM、不连真实 Postgres/Neo4j**；集成测试用 mock / testcontainers 或 `@pytest.mark.integration` 单独标记。
4. **API Key / DB 密码只放 `.env`**，不入 git、不写进日志、不写入 AgentCard。
5. **一次只交给 Codex 一个 v3 模块**（建议 M9→M13 顺序），每步全量 `python -m pytest -v` 通过后再进下一步。
6. **不虚构实验结果**；服务器真实跑数后由 Cursor 写入 `docs/experiment_log.md`。
7. **不破坏 v2 后端**：JSON / SQLite Memory、现有 7 tools、Supervisor 路由、195 pytest 基线必须保持可回退。

### 1.3 三端分工

| 角色 | 职责 |
|------|------|
| **Codex** | 写 Python 模块、mock 测试、CLI 骨架、configs 示例 |
| **Cursor** | 文档维护、服务器 SSH、真实 DB / GPU / API 实验、EXP 记录 |
| **ChatGPT** | 拆任务、审查方案与 v3 设计、论文结构 |
| **GitHub** | 同步代码（不含大数据、模型权重、`.env`） |
| **服务器 a6000** | Postgres/pgvector、vLLM、GPU RL、全量 walk-forward、FinBERT/LoRA |

---

## 2. v2 基线（不可遗忘）

### 2.1 工程完成度

| 项目 | 状态 |
|------|------|
| Plus M1–M8 | ✅ 本地 + 服务器 |
| pytest | **207 passed**（EXP-025 本地）；**212 passed** 服务器（EXP-028） |
| 7 Quant Tools | data_summary / backtest / train_model / report / risk_check / ml_backtest / pipeline |
| Walk-forward OOS | **sharpe 0.586**（19 窗，EXP-20260602-008） |
| Text + OOS | sharpe **0.563** vs 0.586（EXP-TEXT-WF-001，exploratory） |
| RL simulation | TradingEnv + GRPO ranking；`simulation.*` only |
| MCP/A2A | 内部 adapter；export_agent_cards；deny shell/broker/secrets |

### 2.2 v3 继承的关键路径

```text
src/quant_mas/
├── data/fetchers/          # M2 — v3 继续扩展真实数据源
├── memory/                 # M3 — v3 增 Postgres / Neo4j 后端
├── rag/                    # M3 — v3 增 pgvector 持久化
├── context/ + core/llm.py  # M5 — v3 增 local_vllm
├── text/ + features/text_signals.py  # M6 — v3 扩大覆盖与 LoRA
├── rl/                     # M7 — v3 增 training loop / MARL
├── protocols/              # M8 — v3 增 DAG 调度与 Agent 通信
├── research/               # M1 — 所有 v3 实验须 compare 到 OOS 0.586
└── orchestration/          # M4 — v3 扩展多实验 Scheduler
```

---

## 3. v3 核心目标

### 3.1 真实数据落地

- 多数据源：**OHLCV**（Stooq / Alpha Vantage / Finnhub）、**宏观**（FRED）、**监管**（SEC EDGAR）、**新闻/文本**（后续扩展）
- 数据库持久化：**Postgres**（实验与指标）+ **pgvector**（文档/报告嵌入）+ **Neo4j**（策略–特征–实验关系图）
- **Walk-forward OOS 全量实验**：在 v3 数据规模下复跑 baseline，与 EXP-20260602-008 对照

### 3.2 企业化 RAG / Memory

- 将 JSON / SQLite 升级到可选 **Postgres + pgvector + Neo4j**
- 支持：向量检索、嵌套 metric 查询（`oos.sharpe`）、实验回溯、报告检索、策略 lineage 图查询
- **HybridRetriever v3**：keywords + vector + **graph query**

### 3.3 LLM 接入与微调

- **本地 vLLM**（a6000 OpenAI 兼容端点）+ **云端 DeepSeek**
- 用途：文本信号分析、策略解释、研究报告生成
- **不直接下单**，只做分析和建议；metrics 仍以 Quant Engine 为准

### 3.4 竞争学习 / 自博弈 / 策略种群

- **策略 Agent 池**：MomentumAgent、MeanReversionAgent、MLSignalAgent、TextSignalAgent、RiskAgent 等
- 同一市场环境（TradingEnv / walk-forward 窗口）下对抗或协作
- **评分**：Sharpe、MaxDrawdown、OOS Reward、**Elo** 排名
- **种群演化**：Top-K 保留 → 参数扰动 / 策略组合 → **autocurriculum**

### 3.5 强化学习实验

- **GRPO / PPO / MARL** 在 MarketEnv 中训练
- Agent 间竞争或协作；回测后记录 OOS reward、Sharpe、Drawdown
- 与 M11 竞争框架共享 Population / League 基础设施

### 3.6 企业化 Orchestration

- LangGraph DAG + MCP / A2A adapter
- **实验 Scheduler**：批量 walk-forward、Population 实验、RL 训练任务
- 工具调用、权限管理、任务流 **日志化**（audit trail）

---

## 4. v3 模块分层

```text
Quant MAS v3
│
├── Data Layer（M9）
│   ├── Fetchers: Stooq, AlphaVantage, Finnhub, FRED, SEC, （新闻待扩展）
│   ├── Database: Postgres + pgvector + Neo4j
│   └── Preprocessing: feature pipelines, label pipelines, text merge
│
├── Quant Engine（v2 继承 + v3 增强）
│   ├── ML: LightGBM / XGBoost（可选）/ LoRA 文本模型
│   ├── RL Env: TradingEnv（多 Agent 并发扩展）
│   └── Backtest: walk-forward OOS（主指标不变）
│
├── Agent Layer — MAS（M11）
│   ├── StrategyAgents: momentum, mean-reversion, MLSignal, TextSignal
│   ├── RiskAgent
│   ├── SupervisorAgent / ResearchAgent / ReportAgent（v2）
│   └── Population / Competition Controller
│
├── Memory / RAG Layer（M9）
│   ├── ExperimentMemory: Postgres | JSON | SQLite（可切换）
│   ├── VectorStore: pgvector | InMemory | FAISS
│   ├── GraphStore: Neo4j（策略–实验–特征）
│   └── HybridRetriever v3: keywords + vector + graph
│
├── LLM Layer（M10）
│   ├── resolve_llm_client: mock | deepseek | local_vllm
│   └── ResearchAgent / ReportAgent / TextSignal 解释
│
├── RL Training Layer（M12）
│   ├── grpo_agent / ppo_agent（骨架）
│   ├── training_loop / league
│   └── run_rl_experiment.py
│
└── Orchestration Layer（M13）
    ├── LangGraph DAG（多实验节点）
    ├── MCP / A2A adapter（v2 扩展）
    ├── Experiment Scheduler / Logger
    └── run_mcp_pipeline.py / run_competitive_experiment.py
```

---

## 5. v3 主线与优先级（M9–M13）

| 编号 | 名称 | v2 延伸 | 优先级 | 关键交付 |
|------|------|---------|--------|----------|
| **M9** | 企业数据与数据库 | M2 + M3.5 | **P0** | ✅ 本地 EXP-025 | PostgresStore、pgvector、Neo4jStore、factory 切换 |
| **M10** | LLM 生产化 | M5.5 + M6 | **P0** | ✅ 本地+服务器+LLM-002 | `local_vllm`、212 passed | [codex_prompt_M10.md](docs/codex_prompt_M10.md) |
| **M11** | 竞争学习 / 策略种群 | M7 延伸 | **P1** | ✅ EXP-029/POP-002 | [codex_prompt_M11.md](docs/codex_prompt_M11.md) |
| **M11.5** | 种群训练闭环 | M11 延伸 | **P1** | ✅ EXP-030/POP-003，237 双端 | [population_training.md](docs/population_training.md) |
| **M11.6** | Population → OOS 候选验证桥 | M11.5 + Walk-forward | **P1** | ✅ EXP-031/POP-004，248 双端 | [strategy_candidate_bridge.md](docs/strategy_candidate_bridge.md) |
| **M11.7** | StrategyCandidate Walk-forward OOS | M11.6 + WF hook | **P1** | ✅ EXP-032 本地，259 pytest | [strategy_candidate_oos.md](docs/strategy_candidate_oos.md) |
| **M12** | RL 训练实验 | M7 延伸 | **P1** | GRPO/PPO/MARL training loop、GPU smoke |
| **M13** | 企业化编排 | M4 + M8 延伸 | **P2** | 多实验 DAG Scheduler、Agent 通信、audit log |

**建议顺序**：M9 → M10 → M11 → M11.5 → M11.6 → **M11.7** → M12 → M13。  
**M11.6 的定位**：Top-K → `StrategyCandidate` + synthetic backtest smoke（不写 `oos.*`）。  
**M11.7 的定位**：候选策略接入 walk-forward OOS hook，**唯一**允许从候选链路写入 `oos.*`；与 EXP-20260602-008（**0.586**）对比。

**v2 已规划但未做的按需项**（并入 v3）：

| v2 编号 | 内容 | v3 归属 |
|---------|------|---------|
| M3.5 | Postgres / pgvector / 真 Embedding | **M9** |
| M5.5 | 本地 vLLM | **M10** |
| EXP-TEXT-WF-002 | 扩大 text JSONL + walk-forward | **M10** 科研子任务 |

---

## M9：企业数据与数据库

### 目标

- Postgres 持久化实验记录与嵌套 metrics
- pgvector 存储文档/报告 embedding
- Neo4j 存储策略–特征–实验关系，支持 lineage 与对比查询

### 交付物

| 组件 | 路径 |
|------|------|
| Postgres Memory | `src/quant_mas/memory/postgres_store.py` |
| Neo4j Graph | `src/quant_mas/memory/neo4j_store.py` 或 `graph/neo4j_client.py` |
| pgvector RAG | `src/quant_mas/rag/vector_store_pgvector.py` |
| Factory 扩展 | `memory/factory.py` — `backend: json \| sqlite \| postgres` |
| 配置 | `configs/memory.enterprise.yaml.example` |
| CLI | `index_documents.py` / `query_memory.py` 支持 `--backend postgres` |
| 文档 | `docs/database_setup.md` 增量 |
| 测试 | `tests/test_memory_enterprise.py`（mock DB，不联网） |

### 验收标准

- [x] factory 切换 JSON / SQLite / Postgres，v2 行为不变
- [x] 嵌套 metric 查询 `find_best("oos.sharpe")` 在 Postgres 后端通过
- [x] pgvector upsert + search smoke（mock）
- [x] Neo4j 写入实验节点 + 策略边（mock driver）
- [x] 全量 pytest 通过（**207 passed**，+12）
- [x] 服务器 EXP：Postgres 真实连接 + index **443 chunks**（**EXP-20260602-026** ✅ 2026-06-03）

**记录**：**EXP-20260602-025**（本地 207 passed，enterprise 12/12，2026-06-01）

### 边界

- 默认 pytest 仍用 SQLite / InMemory；Postgres/Neo4j 仅 integration 或服务器
- 不迁移历史 parquet 入 git；迁移脚本放 `scripts/` 可选

---

## M10：LLM 生产化与文本增强

### 目标

- `resolve_llm_client` 支持 `local_vllm`（OpenAI 兼容，`VLLM_BASE_URL`）
- ResearchAgent / ReportAgent 生产路径文档化
- 文本信号：扩大 JSONL 覆盖 → **EXP-TEXT-WF-002** walk-forward 复跑
- LoRA 微调流程（FinBERT / text-classifier）与 M6 骨架对接

### 交付物

| 组件 | 路径 |
|------|------|
| LLM 解析 | `core/llm.py` — `provider: mock \| openai_compatible \| local_vllm` |
| ResearchAgent | `agents/research_agent.py` — `use_llm=True` 生产配置 |
| 文本 | `features/text_signals.py` + `text/lora_finetune.py` 增强 |
| 配置 | `configs/llm.server.yaml.example`（vLLM endpoint） |
| 文档 | `docs/context_engineering.md` — vLLM env、DeepSeek、注意事项 |
| 测试 | `tests/test_context_engineering.py` — vLLM mock HTTP smoke |

### 验收标准

- [x] 无 key / 无 VLLM_BASE_URL 时回退 Mock；local_vllm mock HTTP 测试通过
- [x] LLM 输出不覆盖 Quant Engine metrics
- [ ] EXP-TEXT-WF-002：扩大 text 覆盖后 OOS 与 0.586 对比（Cursor 科研，待做）
- [x] pytest 全绿 — **212 passed**（+5）

**记录**：**EXP-20260602-027**（本地 212 passed，context 17/17，2026-06-01）

---

## M11：竞争学习 / 自博弈 / 策略种群

### 目标

- 多 Agent 同时在 MarketEnv（或 walk-forward 窗口序列）运行
- Agent Pool + PopulationManager + Elo / Top-K / autocurriculum
- 实验记录入 BaselineRegistry + ExperimentMemory

### Agent Pool（第一版）

| Agent | 策略逻辑 | 输入 |
|-------|----------|------|
| MomentumAgent | 趋势 / 动量 | OHLCV features |
| MeanReversionAgent | 均值回归 | OHLCV features |
| MLSignalAgent | LightGBM 概率 → 权重 | ML features |
| TextSignalAgent | 文本 sentiment | text_signals columns |
| RiskAgent | 裁剪 / 否决 | target_weight + limits |

### 交付物

| 组件 | 路径 |
|------|------|
| Population | `src/quant_mas/agents/population_manager.py` |
| Strategy Agent | `src/quant_mas/agents/strategy_agent.py` — `propose()` / `evaluate()` |
| Multi-agent Env | `src/quant_mas/rl/trading_env.py` — 多 Agent 槽位（扩展 v2） |
| CLI | `scripts/run_competitive_experiment.py` |
| 文档 | `docs/competitive_learning.md` |
| 测试 | `tests/test_population_training.py` — mock 小数据 smoke |

### 评分与演化

```text
每窗口 / 每 episode:
  raw_metrics → oos.sharpe, max_drawdown, custom_reward
  → Elo 更新（相对排名）
  → Top-K 保留
  → mutate / crossover / ensemble → 下一代候选（autocurriculum）
```

### 验收标准

- [x] 2+ Agent mock 竞争一轮，写出 ExperimentMemory 记录（非 dry-run）
- [x] Elo 排名 deterministic（同 v2 GRPO tie-break 风格）
- [x] walk-forward 多窗口模式可选（`--mode mock` 默认；walk_forward stub）
- [x] 与 EXP-20260602-008 OOS 0.586 比较表可生成（文档对照；不写 oos.sharpe）
- [x] pytest 全绿 **225 passed**；**不**声称 Population Elo 等于 OOS 主指标

**记录**：**EXP-20260602-029**（本地 225 passed，population 13/13）· **EXP-POP-002**（服务器 225 passed，2026-06-03）

---

## M11.5：种群训练闭环

### 目标

- 在 M11 单轮 competitive mock 之上，运行 **多代** Top-K + mutation 训练闭环
- 复用 `PopulationManager`、`CompetitiveRunner`、Elo；为 M12 RL 训练提供骨架

### 交付物

| 组件 | 路径 |
|------|------|
| Training loop | `src/quant_mas/rl/population_training.py` — `PopulationTrainingLoop` |
| CLI | `scripts/run_population_training.py` |
| 配置 | `configs/population_training.yaml` |
| 文档 | `docs/population_training.md` |
| 测试 | `tests/test_population_training_loop.py` — **12** 项 |

### 验收标准

- [x] 3 代 mock 闭环（initial → eval → Top-K → mutation → next gen）
- [x] dry-run 不写 memory/artifacts；非 dry-run 写 generation metrics + ExperimentMemory
- [x] 指标仅 `population.*` / `simulation.*`；**无** `oos.*`
- [x] pytest 全绿 **237 passed**（225→237，+12）；**不**声称 simulation sharpe 等于 OOS 主指标

**记录**：**EXP-20260602-030**（本地 237 passed，loop 12/12）· **EXP-POP-003**（服务器 237 passed + 3-gen dry-run，41.83s @ `aa841d4`，2026-06-03）

---

## M11.6：Population → OOS 候选验证桥

### 定位

M11 / M11.5 已经能产生 `population.*` / `simulation.*` 排名，但这些指标仍然只是**策略发现与筛选指标**。M11.6 的目标是补上最关键的研究桥梁：

```text
Population Top-K
  → StrategyCandidate（可序列化候选）
  → deterministic strategy / signal adapter
  → BacktestEngine smoke
  → 可选 Walk-forward OOS hook（第一版 dry-run / stub）
  → compare_experiments / baseline 对照
```

M11.6 **不是**真实 MARL 训练，也不把 Elo 转成论文结论；它只负责把“模拟里表现好的 agent”交给 Quant Engine 做正式验证。

### 目标

- 将 `PopulationTrainingLoop` 的 final rankings / Top-K 导出为可审计 `StrategyCandidate`
- 对候选做小规模 deterministic backtest smoke，证明能进入 Quant Engine
- 预留 walk-forward OOS hook，但第一版不虚构 OOS 数字
- 在报告中明确区分：
  - `population.*` / `simulation.*`：候选筛选指标
  - `backtest.*`：候选单段 smoke 指标
  - `oos.*`：只有真实 walk-forward hook 跑完才允许写

### 交付物

| 组件 | 路径 |
|------|------|
| Candidate schema | `src/quant_mas/research/strategy_candidate.py` |
| Candidate exporter | `src/quant_mas/rl/candidate_bridge.py` |
| CLI | `scripts/export_population_candidates.py` |
| 配置 | `configs/candidate_validation.yaml` |
| 文档 | `docs/strategy_candidate_bridge.md` |
| 测试 | `tests/test_strategy_candidate_bridge.py` |

### 第一版行为

1. 读取 `PopulationTrainingLoop` 结果 JSON 或直接运行 `run_population_training_from_config(..., dry_run=True)`。
2. 提取 Top-K agent specs，转为：

```python
StrategyCandidate(
    candidate_id="cand_mean_rev_1",
    source="population_training",
    agent_id="mean_rev_1",
    agent_type="mean_reversion",
    params={"lookback": 5, "scale": 1.0},
    selection_metrics={
        "population.elo": 1500.0,
        "simulation.reward_mean": 7.12,
    },
)
```

3. 写出 `candidates.json` / `candidates.csv`。
4. 可选执行 `--run-backtest-smoke`：
   - 用 synthetic OHLCV
   - 将 Momentum / MeanReversion candidate 转成 target_weight 序列
   - 走 `BacktestEngine` 或现有策略适配器
   - 输出 `backtest.*` metrics
5. `--run-walk-forward` 第一版只做 stub：
   - 打印清晰 warning
   - 不写 `oos.*`
   - 文档说明真实 OOS hook 留给服务器科研任务

### 验收标准

- [x] Top-K candidates 可从 population training dry-run 结果导出
- [x] `candidates.json` / `candidates.csv` 可写入
- [x] synthetic backtest smoke 可跑通，并写 `backtest.*`
- [x] 默认不写 `oos.*`
- [x] `--run-walk-forward` stub 不虚构 OOS 数字
- [x] ExperimentMemory 记录 family=`strategy_candidate_validation`
- [x] pytest 全绿 **248 passed**（237→248，+11）；不接 broker、不联网、不调 LLM

**记录**：**EXP-20260602-031**（本地 248 passed，bridge **11/11**）· **EXP-POP-004** ✅ 服务器 **248 passed**（55.15s）+ export dry-run（2026-06-03 @ `7ab510f`）

### 科研意义

M11.6 是竞争学习模块从“演示”走向“服务主研究闭环”的关键步骤。它让 Population 不再只是输出 Elo 排名，而是输出一组可以被正式量化流程验证的候选策略。论文中可以把它描述为：

> A simulation-only candidate generation layer whose selected candidates are subsequently validated by deterministic backtesting and walk-forward OOS evaluation.

---

## M11.7：StrategyCandidate Walk-forward OOS Validation

### 定位

M11.6 的 `backtest.*` 只是 synthetic smoke。**M11.7** 才是候选策略接入真实 walk-forward 窗口、产出 `oos.*` 的第一版 hook：

```text
StrategyCandidate
  → CandidateStrategyAdapter（momentum / mean_reversion）
  → build_walk_forward_windows（复用，不改 ML walk_forward.py）
  → OOS BacktestEngine
  → oos.* + vs_baseline_sharpe
  → ExperimentMemory / artifacts
```

分两层推进：

| 层 | 环境 | 数据 | 状态 |
|----|------|------|------|
| mock-first | 本地 pytest | synthetic features | ✅ EXP-032 |
| 真实 OOS | 服务器 | 真实 `features.parquet` vs **0.586** | ✅ EXP-POP-005（`oos.sharpe` **1.036**） |

### 交付物

| 组件 | 路径 |
|------|------|
| OOS validation | `src/quant_mas/research/candidate_validation.py` |
| CLI | `scripts/validate_candidate_oos.py` |
| 配置 | `configs/candidate_oos.yaml` |
| 文档 | `docs/strategy_candidate_oos.md` |
| 测试 | `tests/test_candidate_oos_validation.py` — **11** 项 |

### 验收标准

- [x] `CandidateStrategyAdapter` 支持 momentum / mean_reversion；拒绝 `future_*` 信号列
- [x] `run_candidate_walk_forward` 产出 `oos.*`、`summary.vs_baseline_sharpe`
- [x] `save_candidate_validation_report` 写 5 个 artifact 文件
- [x] CLI dry-run / non-dry-run + ExperimentMemory（family=`strategy_candidate_oos`）
- [x] 不修改现有 ML `walk_forward.py` 逻辑
- [x] pytest 全绿 **259 passed**（248→259，+11）；不接 broker / LLM / 网络 / 训练

**记录**：**EXP-20260602-032**（本地 259）· **EXP-POP-005** ✅ 服务器 `cand_mean_rev_1` **oos.sharpe 1.036** vs ML baseline **0.586**（77 窗，2026-06-04 @ `ffef849`）

---

## M12：强化学习训练实验

### 目标

- 在 M7 `TradingEnv` 上实现最小 GRPO / PPO 训练 loop（**simulation only**）
- 从 M7 的 GRPO ranking 推进到可更新的 `PolicyState`
- 与 M11 共享候选导出 / OOS 验证边界，但 **M12 训练 loop 不写 `oos.*`**
- 服务器可做短训练 smoke；metrics 严格区分 `training.*` / `simulation.*` / `oos.*`

### 阶段拆分

| 阶段 | 内容 | 边界 |
|------|------|------|
| **M12.1** | `GRPOPolicyAgent` + `RLTrainingLoop` + checkpoint | synthetic / short training / simulation only |
| **M12.2** | trained policy → `StrategyCandidate` stub | 只做桥接提示，不复制 walk-forward |
| **M12.3** | 服务器 `--max-steps 50` smoke | 仍只写 `training.*` / `simulation.*` |

M12 的科研角色是“可审计 RL 训练原型”。若训练后的 policy 要进入论文 OOS ablation，必须经过：

```text
M12 training
  -> StrategyCandidate export / mapping
  -> M11.7 validate_candidate_oos 或 M11.8 batch_validate_candidates
  -> oos.* metrics
```

### 交付物

| 组件 | 路径 |
|------|------|
| GRPO Agent | `src/quant_mas/rl/grpo_agent.py` |
| Training | `src/quant_mas/rl/training_loop.py` |
| PPO stub | `src/quant_mas/rl/ppo_trainer.py` |
| MARL stub | `src/quant_mas/rl/marl_stub.py` |
| 配置 | `configs/rl_training.yaml` |
| CLI | `scripts/run_rl_experiment.py` |
| 文档 | `docs/rl_experiment.md` |
| 测试 | `tests/test_rl_training.py` — 小规模 mock env |

### 验收标准

- [ ] mock 环境 10 step 训练 loop 不 crash
- [ ] checkpoint / metrics.json 写入 outputs/
- [ ] `training.*` / `simulation.*` metrics 完整，且不含任何 `oos.*`
- [ ] PPO / MARL stub 路径可测试
- [ ] 训练后 OOS 仅通过 stub 指向 M11.7/M11.8，不在 M12 内执行
- [ ] pytest 全绿；不直接下单；不调用 LLM / broker / 网络

---

## M13：企业化编排与协议扩展

### 目标

- MCP / A2A adapter 支持多 Agent 消息与实验调度（**仍不接外部 MCP server 监听**）
- LangGraph DAG 扩展：Population 实验、RL 训练、批量 walk-forward 作为节点
- 任务流 audit log

### 交付物

| 组件 | 路径 |
|------|------|
| 通信 | `src/quant_mas/orchestration/agent_communication.py` |
| MCP 扩展 | `src/quant_mas/orchestration/mcp_scheduler.py`（或扩展 `protocols/mcp/`） |
| CLI | `scripts/run_mcp_pipeline.py` |
| 文档 | `docs/mcp_protocol.md`（v3 调度语义，区别于 v2 `docs/protocols.md`） |
| 测试 | `tests/test_mcp_scheduler.py` |

### 验收标准

- [ ] DAG dry-run 调度 2 个 mock 实验节点
- [ ] ToolPolicy 仍 deny shell/broker/order/secrets
- [ ] audit log JSONL 可查询
- [ ] pytest 全绿

---

## 11. Codex 提示词模板（按模块）

> 使用时复制对应模块整段给 Codex；**一次只做一个模块**；完成后 Cursor 更新文档与 EXP。

### 11.1 M9 — 数据库 + 数据源

```markdown
请为 Quant MAS 实现 v3 模块 **M9：企业数据与数据库**。

## 背景
- v2 已收官：195 pytest，Memory 支持 JSON/SQLite，RAG 为 InMemoryVectorStore + HybridRetriever。
- 论文 OOS baseline：oos.sharpe 0.586（EXP-20260602-008）。
- 参考：项目v3设计.md §M9、docs/database_setup.md、src/quant_mas/memory/factory.py。

## 目标
- Postgres 持久化实验与嵌套 metrics
- pgvector 向量嵌入存储
- Neo4j 策略–特征–实验图关系（可选 mock driver 测）

## 任务
1. `src/quant_mas/memory/postgres_store.py` — 实现 MemoryStore，API 对齐 SqliteMemoryStore
2. `src/quant_mas/memory/neo4j_store.py` — 实验/策略节点与边 CRUD 骨架
3. `src/quant_mas/rag/vector_store_pgvector.py` — VectorStore 实现
4. 扩展 `memory/factory.py` — `backend: json | sqlite | postgres`
5. `scripts/index_documents.py` / `query_memory.py` — `--backend` 参数
6. `configs/memory.enterprise.yaml.example`
7. `tests/test_memory_enterprise.py` — mock，不联网
8. 增量更新 `docs/database_setup.md`

## 要求
- 不破坏现有 JSON/SQLite 与 195 pytest
- 支持嵌套 metric 查询（oos.sharpe）
- API key / DB URL 只读环境变量，不入 git
- 完成后 `python -m pytest -v` 全量通过
```

### 11.2 M10 — LLM + 文本

```markdown
请为 Quant MAS 实现 v3 模块 **M10：LLM 生产化与文本增强**。

## 背景
- v2：resolve_llm_client、ResearchAgent、DeepSeek smoke（EXP-LLM-001）。
- M6：FinBERT smoke + EXP-TEXT-WF-001（OOS 0.563 vs 0.586）。
- 参考：项目v3设计.md §M10、docs/context_engineering.md、configs/llm.yaml。

## 目标
- 支持 DeepSeek 云端 + 本地 vLLM（OpenAI 兼容端点）
- 文本信号分析、策略解释、报告生成；不直接下单
- LoRA 微调骨架与 train_text_model.py 对接

## 任务
1. `core/llm.py` — `local_vllm` provider（VLLM_BASE_URL / VLLM_MODEL）
2. `agents/research_agent.py` — 生产配置与错误回退 Mock
3. 增强 `text/lora_finetune.py` 与 `features/text_signals.py` 文档化边界
4. `configs/llm.server.yaml.example`
5. `tests/test_context_engineering.py` — vLLM mock HTTP
6. 增量更新 `docs/context_engineering.md`

## 要求
- LLM key 不入 git；pytest 默认 Mock
- LLM 不覆盖 Quant Engine metrics
- 完成后 `python -m pytest -v` 全量通过
```

### 11.3 M11 — 竞争学习 / 策略种群

```markdown
请为 Quant MAS 实现 v3 模块 **M11：竞争学习 / 自博弈 / 策略种群**。

## 背景
- v2：TradingEnv、baseline_policy、grpo_experiment（ranking only，simulation.*）。
- 参考：项目v3设计.md §M11、src/quant_mas/rl/、research/baseline.py。

## 目标
- 多 Agent 同时在 MarketEnv 运行（mock 并发即可）
- Agent Pool：Momentum / MeanReversion / MLSignal / TextSignal / Risk
- 评分：Sharpe、MaxDrawdown、OOS Reward、Elo；Top-K + autocurriculum 骨架

## 任务
1. `agents/population_manager.py`
2. `agents/strategy_agent.py` — 基类 + 2 个示例子类（如 Momentum、MLSignal）
3. 扩展 `rl/trading_env.py` — 多 agent 槽位（保持 v2 单 agent 测试兼容）
4. `scripts/run_competitive_experiment.py` — `--dry-run` / `--mode mock`
5. `docs/competitive_learning.md`
6. `tests/test_population_training.py`

## 要求
- 支持 walk-forward 多窗口模式（mock 数据）
- 实验写入 ExperimentMemory + 可选 BaselineRegistry
- Elo / Population 分数不等于论文 OOS 主指标（文档说明）
- pytest 全绿
```

### 11.4 M11.6 — Population → OOS 候选验证桥

```markdown
请为 Quant MAS v3 实现 **M11.6：Population → OOS 候选验证桥**。

## 背景
- M11 已完成 StrategyAgent / PopulationManager / CompetitiveEpisodeRunner。
- M11–M11.6 竞争学习链路双端完成：PopulationTrainingLoop → StrategyCandidate → backtest smoke；pytest 基线 **248 passed**。
- 当前竞争学习指标仍是 `population.*` / `simulation.*`，不能替代论文主指标。
- 论文 OOS baseline：EXP-20260602-008，`oos.sharpe = 0.586`。

## 目标
把 Population Top-K 输出转成可审计、可复现、可进入 Quant Engine 验证的策略候选：

```text
PopulationTrainingLoop result
  → StrategyCandidate
  → candidates.json / candidates.csv
  → synthetic backtest smoke
  → optional walk-forward hook stub
  → ExperimentMemory / comparison artifacts
```

第一版只做 candidate export + synthetic backtest smoke + walk-forward stub，不跑真实服务器 OOS，不虚构任何 `oos.*`。

## 需要实现

1. `src/quant_mas/research/strategy_candidate.py`
   - `StrategyCandidate` dataclass
   - 字段：
     - candidate_id
     - source
     - agent_id
     - agent_type
     - params
     - selection_metrics
     - validation_metrics
     - artifacts
     - notes
   - `to_dict()` / `from_dict()`
   - helper：`assert_no_oos_metrics(metrics)`

2. `src/quant_mas/rl/candidate_bridge.py`
   - `extract_top_candidates(population_result, top_k=2) -> list[StrategyCandidate]`
   - `write_candidates(candidates, output_dir) -> dict[str, str]`
   - `run_candidate_backtest_smoke(candidate, market_data=None) -> dict`
   - Momentum / MeanReversion candidate 转为 target_weight 序列，走 deterministic backtest smoke
   - backtest 指标写入 `backtest.*`
   - 禁止写入 `oos.*`

3. `scripts/export_population_candidates.py`
   - 参数：
     - `--population-config configs/population_training.yaml`
     - `--input-result` 可选：读取已有 population result JSON
     - `--top-k`
     - `--output-dir`
     - `--run-backtest-smoke`
     - `--run-walk-forward`（stub only，warning，不写 oos）
     - `--memory-path`
     - `--dry-run`
   - 默认 dry-run，可打印 candidates summary
   - 非 dry-run 写 candidates.json / candidates.csv / ExperimentMemory

4. `configs/candidate_validation.yaml`
   - top_k
   - output_dir
   - run_backtest_smoke
   - run_walk_forward: false
   - experiment name / family
   - baseline oos reference

5. `docs/strategy_candidate_bridge.md`
   - 说明 M11/M11.5/M11.6 的关系
   - 明确 candidate validation 不等于 OOS
   - 说明何时允许写 `oos.*`
   - CLI 示例

6. `tests/test_strategy_candidate_bridge.py`
   - 至少 10 项，全部 synthetic/mock
   - 不联网、不调 LLM、不接 broker

## 测试要求

覆盖：
1. StrategyCandidate round-trip
2. assert_no_oos_metrics 拒绝 oos
3. extract_top_candidates 从 synthetic population result 提取 Top-K
4. write_candidates 写 json/csv
5. candidate backtest smoke 返回 `backtest.total_return` / `backtest.sharpe` / `backtest.max_drawdown`
6. backtest smoke 不写 `oos.*`
7. CLI `--help`
8. CLI dry-run
9. CLI non-dry-run 写 ExperimentMemory
10. `--run-walk-forward` stub 不写 oos，给出清晰 warning 或 metadata

## 禁止
- 不接 broker
- 不调用真实 LLM
- 不联网
- 不跑真实 walk-forward OOS
- 不把 `population.*` / `simulation.*` / `backtest.*` 当论文 OOS
- 不写大文件
- 不破坏 M11/M11.5 现有测试

## 验收

```bash
python -m pytest tests/test_strategy_candidate_bridge.py -v
python -m pytest tests/test_population_training_loop.py tests/test_population_training.py -v
python scripts/export_population_candidates.py --help
python scripts/export_population_candidates.py --population-config configs/population_training.yaml --top-k 2 --run-backtest-smoke --dry-run
python -m pytest -v
```

预期：全量 pytest 从 237 增加到约 247+。
```

### 11.5 M12 — RL 训练

> **完整 Codex 提示词**：[docs/codex_prompt_M12.md](codex_prompt_M12.md)（固定前缀 + 主任务 + 验收命令 + EXP 编号）

```markdown
请为 Quant MAS 实现 v3 模块 **M12：强化学习训练实验**。
详见 docs/codex_prompt_M12.md 全文；以下为摘要。

## 背景
- M7：run_rl_baseline.py、TradingEnv、GRPO ranking（无训练 loop）。
- M11–M11.8：Population → StrategyCandidate → OOS batch（266 pytest）。
- 参考：项目v3设计.md §M12、docs/rl_plan.md、docs/codex_prompt_M12.md。

## 目标
- GRPO / PPO 训练 loop 骨架（MARL 接口预留）
- 训练 metrics：simulation.* / training.*；OOS 仅 M11.7 事后 hook（M12 stub）
- mock env 10 step 训练不 crash；checkpoint → outputs/

## 任务
1. `rl/grpo_agent.py`、`rl/training_loop.py`、`rl/ppo_trainer.py`
2. `scripts/run_rl_experiment.py` — `--dry-run`、`--max-steps`
3. `docs/rl_experiment.md`
4. `tests/test_rl_training.py`（≥12 项）

## 要求
- 不直接下单；不写 oos.*；pytest 全绿（266→278+）
```

### 11.6 M13 — 编排 / MCP 扩展

```markdown
请为 Quant MAS 实现 v3 模块 **M13：企业化编排与协议扩展**。

## 背景
- v2：LangGraph 6 节点、protocols/mcp adapter、export_agent_cards（内部 only）。
- 参考：项目v3设计.md §M13、docs/protocols.md、orchestration/。

## 目标
- DAG 调度多个 mock 实验（competitive / rl / walk_forward 占位节点）
- Agent 消息传递骨架 + audit log
- 仍不接外部 MCP network listener

## 任务
1. `orchestration/agent_communication.py`
2. `orchestration/experiment_scheduler.py` 或 `mcp_scheduler.py`
3. `scripts/run_mcp_pipeline.py`
4. `docs/mcp_protocol.md`
5. `tests/test_mcp_scheduler.py`

## 要求
- ToolPolicy deny 规则保持
- dry-run 默认；pytest 全绿
```

---

## 12. Cursor 维护清单

每次 v3 模块 Codex 完成或服务器实验后，Cursor 维护：

| # | 文档 / 产物 | 内容 |
|---|-------------|------|
| 1 | `docs/progress.md` | 增量记录 M9–M13 状态与 pytest 基线 |
| 2 | `docs/experiment_log.md` | 每次 EXP：Walk-forward OOS、RL、Population、LLM smoke |
| 3 | `docs/architecture.md` | 更新分层、Agent 交互、Memory/RAG、Orchestration |
| 4 | `docs/data_sources.md` | 真实数据源状态、API key 配置、限速 |
| 5 | `docs/database_setup.md` | Postgres / pgvector / Neo4j 部署 |
| 6 | `docs/context_engineering.md` | vLLM / DeepSeek 配置 |
| 7 | `docs/competitive_learning.md` | Population、Elo、Top-K、autocurriculum（M11 后创建） |
| 8 | `docs/strategy_candidate_oos.md` | StrategyCandidate walk-forward OOS 验证边界 |
| 9 | `docs/rl_experiment.md` | GRPO / MARL、奖励函数（M12 后创建） |
| 10 | `docs/mcp_protocol.md` | v3 调度语义（M13 后创建；v2 见 `docs/protocols.md`） |
| 11 | `项目进度.md` | v3 阶段表与服务器一览 |
| 12 | `/mnt/.../reports/` | 服务器训练、回测、协议导出归档 |
| 13 | **ChatGPT 审查** | 每轮 EXP 后审查，拆解下一轮任务 |

**记录纪律**：

- 必须与 baseline **EXP-20260602-008（OOS sharpe 0.586）** 对比
- 不写真实 API Key；不虚构 metrics
- 区分 `oos.*` / `simulation.*` / `population.elo`

---

## 13. API / 服务一览

| 模块 | API / 服务 | 用途 | v3 备注 |
|------|------------|------|---------|
| 数据 | Stooq / Alpha Vantage / Finnhub / FRED / SEC | OHLCV、宏观、filings | M2 ✅；新闻待扩展 |
| Memory | JSON / SQLite / **Postgres** | 实验记录 | M9 |
| 向量 | InMemory / FAISS / **pgvector** | 文档 embedding | M9 |
| 图 | **Neo4j** | 策略–实验 lineage | M9 |
| LLM | DeepSeek / **本地 vLLM** / OpenAI-compatible | 分析、报告 | M10 |
| RL | GRPO / PPO / MARL | 训练、league | M12 |
| 竞争 | Elo / Top-K / autocurriculum | 策略种群 | M11 |
| 编排 | LangGraph / MCP / A2A | DAG、工具、审计 | M13 |
| GitHub | push / pull | 代码同步 | 不含大数据/权重 |
| 服务器 | a6000 GPU / Postgres / 6033+ rows | 训练、OOS、索引 | EXP 主战场 |
| 本地 | Windows + Codex | pytest、模块开发 | mock 默认 |
| Cursor | SSH + 文档 | 实验记录、smoke | 与 ChatGPT 审查配合 |

### 环境变量（v3 新增，示例）

| 变量 | 用途 |
|------|------|
| `DATABASE_URL` | Postgres 连接 |
| `NEO4J_URI` / `NEO4J_USER` / `NEO4J_PASSWORD` | Neo4j |
| `VLLM_BASE_URL` / `VLLM_MODEL` | 本地 vLLM |
| `DEEPSEEK_API_KEY` | 云端 LLM（已有） |
| 各数据源 key | 见 `docs/data_sources.md` |

---

## 14. 总执行路线表

| 阶段 | 模块 | Codex | 服务器 EXP | 文档 |
|------|------|-------|------------|------|
| v3-1 | **M9** 数据库 | ✅ postgres + pgvector + neo4j mock（EXP-025） | Postgres 真实 index/query ✅ **EXP-026** | database_setup.md ✅ |
| v3-2 | **M10** LLM | ✅ local_vllm mock + server smoke（EXP-027/028/LLM-002） | EXP-TEXT-WF-002 | context_engineering.md ✅ · server_commands §6.13 ✅ |
| v3-3 | **M11** 竞争 | Population + Elo smoke | ✅ 双端 EXP-029/POP-002 | competitive_learning.md ✅ |
| v3-3b | **M11.5** 训练闭环 | Multi-gen loop | ✅ 双端 EXP-030/POP-003 | population_training.md ✅ |
| v3-3c | **M11.6** 候选验证桥 | Top-K → StrategyCandidate → backtest smoke | ✅ 双端 EXP-031/POP-004 | strategy_candidate_bridge.md ✅ |
| v3-3d | **M11.7** 候选 OOS | StrategyCandidate → walk-forward OOS | ✅ 双端 EXP-032/POP-005 | strategy_candidate_oos.md ✅ |
| v3-3e | **M11.8** 批量候选 OOS | Top-K candidates → OOS comparison table | ✅ 双端 EXP-033/POP-006 | candidate_oos_batch.md ✅ |
| v3-4 | **M12.1** RL 训练 | ✅ GRPO/PPO short training loop（simulation only） | ✅ 双端 EXP-034/POP-007 | rl_experiment.md ✅ |
| v3-5 | **M13** 编排 | DAG scheduler dry-run | 批量实验 smoke | mcp_protocol.md |

**pytest 基线演进**：195（v2）→ 207（M9）→ 212（M10）→ 225（M11）→ 237（M11.5）→ 248（M11.6）→ 259（M11.7）→ 266（M11.8）→ **282**（M12.1）

---

## 15. 文献与科研方向备忘

| 方向 | 关键词 | 与 v3 模块 |
|------|--------|------------|
| 竞争学习 | Population-Based Training, League Training, Elo | M11 |
| 自博弈 | Self-Play, Autocurriculum | M11 |
| 多 Agent RL | MARL, CTDE | M12 |
| GRPO | Group Relative Policy Optimization | M12（v2 仅有 ranking） |
| 金融 LLM | FinBERT, LoRA, sentiment alpha | M10 |
| RAG 企业化 | pgvector, GraphRAG, Neo4j | M9 |
| 协议 | MCP, A2A, Agent 互操作 | M13 |

**论文叙事建议**：

1. **主结果**仍报 walk-forward OOS（与 0.586 baseline 对比）
2. Population / RL / LLM 作为 **ablation 或机制分析**，不替代 OOS
3. 明确 **LLM 不参与下单** 与 **simulation ≠ OOS**

---

## 附录：与 v2 文档索引

| 文档 | 说明 |
|------|------|
| [`项目进度.md`](项目进度.md) | v2 收官 + 后续 v3 任务入口 |
| [`项目plus设计.md`](项目plus设计.md) | v2 原始 M1–M8 设计（历史） |
| [`docs/architecture.md`](docs/architecture.md) | 当前架构详图 |
| [`docs/research_protocol.md`](docs/research_protocol.md) | OOS 主指标规范 |
| [`docs/protocols.md`](docs/protocols.md) | v2 MCP/A2A（M8） |
| [`docs/rl_plan.md`](docs/rl_plan.md) | v2 RL 模拟（M7） |

---

*v3 设计稿 · M9–M12.1 ✅ 双端闭环 · 下一步：**M12.2** export bridge / EXP-TEXT-WF-002 / M13*
